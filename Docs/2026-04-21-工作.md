# 2026-04-21 工作

- 变更范围 / 目的：按 `Docs/plans/2026-04-21-券分发模式-阶段1-实施计划.md` 推进券分发模式阶段 1，范围限定在 `CouponTemplate` 的分发模式建模、后台录入与小程序公开查询过滤，不触碰订单、下单、mall、核销链路。
- 关键文件：`backend/src/FaJuan.Api/Domain/Entities/CouponTemplate.cs`、`backend/src/FaJuan.Api/Contracts/CouponTemplateDtos.cs`、`backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs`、`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`frontend/src/types/coupon.ts`、`frontend/src/views/coupon-template/CouponTemplateView.vue`
- 当前结论：已确认当前工作分支为 `feature/coupon-distribution-mode`；阶段 1 设计与实施计划齐备，可直接按任务顺序实施。前端阶段 1 相关未提交改动目前集中在 `CouponTemplateView.vue`，后续修改需基于现状增量叠加，避免覆盖既有 UI 收口工作。
- 后续事项 / 风险：EF 迁移必须保持只修改 `CouponTemplate`，若生成迁移裹挟其他表变更需要立即停止并回报；前端表单与表格变更需兼容现有已重构的工作台布局，不回退现有 UI 风格。
- 变更范围 / 目的：完成券分发模式阶段 1 的实现与验证闭环，补齐 `CouponTemplate` 的分发模式/售价字段、后台录入校验、小程序公开查询过滤和后台页面展示。
- 关键文件：`backend/src/FaJuan.Api/Contracts/CouponTemplateDtos.cs`、`backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs`、`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/20260421051303_AddCouponDistributionMode.cs`、`backend/tests/FaJuan.Api.Tests/CouponTemplatesControllerTests.cs`、`frontend/src/types/coupon.ts`、`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/vite.config.ts`
- 验证情况：已执行 `dotnet build src/FaJuan.Api/FaJuan.Api.csproj --nologo -v minimal`、`dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --nologo`、`frontend: npm run build`，全部通过；并完成接口级手测：后台创建 `FreeClaim / PaidStandalone / PackOnly` 三种券模板均成功落库，后台列表正确回显 `distributionMode/salePrice`，`PaidStandalone` 缺失售价返回 400「单张售卖券必须填写售价，且大于 0」，小程序首页 `GET /api/miniapp/home` 只返回 `FreeClaim` 券。
- 后续事项 / 风险：本轮完成的是构建级、单测级和真实接口级验证；尝试追加 Playwright 浏览器自动化验收时，当前 CLI 会话保持与临时包注入方式在本机环境下不稳定，未形成可复用脚本，因此“后台页面实际点选新增/编辑”与“小程序真机/页面级可视化验收”仍建议在本机浏览器或稳定的 Playwright 基座上补做一次。

- 变更范围 / 目的：推进券分发模式阶段 2，打通 `mall` 页对单张售卖券与商品券的展示、下单、支付成功后发券闭环，并补商品券“待履约 / 待 ERP 处理”表达。
- 关键文件：`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`backend/tests/FaJuan.Api.Tests/MiniAppMallControllerTests.cs`、`miniapp/coupon-miniapp/api/mall.js`、`miniapp/coupon-miniapp/api/miniapp.js`、`miniapp/coupon-miniapp/pages/mall/index.vue`、`miniapp/coupon-miniapp/components/StandaloneCouponCard.vue`、`miniapp/coupon-miniapp/components/ProductCouponCard.vue`、`miniapp/coupon-miniapp/pages/sale-coupon/detail.vue`、`miniapp/coupon-miniapp/pages/order/list.vue`、`miniapp/coupon-miniapp/pages/order/result.vue`、`miniapp/coupon-miniapp/pages.json`
- 当前结论：后端已补齐 `mall` 聚合接口测试、统一下单对单张券分支校验以及商品券与单张售卖券在小程序商城页的独立展示入口；小程序端已完成 `mall` 四区块布局、售卖券详情购买页、订单列表/结果页对“单张售卖券 / 商品券 / 券包”三类订单文案兼容。
- 验证情况：已执行 `dotnet build src/FaJuan.Api/FaJuan.Api.csproj --nologo -v minimal`、`dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --nologo`，全部通过；其中新增 `MiniAppMallControllerTests` 3 条测试覆盖 `mall` 聚合分区、双来源下单互斥校验、单张券下单成功。小程序目录当前不是 Node/Vite 工程目录，不存在 `package.json`，因此本轮未能用 `npm run build` 做自动构建，只完成了文件级对接与静态核对。
- 后续事项 / 风险：仍需在 HBuilderX / 微信开发者工具中补一次页面级验收，重点核对 `mall` 新增区块的视觉节奏、`sale-coupon/detail` 的支付流、商品券订单结果页的待履约文案，以及真机支付成功后的页面跳转与数据刷新。

- 变更范围 / 目的：为小程序补齐下拉刷新 + 上拉加载更多。按 `Docs/plans/2026-04-21-小程序下拉刷新与上拉加载-实施计划.md` 完成 4 个 tab 页 + 2 个列表页的改造。
- 关键文件：`miniapp/coupon-miniapp/components/CmPullRefresh.vue`（新增）、`miniapp/coupon-miniapp/composables/use-list-pagination.js`（新增）、`pages/index/index.vue`、`pages/mall/index.vue`、`pages/coupon/index.vue`、`pages/profile/index.vue`、`pages/verify-record/index.vue`、`pages/order/list.vue`
- 当前结论：`CmPullRefresh` 组件以 `scroll-view` + refresher slot 实现带 logo 旋转的主题色下拉动画，底部带"加载中/已到底啦"三态；`use-list-pagination` composable 封装下拉重置+上拉追加的分页状态机。接入方式：首页/商城/卡包接 `loadHomeData/loadMallData/loadCoupons` 做整页重刷；订单列表改用 composable，`pageSize` 从 50 改为 10 并接入真正的上拉加载；我的页、核销记录页无 API，以 scaffold 形式接入，后续接口到位后替换占位 handler。
- 验证情况：代码级实现与 git 提交完成（e79331d / e09d155 / 2d22c5e / 54ee811 / 3723e5b / 7738e32 / 2ff73be / 55b3390 共 8 个 commit）；手工验证待在微信开发者工具中补跑：6 页下拉刷新、订单列表分页、5 主题（default/light/candy/orange/red）颜色适配、真机阻尼感。
- 后续事项 / 风险：①`scroll-view` 设定 `height: 100vh` 可能与小程序底部 tabbar 有叠加，真机上需核对底部内容是否被遮挡；②swiper（首页 banner）嵌在 vertical scroll-view 内部的手势冲突需真机实测；③核销记录页（`pages/verify-record`）的上拉加载待后端新增 `GET /api/miniapp/users/write-off-records` 分页接口后再补；④profile 页的刷新目前只是占位，等拉取用户统计接口后替换。
