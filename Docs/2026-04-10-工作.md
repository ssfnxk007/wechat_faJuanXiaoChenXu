# 2026-04-10 工作

- 变更范围 / 目的：补充仓库级协作规则，明确 PowerShell 只能使用 UTF-8 写文件，并新增统一踩坑记录文档，防止后续持续出现乱码与计划失步问题。
- 关键文件：`AGENTS.md`、`Docs/Error.md`
- 验证情况：已人工复查新增规则与文档内容，均按 UTF-8 写入。
- 后续事项 / 风险：`Docs/plans/2026-04-08-发卷小程序实施计划.md` 仍需单独修编码并补勾选状态；`frontend/src/views/user-coupon/UserCouponView.vue` 还在收口中。

- 变更范围 / 目的：根据现场协作要求，进一步明确文件编辑优先使用 `apply_patch`，避免继续使用 PowerShell 直写导致中文文件风险；同时记录今日收工时的未完成事项，便于下次续做。
- 关键文件：`AGENTS.md`、`Docs/Error.md`、`Docs/2026-04-10-工作.md`
- 验证情况：已复查规则与文档内容落库成功。
- 后续事项 / 风险：
  - `Docs/plans/2026-04-08-发卷小程序实施计划.md` 仍存在编码异常，且尚未补 checkbox 完成状态。
  - `frontend/src/views/user-coupon/UserCouponView.vue` 正在收口“手动发券”功能，前端构建曾报字符串断裂，需下次优先修复并重跑 `npm run build`。
  - 后端“手动发券”最小版接口已开始落地，需下次核对 `backend/src/FaJuan.Api/Controllers/UserCouponsController.cs`、`backend/src/FaJuan.Api/Contracts/UserCouponDtos.cs`、`frontend/src/api/user-coupon.ts`、`frontend/src/types/user-coupon.ts` 与前端页面是否完全对齐。
  - `database/migrations/003-admin-permission-seed.sql` 已补 `user-coupon.grant` 思路，但下次需再次核对并执行到本机数据库后再交付。

- 变更范围 / 目的：继续收口“手动发券”主线，重写用户券页面为干净 UTF-8 版本，补充 `user-coupon.grant` 权限点并同步到数据库，同时重建实施计划文档为可勾选状态。
- 关键文件：`frontend/src/views/user-coupon/UserCouponView.vue`、`backend/src/FaJuan.Api/Controllers/UserCouponsController.cs`、`backend/src/FaJuan.Api/Contracts/UserCouponDtos.cs`、`frontend/src/api/user-coupon.ts`、`frontend/src/types/user-coupon.ts`、`database/migrations/003-admin-permission-seed.sql`、`Docs/plans/2026-04-08-发卷小程序实施计划.md`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`；已执行 `database/migrations/003-admin-permission-seed.sql`，当前 `AdminPermission=29`、`AdminRolePermission=29`，且 `user-coupon.grant` 已入库。
- 后续事项 / 风险：当前“手动发券”已具备最小实现，但还缺少 Excel / CSV 导入发券、真实业务验收，以及更细的运营侧批量发券流程设计。

- 变更范围 / 目的：继续并行推进后台未完成工作，落地 CSV 导入发券最小版，并同步用户券页上传入口与导入结果提示。
- 关键文件：`backend/src/FaJuan.Api/Controllers/UserCouponsController.cs`、`backend/src/FaJuan.Api/Contracts/UserCouponDtos.cs`、`frontend/src/api/user-coupon.ts`、`frontend/src/types/user-coupon.ts`、`frontend/src/views/user-coupon/UserCouponView.vue`、`Docs/plans/2026-04-08-发卷小程序实施计划.md`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`；当前 CSV 方案无需新增数据库结构。
- 后续事项 / 风险：当前 CSV 导入发券为最小版，后续仍需补模板下载、失败明细展示、导入说明，以及按钮权限 EF Migration 正式收口。

- 变更范围 / 目的：继续并行推进 A/B 两项，补齐 CSV 导入发券正式版细节（模板下载、失败明细、导入结果展示），并生成按钮权限正式 EF Migration 与 SQL 脚本。
- 关键文件：`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/types/user-coupon.ts`、`backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/20260410022408_AddAdminPermissionTables.cs`、`backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/20260410022408_AddAdminPermissionTables.Designer.cs`、`backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs`、`database/migrations/004-ef-admin-permission.sql`、`database/migrations/README.md`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`；已通过 `dotnet ef migrations add AddAdminPermissionTables` 和 `dotnet ef migrations script` 生成正式迁移与 SQL。
- 后续事项 / 风险：当前后台剩余重点转为“手动/导入发券联调验收”、真实微信支付接入、指定商品范围完整校验，以及更完整的导入批次留痕能力。

- 变更范围 / 目的：完成“指定商品范围完整校验”最小闭环，补齐券模板商品范围配置、核销商品入参、商品范围数据库结构与测试库落表。
- 关键文件：`backend/src/FaJuan.Api/Domain/Entities/CouponTemplateProductScope.cs`、`backend/src/FaJuan.Api/Contracts/CouponTemplateDtos.cs`、`backend/src/FaJuan.Api/Contracts/UserCouponDtos.cs`、`backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs`、`backend/src/FaJuan.Api/Controllers/WriteOffController.cs`、`backend/src/FaJuan.Api/Infrastructure/Persistence/AppDbContext.cs`、`backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/20260410030401_AddCouponTemplateProductScope.cs`、`database/migrations/005-coupon-template-product-scope.sql`、`frontend/src/types/coupon.ts`、`frontend/src/types/user-coupon.ts`、`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`；已执行 `sqlcmd -S 127.0.0.1 -U sa -P yhdtpass -d FaJuanDb -i database/migrations/005-coupon-template-product-scope.sql`，并确认 `CouponTemplateProductScope` 与迁移历史存在。
- 后续事项 / 风险：当前为“最小可用版”，券模板页的适用商品仍采用商品 ID 文本输入；后续可升级为商品多选器，并继续推进手动/导入发券联调验收与真实微信支付接入。

- 变更范围 / 目的：继续收口发券链路，统一手动发券与导入发券的后端发券逻辑，并补齐“指定用户发放导入能力”，让 CSV 可按 `AppUserId / 手机号 / 小程序 OpenId / 公众号 OpenId` 解析用户。
- 关键文件：`backend/src/FaJuan.Api/Application/UserCoupons/UserCouponGrantService.cs`、`backend/src/FaJuan.Api/Controllers/UserCouponsController.cs`、`frontend/src/views/user-coupon/UserCouponView.vue`、`Docs/plans/2026-04-08-发卷小程序实施计划.md`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`dotnet test backend/tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj -c Release --no-build`、`npm run build`。
- 后续事项 / 风险：当前导入能力已支持多种用户标识，但页面仍以 CSV 模板导入为主，尚未补“导入前预校验预览”；手动/CSV 发券仍建议做一次带真实测试数据的接口联调验收。

- 变更范围 / 目的：补充协作记忆，明确 EF 生成与导出的 SQL 必须人工复查 `SQL Server 2008 R2` 兼容性，避免把高版本语法直接交付到现网库。
- 关键文件：`AGENTS.md`、`Docs/Error.md`
- 验证情况：已人工复查规则已写入协作说明与错误记忆文档。
- 后续事项 / 风险：后续凡是新增 `database/migrations/*.sql`，都必须把 `SQL Server 2008 R2` 兼容性作为交付前检查项之一。

- 变更范围 / 目的：根据最新 UI 约束，移除后台导航中的 emoji 图标，改为统一的纯样式字母图标，并补齐品牌角标与顶栏头像样式，使后台更接近 `Docs/ui/admin-preview-v1.html` 的视觉方向。
- 关键文件：`frontend/src/layouts/AdminLayout.vue`、`frontend/src/style.css`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：当前只是先去掉 emoji 并统一导航图标，后台整体视觉层级仍需再做一轮深度收口，尤其是表格、筛选栏、统计卡片和弹窗的细节质感。

- 变更范围 / 目的：切换为纯前端 UI/UX 收口模式，落地后台 UI V2 任务文档，并完成第一轮深收口：重建商品管理页、重做核销中心页、统一共享布局与去除前端可见“模拟/联调”文案。
- 关键文件：`Docs/ui/2026-04-10-admin-ui-v2-task.md`、`frontend/src/layouts/AdminLayout.vue`、`frontend/src/style.css`、`frontend/src/views/product/ProductView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`、`frontend/src/views/user/UserView.vue`、`frontend/src/views/order/CouponOrderView.vue`、`frontend/src/views/dashboard/DashboardView.vue`、`frontend/src/router/index.ts`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：当前已去除 emoji 图标和“模拟感”文案，但后台视觉仍可继续深挖第二轮，重点是 `券模板管理 / 用户券 / 订单管理 / 仪表盘` 的细节层次与统一感。

- 变更范围 / 目的：收口今日文档状态，同步把“指定商品范围完整校验”计划项标记为完成，并补充该能力落地后的下一步重点与终端乱码判定经验。
- 关键文件：`Docs/plans/2026-04-08-发卷小程序实施计划.md`、`Docs/2026-04-10-工作.md`、`Docs/Error.md`
- 验证情况：已复查计划文档 UTF-8 文件内容，确认“指定商品范围完整校验”勾选位已更新；本轮仅做文档追加，无额外构建命令。
- 后续事项 / 风险：当前仅完成文档收口，仍需由业务代码线继续完成“指定商品券前端模板配置与核销入参联调”、迁移 SQL 执行与测试库验收；PowerShell 终端显示乱码时应以文件实际 UTF-8 内容与构建结果为准，不可仅凭控制台显示误判文件损坏。

- 变更范围 / 目的：继续推进后台未完成主线，落地“CSV 导入发券”最小版，在不改数据库结构的前提下复用现有用户券发放能力完成批量导入。
- 关键文件：`backend/src/FaJuan.Api/Controllers/UserCouponsController.cs`、`backend/src/FaJuan.Api/Contracts/UserCouponDtos.cs`、`frontend/src/api/user-coupon.ts`、`frontend/src/types/user-coupon.ts`、`frontend/src/views/user-coupon/UserCouponView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`。
- 后续事项 / 风险：当前导入仅支持 CSV，不支持 XLSX；CSV 采用单列 `userId` / `用户ID` 方案，适合最低成本上线，后续若需要复杂模板映射或导入批次留痕，再评估数据库结构变更。

- 变更范围 / 目的：调整前端 Vite 开发端口，避免多个本地项目同时占用 `5173` 导致当前项目无法启动。
- 关键文件：`frontend/vite.config.ts`、`Docs/2026-04-10-工作.md`
- 验证情况：已完成 `npm run build`，并启动 `npm run dev -- --host 127.0.0.1` 验证前端可在 `http://127.0.0.1:5180/` 正常提供服务。
- 后续事项 / 风险：如果你有依赖 `5173` 的本地代理、书签或联调配置，需要同步改到 `5180`。

- 变更范围 / 目的：修正前端开发服务仅监听 IPv6/异常不可访问的问题，明确绑定到 `127.0.0.1`，确保浏览器可以稳定打开新端口。
- 关键文件：`frontend/vite.config.ts`、`Docs/2026-04-10-工作.md`
- 验证情况：已停止异常 `node` 进程并重新启动开发服务，确认 `127.0.0.1:5180` 监听正常，`Invoke-WebRequest http://127.0.0.1:5180/` 返回 `200`。
- 后续事项 / 风险：如果你通过 `localhost` 访问仍命中旧缓存，可直接使用 `http://127.0.0.1:5180/` 或刷新后重试。

- 变更范围 / 目的：修正前端开发环境仍指向旧后端 `5001` 端口的问题，统一把本地 API 基地址固定到当前实际开发端口 `5265`。
- 关键文件：`frontend/.env.development`、`frontend/src/api/http.ts`、`Docs/2026-04-10-工作.md`
- 验证情况：已完成 `npm run build`，并确认 `http://localhost:5265/swagger` 返回 `200`。
- 后续事项 / 风险：如果浏览器页面仍请求旧的 `5001`，需要刷新前端开发服务或强制刷新页面，确保最新环境变量已生效。

- 变更范围 / 目的：修正后台 CORS 仍只允许旧前端端口 `5173` 的问题，补充当前本地前端实际使用的 `5180` 与 `127.0.0.1:5180` 来源，恢复后台登录跨域请求。
- 关键文件：`backend/src/FaJuan.Api/Program.cs`、`Docs/2026-04-10-工作.md`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`，并重启后端后用 `curl` 模拟 `OPTIONS /api/adminauth/login` 预检，确认响应包含 `Access-Control-Allow-Origin: http://127.0.0.1:5180`。
- 后续事项 / 风险：如果浏览器里仍保留旧失败请求记录，需刷新页面后再次登录；当前仅放行本地开发常用来源，后续新增前端端口时需同步更新白名单。

- 变更范围 / 目的：修正后台登录后资料查询在旧 SQL Server / 兼容级别下因 `OPENJSON ... WITH` 翻译失败而崩溃的问题，改为兼容性更稳的联表查询。
- 关键文件：`backend/src/FaJuan.Api/Controllers/AdminAuthController.cs`、`Docs/Error.md`、`Docs/2026-04-10-工作.md`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`，并用默认账号 `admin / 123456` 实测 `POST /api/adminauth/login` 与 `GET /api/adminauth/profile`，两者均返回 `200`。
- 后续事项 / 风险：仓库里其他使用 `collection.Contains(...)` 的后台查询仍可能在旧 SQL Server 上触发类似翻译问题，后续如再出现同类 SQL 报错，应按兼容性优先原则逐处收口。

- 变更范围 / 目的：继续收口旧版 SQL Server 兼容问题，修正分页列表接口的 `OFFSET/FETCH` 报错，并补齐缺失的后台权限管理菜单数据。
- 关键文件：`backend/src/FaJuan.Api/Application/Common/QueryablePagingExtensions.cs`、`backend/src/FaJuan.Api/Controllers/UsersController.cs`、`backend/src/FaJuan.Api/Controllers/ProductsController.cs`、`backend/src/FaJuan.Api/Controllers/StoresController.cs`、`backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs`、`backend/src/FaJuan.Api/Controllers/CouponPacksController.cs`、`backend/src/FaJuan.Api/Controllers/CouponOrdersController.cs`、`backend/src/FaJuan.Api/Controllers/UserCouponsController.cs`、`backend/src/FaJuan.Api/Controllers/AdminUsersController.cs`、`backend/src/FaJuan.Api/Controllers/AdminRolesController.cs`、`database/migrations/006-admin-management-menu-backfill.sql`、`Docs/Error.md`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`，并实测 `GET /api/users`、`/api/products`、`/api/stores`、`/api/coupontemplates`、`/api/couponpacks`、`/api/couponorders`、`/api/usercoupons`、`/api/adminusers`、`/api/adminroles` 全部返回 `200`。
- 后续事项 / 风险：详情类、写入类接口虽未发现新的旧 SQL Server 语法异常，但后续新增分页列表必须统一走兼容分页扩展；新菜单模块也要同步补菜单和角色菜单数据，避免再出现只有按钮权限没有菜单授权的 403。

- 变更范围 / 目的：继续收口后台 UI，补齐商品图片素材表单闭环，并把订单管理、核销中心中的手填 ID 改成业务选择器，降低运营录入成本。
- 关键文件：`frontend/src/views/product/ProductView.vue`、`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/order/CouponOrderView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`
- 验证情况：已完成 `cd frontend && npm run build`，构建通过；同时核对商品页主图必填、详情图可选、订单页用户/券包选择、核销页门店/商品选择的前端交互链路。
- 后续事项 / 风险：`CouponPackItemView`、`UserCouponView` 里的部分手填 ID 仍需继续改造为选择器；券包页图片入口本轮因并行收口先恢复稳定版本，下一轮继续补封面素材交互。

- 变更范围 / 目的：继续优化发券后台交互，把用户券页中的手填用户 ID / 模板 ID 收口为选择器，并补齐券包明细页的业务化选项展示。
- 关键文件：`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/coupon-pack/CouponPackItemView.vue`
- 验证情况：已完成 `cd frontend && npm run build`，构建通过；确认用户券页支持按用户选择筛选、手动发券支持模板选择与用户搜索选择、导入发券仅保留模板选择器。
- 后续事项 / 风险：`CouponPackView` 的封面素材交互、`CouponTemplateView` 的封面素材体验、以及用户券页更强的批量用户选择体验仍可继续提升；如后续用户量很大，`getUserList()` 可能需要后端补分页/搜索接口以支撑更高效选择。

- 变更范围 / 目的：根据前后端适配审查结果，修复 3 个已确认问题：订单号搜索改为服务端过滤、券模板图片素材改用后端支持的 `shared` 分区、用户选择器不再只拿后端默认前 20 条记录。
- 关键文件：`backend/src/FaJuan.Api/Controllers/CouponOrdersController.cs`、`frontend/src/api/coupon-pack.ts`、`frontend/src/views/order/CouponOrderView.vue`、`frontend/src/api/user.ts`、`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/coupon-template/CouponTemplateView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1` 与 `cd frontend && npm run build`，均通过；同时复核订单页已把关键词下推到后端、券模板上传素材分区已改为 `shared`。
- 后续事项 / 风险：当前用户选择器已从后端拉取 200 条作为临时缓解，若实际用户量继续增长，仍建议补真正的远程搜索/分页选择接口，避免前端一次性加载过多用户。

- 变更范围 / 目的：补统一登录失效提示，避免后台会话过期时只看到网络面板一串 `401` 红色请求而没有明确用户提示。
- 关键文件：`frontend/src/api/http.ts`
- 验证情况：已完成 `cd frontend && npm run build`，构建通过；当前 `401` 响应会统一弹出“登录已失效，请重新登录”，并对同一波失效请求做了短时去重处理，避免重复弹多次。
- 后续事项 / 风险：当前仍是跳转登录页前提示；如果后续要做更完整体验，可继续补“会话过期弹窗确认 + 返回当前页”的流程。

- 变更范围 / 目的：执行图片素材能力数据库迁移，补齐 `Product` 图片字段和 `MediaAsset/Banner` 表，修复 API 报“列名无效”的运行时错误。
- 关键文件：`database/migrations/007-media-assets-and-banners.sql`、`Docs/2026-04-10-工作.md`
- 验证情况：已连接 `127.0.0.1 / FaJuanDb` 执行 `007-media-assets-and-banners.sql`；随后用 `INFORMATION_SCHEMA` 验证 `Product.MainImageAssetId`、`Product.DetailImageAssetIds`、`CouponTemplate.ImageAssetId`、`CouponPack.ImageAssetId` 已存在，且 `MediaAsset`、`Banner` 表已创建。
- 后续事项 / 风险：当前这批图片能力表结构是通过 SQL 脚本落库，不是启动自动迁移；后续若要统一迁移管理，建议补正式 EF Migration 并明确执行顺序。

- 变更范围 / 目的：开始收口 P0-1 远程搜索选择器，把后台高频选择场景从固定前 100/200 条列表切换为“关键词 + 远程加载”的正式操作方式。
- 关键文件：`frontend/src/api/user.ts`、`frontend/src/views/order/CouponOrderView.vue`、`frontend/src/views/coupon-pack/CouponPackItemView.vue`、`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`
- 验证情况：已完成 `cd frontend && npm run build`，构建通过；订单创建支持按用户/券包关键词搜索，券包明细支持按券包/券模板搜索，用户券支持按模板/用户搜索，核销页支持按门店/商品搜索。
- 后续事项 / 风险：当前仍是“关键词查询 + 重新加载下拉结果”的轻量方案，还不是独立弹窗式远程搜索组件；如数据量继续增长，下一轮建议提炼通用远程选择器组件并补分页翻页能力。

- 变更范围 / 目的：推进 P0-1 第二轮组件化，新增通用远程选择器字段组件，并在订单页、券包明细页先完成接入，减少重复 UI 与脚本逻辑。
- 关键文件：`frontend/src/components/RemoteSelectField.vue`、`frontend/src/views/order/CouponOrderView.vue`、`frontend/src/views/coupon-pack/CouponPackItemView.vue`
- 验证情况：已完成 `cd frontend && npm run build`，构建通过；订单页与券包明细页已改为统一的“搜索 + 选择”组件形式。
- 后续事项 / 风险：`UserCouponView`、`WriteOffView` 当前仍是页面内分散实现，下一轮可继续替换到 `RemoteSelectField.vue`，再往后再抽更高级的弹窗式远程选择器。

- 变更范围 / 目的：完成 P0-1 第二轮剩余页面组件化，把用户券页与核销中心页也切换到统一远程选择器组件，进一步统一后台选择交互颗粒度。
- 关键文件：`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`、`frontend/src/components/RemoteSelectField.vue`
- 验证情况：已完成 `cd frontend && npm run build`，构建通过；当前订单、券包明细、用户券、核销中心的主要远程选择入口都已统一使用同一组件模式。
- 后续事项 / 风险：用户多选仍保留用户券页的卡片式选择实现，后续如要进一步统一，可再抽“远程多选器”组件；当前组件尚未内置分页翻页能力，适合中等数据量阶段使用。

- 变更范围 / 目的：开始推进 P0-2 提交态统一，先为订单、用户券、核销、商品这四个高频操作页补 loading 文案、按钮禁用和防重复提交保护。
- 关键文件：`frontend/src/views/order/CouponOrderView.vue`、`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`、`frontend/src/views/product/ProductView.vue`
- 验证情况：已完成 `cd frontend && npm run build`，构建通过；当前创建订单、处理支付、手动发券、导入发券、执行核销、商品保存/删除都已具备基本防连点能力。
- 后续事项 / 风险：`CouponTemplateView`、`CouponPackView`、`CouponPackItemView`、`Admin*` 系列页面的提交态仍可继续统一；当前第一轮只覆盖了业务最频繁的 4 个核心页面。

- 变更范围 / 目的：继续推进 P0-2 第二批，补齐券模板、券包、券包明细三张核心业务页的提交态、按钮禁用和防重复提交。
- 关键文件：`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/coupon-pack/CouponPackView.vue`、`frontend/src/views/coupon-pack/CouponPackItemView.vue`
- 验证情况：已完成 `cd frontend && npm run build`，构建通过；当前券模板保存/删除、券包保存/删除、券包明细保存/删除都已具备基础防连点能力。
- 后续事项 / 风险：后台业务主链路页面的提交态已基本补齐；下一轮若继续做 P0-2，可把管理员、角色、菜单、门店等基础管理页也统一到同一规则，并考虑抽复用 composable 简化重复代码。

- 变更范围 / 目的：继续抽查详情接口与写入接口，确认在旧版 SQL Server 环境下不只列表接口可用，基础写入链路也没有新的语法兼容异常。
- 关键文件：`Docs/2026-04-10-工作.md`
- 验证情况：已重启本地 API，并实测 `POST /api/products` 正向创建返回 `200`；同时对 `POST /api/products`、`/api/stores`、`/api/coupontemplates`、`/api/couponpacks`、`/api/couponorders`、`/api/writeoff` 的空请求校验路径验证均返回 `400` 而非数据库异常。`/api/couponorders/{id}` 与 `/api/usercoupons/{id}` 因当前测试库无数据，暂记为 `SKIP`。
- 后续事项 / 风险：订单详情、用户券详情、核销记录等读取链路仍建议在补充测试数据后再做一次正向验收，但目前未发现新的旧 SQL Server 语法兼容错误。

- 变更范围 / 目的：补充最小测试数据并完成订单详情、用户券详情与核销记录链路的正向验收，确认旧版 SQL Server 环境下核心读写闭环可用。
- 关键文件：`Docs/2026-04-10-工作.md`
- 验证情况：已补充测试 `AppUser`、门店、券模板、券包数据，并实测 `POST /api/couponorders`、`POST /api/usercoupons/manual-grant`、`POST /api/writeoff` 返回 `200`；同时验证 `GET /api/couponorders/{id}`、`GET /api/usercoupons/{id}`、`GET /api/usercoupons/{id}/writeoff-records` 全部返回 `200`。
- 后续事项 / 风险：当前兼容性验证已覆盖认证、列表、详情、创建、手动发券与核销主链路；后续如新增模块，应继续遵守旧 SQL Server 兼容分页和权限初始化规则。

- 变更范围 / 目的：按后台 UI V2 任务收口共享层视觉，聚焦 `AdminLayout.vue` 与全局共享样式，使后台整体更靠近 `Docs/ui/admin-preview-v1.html`，且继续禁用 emoji 图标。
- 关键文件：`frontend/src/layouts/AdminLayout.vue`、`frontend/src/style.css`
- 验证情况：已执行 `frontend/npm run build`；当前构建被现有路由依赖缺失阻塞，报错为 `frontend/src/router/index.ts` 引用的 `frontend/src/views/coupon-template/CouponTemplateView.vue` 不存在，本轮未修改该业务文件。
- 后续事项 / 风险：本轮仅收口共享布局与共享样式，未改业务逻辑；待缺失的 `CouponTemplateView.vue` 补齐后需重跑前端构建并做页面视觉走查。

- 变更范围 / 目的：重做 `DashboardView.vue`、`UserView.vue`、`StoreView.vue` 三个后台页面，统一到新版正式后台风格，去除页面可见的非正式表达，并尽量复用共享样式体系。
- 关键文件：`frontend/src/views/dashboard/DashboardView.vue`、`frontend/src/views/user/UserView.vue`、`frontend/src/views/store/StoreView.vue`、`frontend/src/style.css`
- 验证情况：已完成 `cd frontend && npm run build`。
- 后续事项 / 风险：当前三页已完成视觉收口；若后续还要继续统一其他业务页，可沿用本轮新增的共享块样式（如 `overview-grid`、`surface-note`）继续扩展。

- 变更范围 / 目的：继续按后台 UI V2 任务重做两张核心业务页，仅处理前端 UI/UX，重建 `券模板管理` 与 `用户券中心` 的页面结构、弹窗层次与运营工作台视觉。
- 关键文件：`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/user-coupon/UserCouponView.vue`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：建议下一轮继续统一 `coupon-pack`、`coupon-pack-items`、`coupon-orders` 三个页面的视觉语言，并做一次整站后台 UI 走查，重点检查中文文案密度与弹窗信息层级。

- 变更范围 / 目的：继续按后台 UI V2 任务收口剩余三张业务页，仅处理前端 UI/UX，重建 `券包管理`、`券包明细`、`订单管理` 的运营后台页面层次与弹窗风格。
- 关键文件：`frontend/src/views/coupon-pack/CouponPackView.vue`、`frontend/src/views/coupon-pack/CouponPackItemView.vue`、`frontend/src/views/order/CouponOrderView.vue`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：后台核心业务页第一轮 V2 收口已完成，下一轮建议做整站 UI 走查与细节统一，重点收口仪表盘、用户、门店、权限页与业务页之间的视觉密度差异。

- 变更范围 / 目的：继续按后台 UI V2 任务收口剩余基础页与权限页，完成 `仪表盘`、`用户管理`、`门店管理`、`管理员`、`角色管理`、`菜单管理` 的视觉统一与可构建恢复。
- 关键文件：`frontend/src/views/dashboard/DashboardView.vue`、`frontend/src/views/user/UserView.vue`、`frontend/src/views/store/StoreView.vue`、`frontend/src/views/admin-user/AdminUserView.vue`、`frontend/src/views/admin-role/AdminRoleView.vue`、`frontend/src/views/admin-menu/AdminMenuView.vue`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：后台主要页面第一轮 V2 收口已基本完成，下一轮建议做全站细节统一，重点处理中文文案密度、筛选区布局统一、弹窗字段密度和表格信息层级。

- 变更范围 / 目的：完成最后一轮全站 UI 精修，统一登录页、全局提示、空状态和按钮禁用反馈，使后台视觉语言更完整一致。
- 关键文件：`frontend/src/views/login/LoginView.vue`、`frontend/src/components/AppToast.vue`、`frontend/src/style.css`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：当前后台 UI 主体已完成，可转入局部细节走查或开始对接真实设计稿 / 小程序 UI 参考稿。

- 变更范围 / 目的：根据业务反馈做表单交互纠偏，移除用户管理页中的开发式建档/绑定入口，并把券模板的“指定商品 ID 手填”改成“搜索商品 + 勾选商品”的选择器。
- 关键文件：`frontend/src/views/user/UserView.vue`、`frontend/src/views/coupon-template/CouponTemplateView.vue`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：商品图片能力已在前端类型中出现，后续待 API 图片上传/素材库接口稳定后，再补“商品主图上传、轮播图上传、素材复用库”交互。

- 变更范围 / 目的：按新的图片能力方案落地后端素材库与轮播图最小闭环，新增 `MediaAsset` / `Banner` API、本地图片上传入口，并把商品、券模板、券包从直接 URL 字段切换为素材引用，满足“商品图片区 / 轮播图区 / 素材复用区”分开管理。
- 关键文件：`backend/src/FaJuan.Api/Domain/Entities/MediaAsset.cs`、`backend/src/FaJuan.Api/Domain/Entities/Banner.cs`、`backend/src/FaJuan.Api/Contracts/MediaAssetDtos.cs`、`backend/src/FaJuan.Api/Contracts/BannerDtos.cs`、`backend/src/FaJuan.Api/Controllers/MediaAssetsController.cs`、`backend/src/FaJuan.Api/Controllers/BannersController.cs`、`backend/src/FaJuan.Api/Controllers/ProductsController.cs`、`backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs`、`backend/src/FaJuan.Api/Controllers/CouponPacksController.cs`、`backend/src/FaJuan.Api/Infrastructure/Persistence/AppDbContext.cs`、`backend/src/FaJuan.Api/Program.cs`、`database/migrations/007-media-assets-and-banners.sql`
- 验证情况：待执行 `dotnet build backend/src/FaJuan.Api/FaJuan.Api.csproj -c Release -m:1`。
- 后续事项 / 风险：当前上传先落本地 `wwwroot/uploads/media/`，适合本地和单机部署；旧的 `MainImageUrl` / `ImageUrl` 列仍在数据库中保留用于平滑迁移，等线上数据迁完后再单独清理字段。

- ???? / ??????? `P0-2` ???????????????????? `P1` ??????????/??/??/????????????????????????????????????
- ?????`frontend/src/views/admin-user/AdminUserView.vue`?`frontend/src/views/admin-role/AdminRoleView.vue`?`frontend/src/views/admin-menu/AdminMenuView.vue`?`frontend/src/views/store/StoreView.vue`?`frontend/src/views/coupon-template/CouponTemplateView.vue`?`frontend/src/types/coupon.ts`?`frontend/src/types/coupon-pack.ts`
- ???????? `frontend/npm run build`??????
- ???? / ???`admin-user`?`admin-role` ???????????????????????????????????????????????????

- 变更范围 / 目的：固化本机 PowerShell 编码治理方案，明确 `powershell.exe 5.1` 的中文编码风险与优先替代方案，避免后续代理再次踩同类乱码坑。
- 关键文件：`AGENTS.md`、`Docs/Error.md`
- 验证情况：已实测确认本机 `powershell.exe 5.1` 曾出现 `InputEncoding/OutputEncoding/$OutputEncoding` 不一致问题，并已将解决方案和预防规则写入文档。
- 后续事项 / 风险：若后续 Codex 桌面版开放 shell 切换配置，优先改为 `pwsh`；在此之前仍按 `apply_patch > python > pwsh > powershell.exe` 执行。
