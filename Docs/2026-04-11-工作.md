# 2026-04-11 工作

- 变更范围 / 目的：在 `miniapp/coupon-miniapp` 中完成 Uni-app 第一轮前端骨架搭建，采用“新中式绿意”方案落地 4 个主页面结构，并实现首页首屏高保真版本。
- 关键文件：`miniapp/coupon-miniapp/pages.json`、`miniapp/coupon-miniapp/App.vue`、`miniapp/coupon-miniapp/uni.scss`、`miniapp/coupon-miniapp/components/SectionHeader.vue`、`miniapp/coupon-miniapp/components/CouponCard.vue`、`miniapp/coupon-miniapp/components/CouponPackCard.vue`、`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/pages/mall/index.vue`、`miniapp/coupon-miniapp/pages/coupon/index.vue`、`miniapp/coupon-miniapp/pages/profile/index.vue`
- 验证情况：已校验 `pages.json` 为合法 JSON，关键页面与组件文件已创建完成。
- 后续事项 / 风险：当前 TabBar 图标先使用占位资源，下一轮需替换为正式图标，并补齐 `券详情/核销二维码页`、`券包详情/购买页`、`订单结果页`。

- 变更范围 / 目的：补强小程序首页业务表达，在首页新增“直接领券专区”，补齐无门槛券、满减券、指定商品券、活动券等免费可领券型展示，并与付费券包区分层呈现。
- 关键文件：`miniapp/coupon-miniapp/pages/index/index.vue`
- 验证情况：已完成首页结构重组，首页同时具备 `新人券 / 直接领券 / 券包购买 / 推荐商品` 四层业务展示。
- 后续事项 / 风险：下一轮建议继续做 `券详情/核销二维码页`，把首页“直接领券”入口真正接到业务闭环页面。

- 变更范围 / 目的：新增小程序 `券详情 / 核销二维码页`，补齐首页领券后的核心业务落地点，承载二维码核销、券码、有效期、门店范围与使用规则展示。
- 关键文件：`miniapp/coupon-miniapp/pages.json`、`miniapp/coupon-miniapp/pages/coupon/detail.vue`
- 验证情况：已校验 `pages/coupon/detail` 路由已写入 `pages.json`，详情页文件已创建成功。
- 后续事项 / 风险：当前二维码为静态占位方格，后续接真实接口后需替换为正式二维码组件或服务端返回码图；建议下一轮补 `券包详情/购买页`。

- 变更范围 / 目的：新增小程序 `券包详情 / 购买页`，补齐商城与首页券包入口的购买落地点，承载售价、券明细、购买说明、发放规则与固定购买栏。
- 关键文件：`miniapp/coupon-miniapp/pages.json`、`miniapp/coupon-miniapp/pages/coupon-pack/detail.vue`
- 验证情况：已校验 `pages/coupon-pack/detail` 路由已写入 `pages.json`，详情页文件已创建成功。
- 后续事项 / 风险：当前购买按钮仍为静态 UI，下一轮需补 `订单结果页` 并接入真实下单 / 支付流程。

- 变更范围 / 目的：新增小程序 `订单结果页`，补齐券包购买后的支付成功承接页，展示订单号、支付金额、到账权益和后续动作入口。
- 关键文件：`miniapp/coupon-miniapp/pages.json`、`miniapp/coupon-miniapp/pages/order/result.vue`
- 验证情况：已校验 `pages/order/result` 路由已写入 `pages.json`，结果页文件已创建成功。
- 后续事项 / 风险：当前页面仍为静态演示数据，下一轮需接实际下单结果数据并补页面间跳转。

- 变更范围 / 目的：补齐小程序首轮页面跳转闭环，并收口 `券包详情 / 券详情 / 订单结果 / 首页 / 商城 / 我的` 的关键交互，避免 TabBar 路由误用和无效按钮。
- 关键文件：`miniapp/coupon-miniapp/components/SectionHeader.vue`、`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/pages/mall/index.vue`、`miniapp/coupon-miniapp/pages/profile/index.vue`、`miniapp/coupon-miniapp/pages/coupon/detail.vue`、`miniapp/coupon-miniapp/pages/order/result.vue`、`miniapp/coupon-miniapp/pages/coupon-pack/detail.vue`
- 验证情况：已通过关键字检索确认 `SectionHeader` 支持 `action-click`，并确认首页/商城/订单结果页入口、个人中心 TabBar 跳转、券详情返回卡包等 wiring 已落地；券包详情页已补充权益概览、适用场景、购买流程三块内容。
- 后续事项 / 风险：`TabBar` 图标仍为占位资源；商城商品列表和个人中心“订单/核销记录/规则说明”仍属于占位式页面入口，后续需要补真实列表页或正式路由。

- 变更范围 / 目的：继续推进小程序 UI 第二轮收口，同时完成 A+B 两项：替换正式 TabBar 图标，并补齐 `订单列表 / 核销记录 / 规则说明` 三个正式业务页面。
- 关键文件：`miniapp/coupon-miniapp/pages.json`、`miniapp/coupon-miniapp/pages/profile/index.vue`、`miniapp/coupon-miniapp/pages/order/result.vue`、`miniapp/coupon-miniapp/pages/mall/index.vue`、`miniapp/coupon-miniapp/pages/order/list.vue`、`miniapp/coupon-miniapp/pages/verify-record/index.vue`、`miniapp/coupon-miniapp/pages/rules/index.vue`、`miniapp/coupon-miniapp/static/tabbar/home.png`、`miniapp/coupon-miniapp/static/tabbar/home-active.png`、`miniapp/coupon-miniapp/static/tabbar/mall.png`、`miniapp/coupon-miniapp/static/tabbar/mall-active.png`、`miniapp/coupon-miniapp/static/tabbar/coupon.png`、`miniapp/coupon-miniapp/static/tabbar/coupon-active.png`、`miniapp/coupon-miniapp/static/tabbar/profile.png`、`miniapp/coupon-miniapp/static/tabbar/profile-active.png`
- 验证情况：已校验 `pages.json` 可正常解析，新增 3 个页面路由已登记；正式页文件与 8 张 TabBar PNG 资源均已生成；个人中心入口已改为对应正式页，订单结果页“查看订单记录”已接到订单列表，商城商品列表已替换掉数字占位文案。
- 后续事项 / 风险：当前仍是静态展示数据，下一轮建议接真实接口并在 HBuilder X / 微信开发者工具里做一次视觉与点击链路验收。


- 变更范围 / 目的：为 `miniapp/coupon-miniapp` 补齐最小可用请求层与会话状态层，并将首页、商城券包列表、订单列表切换为真实 API 结构调用骨架，接口不可用时保留安全 fallback。
- 关键文件：`miniapp/coupon-miniapp/App.vue`、`miniapp/coupon-miniapp/store/session.js`、`miniapp/coupon-miniapp/utils/request.js`、`miniapp/coupon-miniapp/api/auth.js`、`miniapp/coupon-miniapp/api/home.js`、`miniapp/coupon-miniapp/api/mall.js`、`miniapp/coupon-miniapp/api/order.js`、`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/pages/mall/index.vue`、`miniapp/coupon-miniapp/pages/order/list.vue`
- 验证情况：已使用 `node --check` 校验新增 `api/ utils/ store/` JS 文件语法，并抽取 3 个页面的 `<script setup>` 内容进行语法检查通过。
- 后续事项 / 风险：当前首页 `/api/miniapp/home`、商城 `/api/miniapp/coupon-packs` `/api/miniapp/products`、订单 `/api/miniapp/orders` 仍依赖后端提供小程序公开接口；订单页真实登录依赖 `/api/auth/wechat-status` 与 `/api/auth/mini-login` 的可访问环境与微信配置。

- 变更范围 / 目的：新增后端小程序专用接口，隔离后台管理接口权限，向小程序提供首页聚合、券包、商品、用户卡券、订单详情等公开/轻鉴权数据入口。
- 关键文件：`backend/src/FaJuan.Api/Contracts/MiniAppDtos.cs`、`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`
- 验证情况：已执行 `dotnet build G:\发卷小程序\backend\src\FaJuan.Api\FaJuan.Api.csproj -c Release -m:1`，编译通过且 0 错误 0 警告。
- 后续事项 / 风险：当前小程序专用接口采用 `userId` 入参作为最小改动方案，后续若需要更严格的小程序会话鉴权，可再升级为基于登录态 token / openId 的用户上下文方案。

- 变更范围 / 目的：继续把小程序核心业务页接到真实 API，包括卡包列表、券详情、券包详情、订单结果页，并保留当前视觉层级与安全 fallback。
- 关键文件：`miniapp/coupon-miniapp/api/miniapp.js`、`miniapp/coupon-miniapp/pages/coupon/index.vue`、`miniapp/coupon-miniapp/pages/coupon/detail.vue`、`miniapp/coupon-miniapp/pages/coupon-pack/detail.vue`、`miniapp/coupon-miniapp/pages/order/result.vue`
- 验证情况：已再次抽取上述页面的 `<script setup>` 内容并通过 `node --check` 做语法检查；后端编译通过后，前端请求路由已与 `/api/miniapp/*` 对齐。
- 后续事项 / 风险：当前券详情页二维码仍为静态占位方格，等后端提供真实二维码码值/码图后再替换为真实核销码展示；订单结果页目前依赖路由参数 `id` 才会回填真实订单详情。

- 变更范围 / 目的：继续收口首页/商城/订单结果页的真数据展示细节，补上带真实 ID 的详情跳转、图片背景承接和订单摘要回填，让小程序视觉与数据衔接更自然。
- 关键文件：`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/pages/mall/index.vue`、`miniapp/coupon-miniapp/pages/order/result.vue`
- 验证情况：已再次抽取 `index / mall / order` 页面脚本做 `node --check`，语法通过；首页和商城详情跳转现在可携带 `id` 路由参数，订单结果页顶部摘要已绑定真实订单字段。
- 后续事项 / 风险：由于当前环境不便常驻启动本地 API 做长链路联调，建议下一步在 HBuilder X / 微信开发者工具中配合本地 `FaJuan.Api` 运行实例完成一次页面点击验收。

- 变更范围 / 目的：补通小程序券包购买主链路，新增小程序匿名下单、发起支付、支付完成确认接口；同步把 `coupon-pack/detail`、`order/result`、`profile` 对齐真实接口与当前用户态。
- 关键文件：`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`backend/src/FaJuan.Api/Contracts/MiniAppDtos.cs`、`miniapp/coupon-miniapp/api/miniapp.js`、`miniapp/coupon-miniapp/pages/coupon-pack/detail.vue`、`miniapp/coupon-miniapp/pages/order/result.vue`、`miniapp/coupon-miniapp/pages/profile/index.vue`、`miniapp/coupon-miniapp/pages/index/index.vue`。
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 通过；已对小程序相关 `api/*.js` 与页面 `script setup` 执行 `node --check` 通过。
- 后续事项 / 风险：首页“直接领券 / 新人券”仍缺券模板详情与领券接口；券详情二维码仍是视觉占位，后续需接真实二维码生成/返回能力。


- 变更范围 / 目的：收口 `miniapp/coupon-miniapp` 的领券与券包 UI 语义，明确区分“可领取券模板说明”和“已到账卡券详情”，避免用户在首页误把模板当成已到账卡券。
- 关键文件：`miniapp/coupon-miniapp/components/CouponCard.vue`、`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/pages/coupon/index.vue`、`miniapp/coupon-miniapp/pages/coupon/detail.vue`
- 验证情况：已对上述 4 个 Vue 文件的 `<script setup>` 内容执行 `node --check`，通过。
- 后续事项 / 风险：当前首页“可领取券模板”仅做前端说明态；若要支持真实领取按钮、模板详情拉取与领取结果回写，仍需后端提供模板详情/领券接口。

- 变更范围 / 目的：补通小程序首页领券闭环，新增券模板详情、立即领取、用户券二维码图片接口；同步把小程序券详情页改为“模板详情/已领取详情”双模式。
- 关键文件：`backend/src/FaJuan.Api/FaJuan.Api.csproj`、`backend/src/FaJuan.Api/Contracts/MiniAppDtos.cs`、`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`miniapp/coupon-miniapp/api/miniapp.js`、`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/pages/coupon/detail.vue`。
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 通过；已对小程序 `api/miniapp.js`、`pages/index/index.vue`、`pages/coupon/detail.vue`、`pages/coupon-pack/detail.vue` 的 `script setup` 执行 `node --check` 通过。
- 后续事项 / 风险：卡包页与首页的中文文案仍有部分历史编码污染，建议下一轮专门做一轮 miniapp 文案/视觉清洗；券详情二维码已改为真实接口图片，但仍需在微信开发者工具内实测显示与扫码识别效果。

- 变更范围 / 目的：针对小程序前端做一轮用户文案清洗，删除过多功能说明文字，压缩首页、卡券、券包、订单结果、规则页等副标题和说明文案，并同步清理历史乱码字符串。
- 关键文件：`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/pages/mall/index.vue`、`miniapp/coupon-miniapp/pages/coupon/index.vue`、`miniapp/coupon-miniapp/pages/coupon/detail.vue`、`miniapp/coupon-miniapp/pages/coupon-pack/detail.vue`、`miniapp/coupon-miniapp/pages/order/result.vue`、`miniapp/coupon-miniapp/pages/profile/index.vue`、`miniapp/coupon-miniapp/pages/verify-record/index.vue`、`miniapp/coupon-miniapp/pages/rules/index.vue`。
- 验证情况：已对上述页面 `script setup` 执行 `node --check` 通过；已复查关键中文可见文案不再出现明显乱码。
- 后续事项 / 风险：当前主要完成了文字瘦身与清洗，下一轮建议在微信开发者工具里按首页、领券、购券、卡包、核销记录链路做一轮视觉与交互实测。

- 变更范围 / 目的：按最新首页视觉口径统一动作文案与头图结构，将首页顶部主视觉改为轮播图区样式，并统一“免费领取 / 立即购买 / 我的券包”按钮表达。
- 关键文件：`miniapp/coupon-miniapp/api/home.js`、`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/components/CouponCard.vue`。
- 验证情况：已对 `api/home.js`、`pages/index/index.vue`、`components/CouponCard.vue`、`components/CouponPackCard.vue` 执行 `node --check` 通过。
- 后续事项 / 风险：当前首页轮播已落样式并支持后端 `banners` 数据，但仍建议在微信开发者工具中确认轮播高度、圆角和指示点实际观感。

- 变更范围 / 目的：根据首页最新反馈，移除轮播图下方多余按钮，并把轮播下三块信息卡统一收口为更明确的首页功能说明卡：`新人专享 / 免费领取 / 到店核销`。
- 关键文件：`miniapp/coupon-miniapp/pages/index/index.vue`。
- 验证情况：已对 `pages/index/index.vue` 的 `script setup` 执行 `node --check` 通过。
- 后续事项 / 风险：轮播图点击跳转逻辑后续可直接绑定 `banners.linkUrl` 或活动路由；当前先保留视觉轮播位。

- 变更范围 / 目的：将首页轮播下三块改为“轻入口卡片”，分别承接 `新人专享 -> 新人券`、`免费领取 -> 免费券区`、`到店核销 -> 规则页`，避免纯展示卡片信息密度低。
- 关键文件：`miniapp/coupon-miniapp/pages/index/index.vue`。
- 验证情况：已对 `pages/index/index.vue` 的 `script setup` 执行 `node --check` 通过。
- 后续事项 / 风险：如后续希望三块做得更像快捷入口，可再加小图标或箭头，但当前先保持简洁。

- 变更范围 / 目的：小程序真机调试前置问题排查与页面闭环修复，补齐运行时未定义变量、死按钮、路由参数不匹配等阻塞性问题。
- 关键文件：`miniapp/coupon-miniapp/pages/profile/index.vue`、`miniapp/coupon-miniapp/pages/coupon-pack/detail.vue`、`miniapp/coupon-miniapp/pages/order/result.vue`、`miniapp/coupon-miniapp/pages/order/list.vue`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 通过；已对修复页面执行 `node --check` 通过。
- 后续事项 / 风险：需在 HBuilder X / 微信开发者工具中配合本地 API 实例完成真机视觉与点击链路验收；`manifest.json` 仍需填写真实小程序 AppId；真机调试如使用 HTTP 内网地址，需开启"不校验合法域名"。如需修改默认 API 基地址，请直接编辑 `miniapp/coupon-miniapp/store/session.js` 中的 `DEFAULT_API_BASE_URL`。


- 变更范围 / 目的：将小程序默认 API 基地址从 `127.0.0.1:5265` 改为实际内网地址 `10.168.1.106:5265`，以便真机/模拟器直接联调后端。
- 关键文件：`miniapp/coupon-miniapp/store/session.js`
- 验证情况：已确认文件内容更新正确，`DEFAULT_API_BASE_URL` 已指向新地址。
- 后续事项 / 风险：真机调试前请确认后端已监听 `10.168.1.106:5265`，并在微信开发者工具中开启"不校验合法域名"。

- 变更范围 / 目的：配置真实微信小程序参数，将用户提供的 AppId / AppSecret 填入后端配置与小程序 manifest，完成小程序真实登录的前置配置项。
- 关键文件：`backend/src/FaJuan.Api/appsettings.json`、`backend/src/FaJuan.Api/appsettings.Development.json`、`miniapp/coupon-miniapp/manifest.json`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 通过；已确认 `manifest.json` 中 `mp-weixin.appid` 已更新。
- 后续事项 / 风险：真实微信支付参数（商户号、证书序列号、私钥 PEM 路径、APIv3 Key、公网回调地址）仍缺失，需补齐后方可进行真实支付联调。

- 变更范围 / 目的：修正开发环境 API 监听地址为局域网可访问，并将首页“免费领取”区域改成与推荐商品一致的两列网格表现。
- 关键文件：`backend/src/FaJuan.Api/Properties/launchSettings.json`、`miniapp/coupon-miniapp/pages/index/index.vue`。
- 验证情况：已执行 `node --check` 校验首页脚本通过；已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 通过。
- 后续事项 / 风险：若使用 H5 方式联调仍出现跨域，需要再把当前前端来源域名补入 CORS 白名单；小程序真机/开发者工具模式通常不受浏览器 CORS 限制。


- 变更范围 / 目的：新增“小程序主题”后台配置能力，保留现有“新中式绿意”主题，并增加“极简纯白风”主题；同时让小程序按后台配置动态切换主题。
- 关键文件：`backend/src/FaJuan.Api/Infrastructure/MiniApp/MiniAppThemeSettingsService.cs`、`backend/src/FaJuan.Api/Controllers/AdminMiniAppThemeController.cs`、`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`frontend/src/views/miniapp-setting/MiniAppSettingView.vue`、`frontend/src/router/index.ts`、`miniapp/coupon-miniapp/App.vue`、`miniapp/coupon-miniapp/store/session.js`、`miniapp/coupon-miniapp/composables/use-theme.js`、`Docs/sql/2026-04-11-小程序主题配置-菜单权限初始化.sql`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 通过；已执行 `npm run build`（`frontend/`）通过；已执行 `node --check` 校验 `miniapp/coupon-miniapp/store/session.js`、`miniapp/coupon-miniapp/api/home.js`、`miniapp/coupon-miniapp/api/miniapp.js`、`miniapp/coupon-miniapp/composables/use-theme.js` 通过。
- 后续事项 / 风险：数据库无需新增表，但后台菜单与权限需要执行 `Docs/sql/2026-04-11-小程序主题配置-菜单权限初始化.sql` 后，非 fallback 管理员账号才会看到入口；小程序其余页面已接入主题 class，仍建议在 HBuilder X / 微信开发者工具里实机检查绿意与纯白两套主题的完整视觉细节。


- 变更范围 / 目的：修复后台前端 `角色管理`、`账号管理` 页面中文文案乱码问题，恢复筛选区、表格头、弹窗、按钮、提示文案的正常中文显示。
- 关键文件：`frontend/src/views/admin-role/AdminRoleView.vue`、`frontend/src/views/admin-user/AdminUserView.vue`
- 验证情况：已执行 `npm run build`（`frontend/`）通过；已复扫 `frontend/src`，未发现残留的 `????` 显性乱码文本。
- 后续事项 / 风险：浏览器可能缓存旧版脚本，如页面仍显示乱码，请先强刷 `Ctrl+F5` 或关闭后重开前端 dev 服务再检查其它后台页面。


- 变更范围 / 目的：排查后台 `403 Forbidden`，确认根因是数据库缺少 `/admin-menus` 菜单记录且 `super_admin` 角色未绑定该菜单；同时补齐 `/miniapp-settings` 菜单与主题权限绑定。
- 关键文件 / 数据：`backend/src/FaJuan.Api/Infrastructure/Auth/AdminMenuAuthorizeFilter.cs`，以及数据库表 `AdminMenu`、`AdminRoleMenu`、`AdminPermission`、`AdminRolePermission`
- 验证情况：已直连 `FaJuanDb` 查询确认 `super_admin` 角色现已包含 `/admin-menus` 与 `/miniapp-settings` 菜单绑定。
- 后续事项 / 风险：需要重启后端 API 并重新登录后台，前端本地缓存的 `menuPaths` 才会刷新；否则仍可能看到旧权限结果。


- 变更范围 / 目的：修复数据库中的后台菜单中文乱码（权限管理、角色管理、核销中心），并继续扩大小程序“极简纯白风”覆盖范围到商城、我的券包、个人中心、订单列表与详情关键区域。
- 关键文件 / 数据：`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/pages/coupon/detail.vue`、`miniapp/coupon-miniapp/pages/coupon-pack/detail.vue`、`miniapp/coupon-miniapp/pages/mall/index.vue`、`miniapp/coupon-miniapp/pages/coupon/index.vue`、`miniapp/coupon-miniapp/pages/profile/index.vue`、`miniapp/coupon-miniapp/pages/order/list.vue`；数据库表 `AdminMenu`
- 验证情况：已执行 `node --check` 校验小程序主题相关脚本通过；已直连数据库确认 `/admin-users`、`/admin-roles`、`/writeoff`、`/miniapp-settings`、`/admin-menus` 菜单名均为正确中文。
- 后续事项 / 风险：小程序主题样式变更需要在 HBuilder X / 微信开发者工具中重新编译后才能看到；若仍显示旧绿色，多半是编译缓存未清理，而不是后台配置未保存。


- 变更范围 / 目的：修复小程序接口在 SQL Server 2008 R2 下仍生成 `OFFSET/FETCH` 的兼容问题，改掉 `MiniAppController` 中残留的 `Skip/Take` 分页写法。
- 关键文件：`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 通过；已复扫 `backend/src/FaJuan.Api`，数据库查询场景不再残留 LINQ `Skip(...)` 分页写法。
- 后续事项 / 风险：后端运行进程需要重启后新代码才会生效；若再出现 SQL Server 2008 R2 兼容报错，优先检查是否新增了直接 `Skip/Take`、新语法索引或其他高版本 SQL 生成。


- 变更范围 / 目的：完成一轮 `SQL Server 2008 R2` 兼容巡检，复扫控制器分页、模型类型与迁移语法，并输出专项审计文档。
- 关键文件：`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`backend/src/FaJuan.Api/Application/Common/QueryablePagingExtensions.cs`、`Docs/api/2026-04-11-SQLServer2008R2-兼容巡检.md`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 通过；已确认业务数据库分页查询不再残留直接 `Skip/Take`。
- 后续事项 / 风险：后续任何新增分页接口或 EF Migration 都必须继续按 2008 R2 规则手审，尤其防止再次引入 `OFFSET/FETCH`。
