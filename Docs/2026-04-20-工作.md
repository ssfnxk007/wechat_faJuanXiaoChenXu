# 2026-04-20 工作

- 变更范围 / 目的：启动后台管理端 UI 重构设计流程，明确本轮仅调整 UI 表达与信息架构，不修改业务逻辑、接口契约与数据库结构；采用 `backend-admin-ui` 技能基线推进，并引入可视化 companion 做方案确认。
- 关键文件：`frontend/src/layouts/AdminLayout.vue`、`frontend/src/router/index.ts`、`frontend/src/style.css`、`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/user/UserView.vue`、`frontend/src/views/share-tracking/ShareTrackingView.vue`
- 当前结论：用户已确认本轮按“中改版”执行，但允许重排后台信息架构；后续设计与实现需坚持“只改 UI，不改逻辑”边界。
- 后续事项 / 风险：后台现状存在较多展示型 hero/stats 结构，和业务后台高频操作场景不匹配；重构时需避免把视觉调整误伤现有交互路径、权限入口和接口入参。
- 变更范围 / 目的：按“混合首页 + 骨架重排 + 关键页优先”路线落第一批后台 UI 改造，先统一工作台骨架与首页，再补核销中心的执行型工作台样式；严格保持现有路由、权限落点与业务逻辑不变。
- 关键文件：`frontend/src/layouts/AdminLayout.vue`、`frontend/src/views/dashboard/DashboardView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`、`frontend/src/style.css`
- 验证情况：已执行 `frontend: npm run build` 通过；`AdminLayout.vue`、`DashboardView.vue`、`WriteOffView.vue` 的 TypeScript 诊断为 0 error。
- 后续事项 / 风险：券模板/订单页已基本具备新版基线，但券包、用户、分享追踪等页面仍存在局部展示型结构；下一轮可继续把筛选区、摘要区、结果区收口成统一后台工作台样式。
- 变更范围 / 目的：继续推进后台 UI 第二批改造，把 `券包管理`、`用户管理`、`分享追踪` 三页收口到统一工作台结构，进一步减少展示型布局，强化筛选、结果区与业务说明边界。
- 关键文件：`frontend/src/views/coupon-pack/CouponPackView.vue`、`frontend/src/views/user/UserView.vue`、`frontend/src/views/share-tracking/ShareTrackingView.vue`
- 验证情况：已执行 `frontend: npm run build` 通过；`CouponPackView.vue`、`UserView.vue`、`ShareTrackingView.vue` 的 TypeScript 诊断为 0 error。
- 后续事项 / 风险：订单、券模板、核销中心已进入统一基线；仍可继续把 `券包明细`、`用户券`、`门店/商品` 等页的局部组件细节继续收口，避免同站内出现过多不同密度的表单/表格样式。
- 变更范围 / 目的：继续推进后台 UI 第三批改造，把 `用户券`、`券包明细`、`门店管理`、`商品管理` 收口到统一工作台基线；其中门店页完成从旧版页头式布局向工作台结构的切换，其余页面补齐风险提示、维护要点与规则卡片。
- 关键文件：`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/coupon-pack/CouponPackItemView.vue`、`frontend/src/views/store/StoreView.vue`、`frontend/src/views/product/ProductView.vue`
- 验证情况：已执行 `frontend: npm run build` 通过；上述四个 Vue 文件的 TypeScript 诊断均为 0 error。
- 后续事项 / 风险：后台主要业务页已基本进入统一基线；后续若继续深挖，可优先收口弹窗内部字段密度、表格操作按钮层级，以及跨页面的素材选择器/远程选择器视觉一致性。
- 变更范围 / 目的：继续推进后台 UI 第四批改造，把 `账号管理`、`角色管理`、`菜单管理`、`小程序主题` 收口到统一工作台基线；其中账号/角色/菜单三页从旧版页头式布局切换为统一的 hero + stats + workbench 结构，主题页补齐工作台说明与维护要点。
- 关键文件：`frontend/src/views/admin-user/AdminUserView.vue`、`frontend/src/views/admin-role/AdminRoleView.vue`、`frontend/src/views/admin-menu/AdminMenuView.vue`、`frontend/src/views/miniapp-setting/MiniAppSettingView.vue`
- 验证情况：已执行 `frontend: npm run build` 通过；上述四个 Vue 文件的 TypeScript 诊断均为 0 error。
- 后续事项 / 风险：后台主要页面已全部进入统一工作台风格；后续如果继续收口，可优先统一多选框面板、远程选择器、弹窗头部与表单字段垂直节奏，减少系统管理页和业务页之间的微观样式差异。
- 变更范围 / 目的：继续做后台 UI 第五步收口，针对前几批已经统一的页面做公共样式去重与规范化，把 `hero-side-grid`、`quick-card.compact`、`quick-card-label`、`field-card`、`field-label`、`summary-field`、`guide-item` 等重复工作台样式提升到 `frontend/src/style.css`，并删除多页内重复 scoped CSS。
- 关键文件：`frontend/src/style.css`、`frontend/src/views/admin-user/AdminUserView.vue`、`frontend/src/views/admin-role/AdminRoleView.vue`、`frontend/src/views/admin-menu/AdminMenuView.vue`、`frontend/src/views/miniapp-setting/MiniAppSettingView.vue`、`frontend/src/views/store/StoreView.vue`、`frontend/src/views/user/UserView.vue`、`frontend/src/views/share-tracking/ShareTrackingView.vue`、`frontend/src/views/coupon-pack/CouponPackView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`
- 验证情况：已执行 `frontend: npm run build` 通过；抽检 `AdminUserView.vue`、`AdminRoleView.vue`、`AdminMenuView.vue`、`StoreView.vue`、`UserView.vue`、`WriteOffView.vue` 的 TypeScript 诊断均为 0 error。
- 后续事项 / 风险：当前已把一批高频重复的工作台样式提升到全局，但像 `RemoteSelectField` 包裹态、素材选择器卡片、复杂弹窗内部字段分组等仍存在页面级重复；下一步若继续收口，可进一步把这些组件级模式抽成共享样式或轻量封装组件。
- 变更范围 / 目的：继续做后台 UI 第六步收口，针对远程选择器 / 素材选择器 / 弹窗分组等重复模式进一步提取公共样式；把 `dialog-form > label`、`selector-field-card`、`selector-field-head`、`search-inline`、`selection-grid/card`、`media-grid/card/image`、`helper-card`、`result-board` 等模式提升到 `frontend/src/style.css`，并删除 `券模板`、`商品管理`、`用户券` 等页内重复 scoped CSS。
- 关键文件：`frontend/src/style.css`、`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/product/ProductView.vue`、`frontend/src/views/user-coupon/UserCouponView.vue`
- 验证情况：已执行 `frontend: npm run build` 通过；`CouponTemplateView.vue`、`ProductView.vue`、`UserCouponView.vue` 的 TypeScript 诊断均为 0 error。
- 后续事项 / 风险：当前工作台公共样式已覆盖大部分高频布局模式；剩余重复主要集中在个别页面的特色组件（如素材选中预览、复杂结果面板、媒体库紧凑布局）与响应式断点组合，后续若继续收口，可进一步评估是否抽成轻量通用组件。
- 变更范围 / 目的：继续做后台 UI 第七步收口，补充把 `action-entry-card / entry-head / entry-metrics / result-panel` 等工作台信息卡模式统一到 `frontend/src/style.css`，并继续减少 `商品管理`、`用户券`、`订单管理` 页中的重复 scoped CSS；同时保持前一轮提取的选择器 / 媒体库 / 弹窗分组样式可继续复用。
- 关键文件：`frontend/src/style.css`、`frontend/src/views/product/ProductView.vue`、`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/order/CouponOrderView.vue`
- 验证情况：已执行 `frontend: npm run build` 通过；`ProductView.vue`、`UserCouponView.vue`、`CouponOrderView.vue` 的 TypeScript 诊断均为 0 error。
- 后续事项 / 风险：当前全站后台工作台样式已高度收口；剩余重复更多是页面语义层差异和个别特色块（如素材已选预览布局、复杂详情网格列数）而不是基础视觉模式，继续抽取时需警惕为了去重而牺牲页面可读性。
- 变更范围 / 目的：根据订单页实际截图反馈，修正筛选区过高的问题；补回 `filter-panel-grid` 的全局 grid 能力，并把 `订单管理` 的筛选区压缩为桌面端 1~2 排布局，避免每个字段独占整行。
- 关键文件：`frontend/src/style.css`、`frontend/src/views/order/CouponOrderView.vue`
- 验证情况：已执行 `frontend: npm run build` 通过；`CouponOrderView.vue` 的 TypeScript 诊断为 0 error。
- 后续事项 / 风险：`filter-panel-grid` 现已恢复全局 grid 行为，其他使用该类名的页面会重新按各自局部列数规则生效；如果后续还发现筛选区过高，应优先检查是否是局部列数/摘要卡跨列设置过宽，而不是再次移除 grid 基础样式。
