# 2026-05-06 工作

- 变更范围 / 目的：按服务器免装 .NET 运行时的目标，生成后端 API Windows x64 自包含单文件 EXE 发布包，便于直接上传服务器运行。
- 关键文件：`backend/src/FaJuan.Api/appsettings.Production.json.example`
- 验证情况：已执行 `dotnet publish backend/src/FaJuan.Api/FaJuan.Api.csproj -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true /p:IncludeNativeLibrariesForSelfExtract=true -o release/2026-05-06/backend-api-singlefile`，产物包含 `FaJuan.Api.exe`。
- 后续事项 / 风险：虽然主程序是单文件 EXE，但配置文件 `appsettings*.json`、静态资源 `wwwroot` 仍需一并部署；正式环境仍需补齐数据库、小程序、支付密钥等配置。
