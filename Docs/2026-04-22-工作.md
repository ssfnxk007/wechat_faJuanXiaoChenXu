# 2026-04-22 工作

- 变更范围 / 目的：新增 ERP 专用券预检与核销 API，支持多门店按 `siteCode` 调用、统一 `X-Api-Key` 鉴权，并把商品券的商品选择结果落到核销记录。
- 关键文件：`backend/src/FaJuan.Api/Controllers/ErpCouponsController.cs`、`backend/src/FaJuan.Api/Application/Erp/ErpCouponService.cs`、`backend/src/FaJuan.Api/Infrastructure/Auth/ErpApiKeyAuthorizeFilter.cs`、`backend/src/FaJuan.Api/Contracts/ErpCouponDtos.cs`、`backend/src/FaJuan.Api/Domain/Entities/CouponWriteOffRecord.cs`、`Docs/api/2026-04-22-ERP专用核销API说明.md`
- 验证情况：已执行 `dotnet build backend/src/FaJuan.Api/FaJuan.Api.csproj -c Release -m:1` 通过；已生成 EF Core 迁移 `AddCouponWriteOffProductSelection`。
- 后续事项 / 风险：当前 ERP 接口未实现幂等请求号与 ERP 单据号落库，如后续需要对账追溯，建议增加 `requestNo / erpOrderNo / erpReceiptNo` 字段与唯一约束。
