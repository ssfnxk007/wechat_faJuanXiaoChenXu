# 2026-04-12 工作


- 变更范围 / 目的：根据代码审核结果修复旧版兼容分页的排序缺陷，并清理后台登录/账号接口中的乱码文案。
- 关键文件：`backend/src/FaJuan.Api/Application/Common/QueryablePagingExtensions.cs`、`backend/src/FaJuan.Api/Controllers/BannersController.cs`、`backend/src/FaJuan.Api/Controllers/MediaAssetsController.cs`、`backend/src/FaJuan.Api/Controllers/AdminAuthController.cs`、`backend/src/FaJuan.Api/Controllers/AdminUsersController.cs`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 通过。
- 后续事项 / 风险：仍建议继续清理其它后台控制器中的 `????` 中文文案，尤其 `AdminRolesController`、`AdminMenusController`、`BannersController`、`MediaAssetsController` 等。

- 变更范围 / 目的：补充轮播图管理后台菜单与按钮权限初始化 SQL，确保后续独立轮播图页面上线时可直接执行基础数据脚本，无需改动数据库结构。
- 关键文件：`Docs/sql/2026-04-12-轮播图管理-菜单权限初始化.sql`、`Docs/2026-04-12-工作.md`、`Docs/Error.md`
- 验证情况：已比对现有 `AdminMenu` / `AdminPermission` 初始化脚本模式，以及 `backend/src/FaJuan.Api/Controllers/BannersController.cs` 当前鉴权配置；新脚本采用幂等写法、兼容 `SQL Server 2008 R2`，仅写入菜单、权限与角色绑定数据。
- 后续事项 / 风险：当前后端 `BannersController` 运行时仍复用 `/products` 菜单路径和 `product.create` / `product.edit` / `product.delete` 权限码；若要真正切换到独立“轮播图管理”权限，还需后续代码同步到 `/banners` 与 `banner.*`。

- 变更范围 / 目的：补齐轮播图管理的前后端落地，支持后台列表、分页、弹窗新增编辑、图片上传、素材选择、链接跳转，并将独立菜单权限 SQL 实际执行到数据库。
- 关键文件：`backend/src/FaJuan.Api/Controllers/BannersController.cs`、`frontend/src/types/banner.ts`、`frontend/src/api/banner.ts`、`frontend/src/views/banner/BannerView.vue`、`frontend/src/router/index.ts`、`frontend/src/layouts/AdminLayout.vue`、`Docs/sql/2026-04-12-轮播图管理-菜单权限初始化.sql`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1`、`npm run build`，并执行 `sqlcmd -S 127.0.0.1 -U sa -P yhdtpass -d FaJuanDb -i "Docs/sql/2026-04-12-轮播图管理-菜单权限初始化.sql"`。
- 后续事项 / 风险：当前项目其他旧页面与文档仍有历史乱码；轮播图跳转当前为自由链接字段，后续若要强约束到“小程序页面 / 商品 / 券包”选择器，可再补结构化跳转配置。

- 变更范围 / 目的：对实施计划、最近工作记录、验收清单和接口说明做一次进度盘点，校正计划文档中已经过时的未勾选项，并补齐“上线前待办清单”和“逐项勾选剩余工作表”两份跟踪文档。
- 关键文件：`Docs/plans/2026-04-08-发卷小程序实施计划.md`、`Docs/plans/2026-04-12-上线前待办清单.md`、`Docs/plans/2026-04-12-剩余工作勾选清单.md`、`Docs/2026-04-12-工作.md`
- 验证情况：已交叉核对 `Docs/2026-04-10-工作.md`、`Docs/2026-04-11-工作.md`、`Docs/2026-04-12-工作.md`、`Docs/验收/2026-04-10-发券联调验收清单.md`、`Docs/api/2026-04-09-微信支付接入说明.md`，并抽查 `miniapp/coupon-miniapp/api/auth.js`、`miniapp/coupon-miniapp/pages/coupon-pack/detail.vue`，确认小程序工程、真实登录骨架与支付拉起代码已存在。
- 后续事项 / 风险：旧计划文档中与“真实联调/真实环境”相关事项仍需继续区分“代码已落地”和“环境已验收”；当前真正剩余重点已经收敛到发券联调验收、真实微信支付联调、小程序真机验收和安全/环境收尾四大块。

- 变更范围 / 目的：把“剩余工作勾选清单”继续拆成可执行的本周版本，明确 `2026-04-12 ~ 2026-04-18` 的执行顺序、建议负责人、验证方式和每阶段交付物，便于后续按天推进与勾选。
- 关键文件：`Docs/plans/2026-04-12-本周执行版.md`、`Docs/2026-04-12-工作.md`
- 验证情况：已基于 `Docs/plans/2026-04-12-剩余工作勾选清单.md` 重新编排为 `A~G` 七段执行结构，并把发券验收、真实微信支付、真机联调、安全收尾拆到具体子项；排期日期使用绝对日期，避免“本周/明天”类相对时间歧义。
- 后续事项 / 风险：该文档当前属于“建议执行版”，后续若实际参与人、环境准备、支付参数到位时间有变化，需及时调整每日排期，但优先级顺序不建议改变。


- 变更范围 / 目的：打通小程序首页轮播图真实跳转链路，并对齐首页接口返回的轮播排序与图片地址。
- 关键文件：`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/pages/webview/index.vue`、`miniapp/coupon-miniapp/pages.json`、`miniapp/coupon-miniapp/api/home.js`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 与 `npm run build`（frontend）通过；小程序侧已完成代码级联调，待 HBuilder X / 微信开发者工具实点验证站内页、TabBar 页、H5 三类跳转。
- 后续事项 / 风险：H5 外链跳转依赖微信小程序后台配置业务域名；若未配置，`pages/webview/index` 页面可打开但目标站点不会被微信放行。


- 变更范围 / 目的：将轮播图跳转方案改为结构化内部跳转，后台不再手填 linkUrl，只允许选择券模板、券包、商品入口或活动页，并由系统自动生成小程序内部路由。
- 关键文件：`frontend/src/views/banner/BannerView.vue`、`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/pages.json`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 与 `npm run build`（frontend）通过；小程序首页轮播点击逻辑已收口为“仅支持内部页面跳转”。
- 后续事项 / 风险：商品类轮播当前生成 `/pages/mall/index?productId={id}`，因为小程序暂未有独立商品详情页；后续若新增商品详情页，只需调整后台生成规则即可无缝切换。


- 变更范围 / 目的：补齐小程序商品详情页与商品详情接口，让首页推荐商品、商城商品卡片以及轮播图中的“商品”都能跳到真实商品详情，不再只落到商城入口。
- 关键文件：`backend/src/FaJuan.Api/Contracts/MiniAppDtos.cs`、`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`miniapp/coupon-miniapp/pages/product/detail.vue`、`miniapp/coupon-miniapp/api/miniapp.js`、`miniapp/coupon-miniapp/pages.json`、`frontend/src/views/banner/BannerView.vue`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 与 `npm run build`（frontend）通过。
- 后续事项 / 风险：小程序商品详情页当前已对接真实后端详情接口，详情图会按后台商品详情图配置展示；若后续要补更丰富的商品卖点、库存、门店可售等信息，需要继续扩展 miniapp 商品详情 DTO。


- 变更范围 / 目的：继续完善小程序商品详情页，增加当前商品可用优惠券区块，并复用真实后端商品详情接口返回关联券数据，支持从商品页直接跳券详情或去领券中心。
- 关键文件：`backend/src/FaJuan.Api/Contracts/MiniAppDtos.cs`、`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`miniapp/coupon-miniapp/api/mall.js`、`miniapp/coupon-miniapp/pages/product/detail.vue`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 与 `npm run build`（frontend）通过；小程序侧待在 HBuilder X / 微信开发者工具中手动查看商品详情页展示与跳券详情链路。
- 后续事项 / 风险：当前商品可用券展示基于指定商品券关联表和已启用券模板；若后续还要把无门槛券/满减券按“推荐可用”一起展示，需要再补一层推荐规则。


- 变更范围 / 目的：继续增强商品详情页的券展示逻辑，在指定商品券之外增加通用推荐券（无门槛券 / 满减券等）展示，形成“商品专享券 + 推荐券”双层结构。
- 关键文件：`backend/src/FaJuan.Api/Contracts/MiniAppDtos.cs`、`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`、`miniapp/coupon-miniapp/api/mall.js`、`miniapp/coupon-miniapp/pages/product/detail.vue`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 与 `npm run build`（frontend）通过。
- 后续事项 / 风险：当前推荐券为通用推荐逻辑，按已启用且非指定商品券、非新人专享券的模板返回；若后续要按活动、门店、渠道做更精细推荐，需要继续补推荐策略。


- 变更范围 / 目的：补齐小程序真实活动页，并把首页/轮播默认活动入口切换到活动页；同时重写商品详情页结构与文案，形成“商品说明 + 亮点 + 商品专享券 + 推荐券 + CTA”完整链路。
- 关键文件：`miniapp/coupon-miniapp/pages/activity/detail.vue`、`miniapp/coupon-miniapp/pages/product/detail.vue`、`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/api/home.js`、`miniapp/coupon-miniapp/pages.json`、`frontend/src/views/banner/BannerView.vue`
- 验证情况：已执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 与 `npm run build`（frontend）通过。
- 后续事项 / 风险：PowerShell 终端里仍可能把小程序页面中文显示成乱码，但本轮文件写入已按 UTF-8 落盘；最终请以 HBuilder X / 微信开发者工具中的页面显示效果为准。


- 变更范围 / 目的：对“小程序活动页 + 商品详情页 + 首页轮播活动入口”做一次收口复核，确认后台轮播结构化内部跳转、真实活动页、真实商品详情接口三条链路已对齐，并补一轮构建级验收。
- 关键文件：`miniapp/coupon-miniapp/pages/activity/detail.vue`、`miniapp/coupon-miniapp/pages/product/detail.vue`、`miniapp/coupon-miniapp/pages/index/index.vue`、`miniapp/coupon-miniapp/api/home.js`、`miniapp/coupon-miniapp/api/mall.js`、`frontend/src/views/banner/BannerView.vue`、`backend/src/FaJuan.Api/Controllers/MiniAppController.cs`
- 验证情况：已复核首页默认轮播跳转为 `/pages/activity/detail?key=newcomer|free|writeoff`，后台轮播“商品”跳转为 `/pages/product/detail?id={id}`；并再次执行 `dotnet build "backend/src/FaJuan.Api/FaJuan.Api.csproj" -c Release -m:1` 与 `npm run build`（frontend）通过。
- 后续事项 / 风险：小程序端目前缺少命令行统一构建与真机自动化验收，下一步仍需在 `HBuilder X` / 微信开发者工具中重点实点 4 条链路：轮播跳活动页、活动页 CTA、商品详情打开券详情、商品/券包图片与详情图展示。
