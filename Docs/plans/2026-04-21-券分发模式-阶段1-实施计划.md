# 券分发模式（阶段 1）· 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 `CouponTemplate` 加 `DistributionMode`（FreeClaim/PaidStandalone/PackOnly）和 `SalePrice` 字段；后台可录入；小程序公开展示入口只展示 `FreeClaim` 的券。**不**动订单模型 / 下单流程 / mall / 核销（那些属于阶段 2）。

**Architecture:** 新增一个枚举 + 两个字段 + 一个 EF 迁移；在 `CouponTemplatesController.ValidateRequest` 追加校验；3 处 miniapp 公开查询加 `DistributionMode == FreeClaim` 过滤；前端表单按模式条件显示售价字段。

**Tech Stack:** .NET 8 / EF Core 8 / SQL Server / xUnit + InMemory / Vue 3 + TypeScript

**设计文档：** `Docs/specs/2026-04-21-券分发模式-阶段1设计.md`

**Test Strategy（务实版）：**
- **后端**：`CouponTemplatesControllerTests` 新建，TDD 覆盖 `DistributionMode + SalePrice` 的组合校验。已有的校验行为不回测（非本 PR 职责）。
- **小程序过滤**：不写 unit test，靠 Task 7 的手测验证（项目目前无 MiniAppControllerTests 骨架，新搭一个测试基座成本超过价值）。

---

## File Structure

### 新建
| 文件 | 职责 |
|---|---|
| `backend/src/FaJuan.Api/Domain/Enums/CouponDistributionMode.cs` | 新枚举定义 |
| `backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/<ts>_AddCouponDistributionMode.cs` | EF 迁移 |
| `backend/tests/FaJuan.Api.Tests/CouponTemplatesControllerTests.cs` | 后台校验单元测试 |

### 修改
| 文件 | 改动概要 |
|---|---|
| `backend/src/FaJuan.Api/Domain/Entities/CouponTemplate.cs` | 加 2 个属性 |
| `backend/src/FaJuan.Api/Infrastructure/Persistence/AppDbContext.cs` | `CouponTemplate` 配置块加新字段映射 |
| `backend/src/FaJuan.Api/Contracts/CouponTemplateDtos.cs` | `CouponTemplateListItemDto` + `SaveCouponTemplateRequest` 加 2 个字段 |
| `backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs` | List/Create/Update 写入回传新字段；ValidateRequest 加校验；Create/Update 对非 PaidStandalone 时强制置空 SalePrice |
| `backend/src/FaJuan.Api/Controllers/MiniAppController.cs` | L106 / L275 / L302 三个查询加 `DistributionMode == FreeClaim` 过滤 |
| `frontend/src/types/coupon.ts` | 两个 interface 加字段 |
| `frontend/src/views/coupon-template/CouponTemplateView.vue` | 表格加列；表单加 select+input；条件显示 + 本地清空逻辑 |

---

## Task 1：新枚举 + 实体字段

**Files:**
- Create: `backend/src/FaJuan.Api/Domain/Enums/CouponDistributionMode.cs`
- Modify: `backend/src/FaJuan.Api/Domain/Entities/CouponTemplate.cs`
- Modify: `backend/src/FaJuan.Api/Infrastructure/Persistence/AppDbContext.cs`

- [ ] **Step 1：创建枚举**

`backend/src/FaJuan.Api/Domain/Enums/CouponDistributionMode.cs`：

```csharp
namespace FaJuan.Api.Domain.Enums;

public enum CouponDistributionMode
{
    FreeClaim = 0,
    PaidStandalone = 1,
    PackOnly = 2,
}
```

- [ ] **Step 2：实体加字段**

`CouponTemplate.cs` 在 `IsEnabled` 之后插入：

```csharp
    public CouponDistributionMode DistributionMode { get; set; } = CouponDistributionMode.FreeClaim;
    public decimal? SalePrice { get; set; }
```

最终 `CouponTemplate` 结构示意（供对照，不要整体替换）：

```csharp
public bool IsEnabled { get; set; } = true;
public CouponDistributionMode DistributionMode { get; set; } = CouponDistributionMode.FreeClaim;
public decimal? SalePrice { get; set; }
public string? Remark { get; set; }
```

- [ ] **Step 3：DbContext 加列配置**

`AppDbContext.cs` 的 `modelBuilder.Entity<CouponTemplate>(...)` 块内（已有 `entity.Property(x => x.Remark).HasMaxLength(500);` 那段），追加：

```csharp
            entity.Property(x => x.SalePrice).HasColumnType("decimal(18,2)");
```

（`DistributionMode` 是 enum，EF Core 默认存成 int，不用显式配置。）

- [ ] **Step 4：编译**

```bash
cd /g/发卷小程序/backend && dotnet build src/FaJuan.Api/FaJuan.Api.csproj --nologo -v minimal
```

Expected：`Build succeeded. 0 Warning(s). 0 Error(s).`

- [ ] **Step 5：提交**

```bash
cd /g/发卷小程序 && git add \
  backend/src/FaJuan.Api/Domain/Enums/CouponDistributionMode.cs \
  backend/src/FaJuan.Api/Domain/Entities/CouponTemplate.cs \
  backend/src/FaJuan.Api/Infrastructure/Persistence/AppDbContext.cs
git commit -m "feat(coupon): add DistributionMode enum and SalePrice field to CouponTemplate"
```

---

## Task 2：EF 迁移

**Files:**
- Create: `backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/<timestamp>_AddCouponDistributionMode.cs`

- [ ] **Step 1：生成迁移**

```bash
cd /g/发卷小程序/backend && dotnet ef migrations add AddCouponDistributionMode --project src/FaJuan.Api/FaJuan.Api.csproj
```

Expected：Migrations 目录下出现 `<timestamp>_AddCouponDistributionMode.cs` + `.Designer.cs`，`AppDbContextModelSnapshot.cs` 更新。

- [ ] **Step 2：验证迁移内容**

打开生成的 `<timestamp>_AddCouponDistributionMode.cs`。`Up()` 应该**只**包含这两行操作：

```csharp
migrationBuilder.AddColumn<int>(
    name: "DistributionMode",
    table: "CouponTemplate",
    type: "int",
    nullable: false,
    defaultValue: 0);

migrationBuilder.AddColumn<decimal>(
    name: "SalePrice",
    table: "CouponTemplate",
    type: "decimal(18,2)",
    nullable: true);
```

如果 `Up()` 里多出任何 **其他表** 的改动（说明 worktree 有未提交实体改动被裹挟进来），停下来向用户报告，**不要**提交。

- [ ] **Step 3：确认无其他表被改动**

用 `grep` 扫一遍：

```bash
grep -E "table: \"(?!CouponTemplate\")" backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/*_AddCouponDistributionMode.cs
```

（预期无输出 = 干净的迁移）

- [ ] **Step 4：提交**

```bash
git add backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/
git commit -m "feat(coupon): EF migration for DistributionMode and SalePrice"
```

---

## Task 3：DTO 扩展

**Files:**
- Modify: `backend/src/FaJuan.Api/Contracts/CouponTemplateDtos.cs`

- [ ] **Step 1：`CouponTemplateListItemDto` 加字段**

在 `Remark` 之后（现有的 `public IReadOnlyCollection<long> ProductIds` 之前）插入：

```csharp
    public CouponDistributionMode DistributionMode { get; init; }
    public decimal? SalePrice { get; init; }
```

- [ ] **Step 2：`SaveCouponTemplateRequest` 加字段**

同样位置插入：

```csharp
    public CouponDistributionMode DistributionMode { get; init; } = CouponDistributionMode.FreeClaim;
    public decimal? SalePrice { get; init; }
```

- [ ] **Step 3：编译**

```bash
cd /g/发卷小程序/backend && dotnet build src/FaJuan.Api/FaJuan.Api.csproj --nologo -v minimal
```

Expected：`Build succeeded`。

- [ ] **Step 4：提交**

```bash
git add backend/src/FaJuan.Api/Contracts/CouponTemplateDtos.cs
git commit -m "feat(coupon): extend CouponTemplate DTOs with DistributionMode and SalePrice"
```

---

## Task 4：后台校验 TDD（失败测试 + 控制器实现）

**Files:**
- Create: `backend/tests/FaJuan.Api.Tests/CouponTemplatesControllerTests.cs`
- Modify: `backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs`

- [ ] **Step 1：写测试（先写，预期编译失败）**

创建 `backend/tests/FaJuan.Api.Tests/CouponTemplatesControllerTests.cs`：

```csharp
using FaJuan.Api.Contracts;
using FaJuan.Api.Controllers;
using FaJuan.Api.Domain.Enums;
using FaJuan.Api.Infrastructure.Persistence;
using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;

namespace FaJuan.Api.Tests;

public class CouponTemplatesControllerTests
{
    [Fact]
    public async Task Create_Should_Reject_PaidStandalone_Without_SalePrice()
    {
        await using var db = CreateDbContext();
        var controller = new CouponTemplatesController(db);

        var result = await controller.Create(BuildRequest(mode: CouponDistributionMode.PaidStandalone, salePrice: null));

        AssertBadRequestContains(result, "售价");
    }

    [Fact]
    public async Task Create_Should_Reject_PaidStandalone_With_Zero_SalePrice()
    {
        await using var db = CreateDbContext();
        var controller = new CouponTemplatesController(db);

        var result = await controller.Create(BuildRequest(mode: CouponDistributionMode.PaidStandalone, salePrice: 0m));

        AssertBadRequestContains(result, "售价");
    }

    [Fact]
    public async Task Create_Should_Force_SalePrice_Null_For_FreeClaim()
    {
        await using var db = CreateDbContext();
        var controller = new CouponTemplatesController(db);

        var action = await controller.Create(BuildRequest(mode: CouponDistributionMode.FreeClaim, salePrice: 9.9m));

        var ok = Assert.IsType<OkObjectResult>(action.Result);
        var body = Assert.IsType<ApiResponse<long>>(ok.Value);
        var saved = db.CouponTemplates.AsNoTracking().Single(x => x.Id == body.Data);
        Assert.Null(saved.SalePrice);
        Assert.Equal(CouponDistributionMode.FreeClaim, saved.DistributionMode);
    }

    [Fact]
    public async Task Create_Should_Force_SalePrice_Null_For_PackOnly()
    {
        await using var db = CreateDbContext();
        var controller = new CouponTemplatesController(db);

        var action = await controller.Create(BuildRequest(mode: CouponDistributionMode.PackOnly, salePrice: 20m));

        var ok = Assert.IsType<OkObjectResult>(action.Result);
        var body = Assert.IsType<ApiResponse<long>>(ok.Value);
        var saved = db.CouponTemplates.AsNoTracking().Single(x => x.Id == body.Data);
        Assert.Null(saved.SalePrice);
        Assert.Equal(CouponDistributionMode.PackOnly, saved.DistributionMode);
    }

    [Fact]
    public async Task Create_Should_Persist_SalePrice_For_PaidStandalone()
    {
        await using var db = CreateDbContext();
        var controller = new CouponTemplatesController(db);

        var action = await controller.Create(BuildRequest(mode: CouponDistributionMode.PaidStandalone, salePrice: 12.5m));

        var ok = Assert.IsType<OkObjectResult>(action.Result);
        var body = Assert.IsType<ApiResponse<long>>(ok.Value);
        var saved = db.CouponTemplates.AsNoTracking().Single(x => x.Id == body.Data);
        Assert.Equal(12.5m, saved.SalePrice);
        Assert.Equal(CouponDistributionMode.PaidStandalone, saved.DistributionMode);
    }

    private static AppDbContext CreateDbContext()
    {
        var options = new DbContextOptionsBuilder<AppDbContext>()
            .UseInMemoryDatabase(Guid.NewGuid().ToString("N"))
            .Options;
        return new AppDbContext(options);
    }

    private static SaveCouponTemplateRequest BuildRequest(CouponDistributionMode mode, decimal? salePrice)
    {
        return new SaveCouponTemplateRequest
        {
            Name = "TestCoupon",
            TemplateType = CouponTemplateType.NoThreshold,
            ValidPeriodType = CouponValidPeriodType.AfterReceiveDays,
            ValidDays = 7,
            IsAllStores = true,
            IsEnabled = true,
            PerUserLimit = 1,
            DistributionMode = mode,
            SalePrice = salePrice,
        };
    }

    private static void AssertBadRequestContains(ActionResult<ApiResponse<long>> actionResult, string expectedKeyword)
    {
        var bad = Assert.IsType<BadRequestObjectResult>(actionResult.Result);
        var body = Assert.IsType<ApiResponse<long>>(bad.Value);
        Assert.False(body.Success);
        Assert.Contains(expectedKeyword, body.Message);
    }
}
```

- [ ] **Step 2：跑测试，验证失败**

```bash
cd /g/发卷小程序/backend && dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --filter "FullyQualifiedName~CouponTemplatesControllerTests" --nologo
```

Expected：5 个测试中至少 2 个 FAIL（现在的 `Create` 不校验 SalePrice，也不写入 DistributionMode/SalePrice），其他 3 个可能 PASS 或 FAIL，取决于当前代码。整体**不全通过**。

- [ ] **Step 3：改 `CouponTemplatesController.ValidateRequest` 加校验**

`CouponTemplatesController.cs` 的 `ValidateRequest` 方法结尾（`return null;` 之前）插入：

```csharp
        if (request.DistributionMode == CouponDistributionMode.PaidStandalone
            && (!request.SalePrice.HasValue || request.SalePrice.Value <= 0m))
        {
            return "单张售卖券必须填写售价，且大于 0";
        }
```

- [ ] **Step 4：改 `Create` 写入新字段并容错**

`CouponTemplatesController.cs` 第 120-136 行构造 `new CouponTemplate { ... }` 段落的末尾（`Remark = request.Remark?.Trim()` 之后，`}` 之前）插入：

```csharp
            DistributionMode = request.DistributionMode,
            SalePrice = request.DistributionMode == CouponDistributionMode.PaidStandalone ? request.SalePrice : null,
```

- [ ] **Step 5：改 `Update` 写入新字段并容错**

`CouponTemplatesController.cs` 第 161-174 行 `entity.Name = request.Name.Trim(); ...` 段落中 `entity.Remark = request.Remark?.Trim();` 之后追加：

```csharp
        entity.DistributionMode = request.DistributionMode;
        entity.SalePrice = request.DistributionMode == CouponDistributionMode.PaidStandalone ? request.SalePrice : null;
```

- [ ] **Step 6：跑测试，验证全通过**

```bash
dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --filter "FullyQualifiedName~CouponTemplatesControllerTests" --nologo
```

Expected：`Passed! - Failed: 0, Passed: 5, Skipped: 0`

- [ ] **Step 7：提交**

```bash
cd /g/发卷小程序 && git add \
  backend/tests/FaJuan.Api.Tests/CouponTemplatesControllerTests.cs \
  backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs
git commit -m "feat(coupon): validate and persist DistributionMode/SalePrice in admin controller"
```

---

## Task 5：后台 List 回显 + 小程序端过滤

**Files:**
- Modify: `backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs`
- Modify: `backend/src/FaJuan.Api/Controllers/MiniAppController.cs`

- [ ] **Step 1：List 接口回显新字段**

`CouponTemplatesController.cs` 的 List 查询第一次 `.Select(x => new CouponTemplateListItemDto { ... })`（第 30-48 行附近），在 `IsEnabled = x.IsEnabled,` 之后加两行：

```csharp
                DistributionMode = x.DistributionMode,
                SalePrice = x.SalePrice,
```

然后第二次构造（第 77-98 行的 `items = items.Select(x => new CouponTemplateListItemDto { ... }).ToList();`），同样位置加：

```csharp
            DistributionMode = x.DistributionMode,
            SalePrice = x.SalePrice,
```

- [ ] **Step 2：小程序端 3 处加 FreeClaim 过滤**

打开 `MiniAppController.cs`，找到第 106 行附近的 `directCoupons` 查询：

```csharp
var directCoupons = await dbContext.CouponTemplates.AsNoTracking()
    .Where(x => x.IsEnabled)
    .OrderByDescending(x => x.IsNewUserOnly)
```

把 `.Where(x => x.IsEnabled)` 替换为：

```csharp
    .Where(x => x.IsEnabled && x.DistributionMode == CouponDistributionMode.FreeClaim)
```

第 275 行附近（商品详情"相关券"）：

```csharp
.Join(
    dbContext.CouponTemplates.AsNoTracking().Where(x => x.IsEnabled),
    scope => scope.CouponTemplateId,
```

把 `.Where(x => x.IsEnabled)` 替换为：

```csharp
    dbContext.CouponTemplates.AsNoTracking().Where(x => x.IsEnabled && x.DistributionMode == CouponDistributionMode.FreeClaim),
```

第 302 行附近（商品详情"推荐券"）：

```csharp
var recommendedCoupons = await dbContext.CouponTemplates.AsNoTracking()
    .Where(x => x.IsEnabled
        && !x.IsNewUserOnly
        && (int)x.TemplateType != 3
```

在 `x.IsEnabled` 之后追加条件：

```csharp
    .Where(x => x.IsEnabled
        && x.DistributionMode == CouponDistributionMode.FreeClaim
        && !x.IsNewUserOnly
        && (int)x.TemplateType != 3
```

- [ ] **Step 3：确认文件顶部有 `using FaJuan.Api.Domain.Enums;`**

MiniAppController.cs 第 7 行已有 `using FaJuan.Api.Domain.Enums;`，不用加。确认即可。

- [ ] **Step 4：编译 + 全量测试**

```bash
cd /g/发卷小程序/backend && dotnet build FaJuan.sln --nologo -v minimal && dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --nologo
```

Expected：Build succeeded，全部测试通过（20 + 5 = 25 个）。

- [ ] **Step 5：提交**

```bash
cd /g/发卷小程序 && git add \
  backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs \
  backend/src/FaJuan.Api/Controllers/MiniAppController.cs
git commit -m "feat(coupon): echo new fields in list and filter miniapp public queries by FreeClaim"
```

---

## Task 6：前端类型 + 表单 + 表格列

**Files:**
- Modify: `frontend/src/types/coupon.ts`
- Modify: `frontend/src/views/coupon-template/CouponTemplateView.vue`

- [ ] **Step 1：前端类型加字段**

`frontend/src/types/coupon.ts` 在 `CouponTemplateListItemDto` 的 `remark?: string` 之前（保持字段顺序接近后端 DTO）插入：

```ts
  distributionMode: number
  salePrice?: number
```

同样在 `SaveCouponTemplateRequest` 的 `remark?: string` 之前插入：

```ts
  distributionMode: number
  salePrice?: number
```

- [ ] **Step 2：在 Vue 页面 script setup 里加分发模式常量**

`frontend/src/views/coupon-template/CouponTemplateView.vue` 的 `<script setup>` 段落（开头 `import` 之后，其他常量附近）加入：

```ts
const DISTRIBUTION_MODE_OPTIONS = [
  { value: 0, label: '免费领取' },
  { value: 1, label: '单张售卖' },
  { value: 2, label: '仅组包' },
] as const

const distributionModeLabel = (mode: number | null | undefined): string => {
  const found = DISTRIBUTION_MODE_OPTIONS.find((x) => x.value === mode)
  return found ? found.label : '未知'
}
```

- [ ] **Step 3：在表单对象 `form` 里加初始值**

找到声明 `form` reactive 对象的位置（搜索 `isNewUserOnly`），在合适位置加两个字段：

```ts
  distributionMode: 0,
  salePrice: undefined,
```

以及任何 `resetForm()` 类函数里要一并补上对应清空逻辑（`distributionMode: 0, salePrice: undefined`）。

- [ ] **Step 4：表格新增"分发模式"列**

找到表格 `<table>` 的 `<thead>` 段落，在"启用"或类似列的前后合适位置加一个 `<th>分发模式</th>`；对应 `<tbody>` 的 `<tr>` 加：

```html
<td>
  <span class="tag" :class="`tag-mode-${row.distributionMode}`">{{ distributionModeLabel(row.distributionMode) }}</span>
  <span v-if="row.distributionMode === 1 && row.salePrice != null" class="price-badge">¥{{ row.salePrice }}</span>
</td>
```

（具体 DOM 结构按现有表格单元格的 class 风格微调。`tag` 可用现有通用 `badge` 样式，`price-badge` 可沿用页面内其他金额类样式；不新引入外部库。）

- [ ] **Step 5：编辑/创建表单加字段**

在表单 Dialog 内找到"基本信息"分组（邻近 `templateType` 下拉的位置），加入两块字段：

```html
<label class="field-card">
  <span class="field-label">分发模式</span>
  <select v-model.number="form.distributionMode" @change="onDistributionModeChange">
    <option v-for="opt in DISTRIBUTION_MODE_OPTIONS" :key="opt.value" :value="opt.value">{{ opt.label }}</option>
  </select>
</label>

<label v-if="form.distributionMode === 1" class="field-card">
  <span class="field-label">售价（元）<span class="required-hint">*</span></span>
  <input v-model.number="form.salePrice" type="number" step="0.01" min="0.01" placeholder="仅单张售卖模式填写" />
</label>
```

`onDistributionModeChange`：

```ts
const onDistributionModeChange = () => {
  if (form.distributionMode !== 1) {
    form.salePrice = undefined
  }
}
```

- [ ] **Step 6：提交前前端校验**

在现有 `submit()`（或创建 / 更新前的 handler）开头校验：

```ts
if (form.distributionMode === 1 && (!form.salePrice || form.salePrice <= 0)) {
  notify.error('单张售卖券必须填写大于 0 的售价')
  return
}
```

- [ ] **Step 7：前端编译**

```bash
cd /g/发卷小程序/frontend && npm run build
```

Expected：`built in <time>`，无 TS 错误。

- [ ] **Step 8：提交**

```bash
cd /g/发卷小程序 && git add \
  frontend/src/types/coupon.ts \
  frontend/src/views/coupon-template/CouponTemplateView.vue
git commit -m "feat(coupon): admin template form and table support DistributionMode and SalePrice"
```

---

## Task 7：端到端手测

- [ ] **Step 1：启动后端**

```bash
cd /g/发卷小程序/backend/src/FaJuan.Api && dotnet run
```

启动时应该看到 EF 自动应用 `AddCouponDistributionMode` 迁移，`CouponTemplate` 表多出 `DistributionMode` / `SalePrice` 两列。

- [ ] **Step 2：启动前端**

```bash
cd /g/发卷小程序/frontend && npm run dev
```

- [ ] **Step 3：登录后台 → 进入「券模板管理」**

- 表格出现"分发模式"列，旧数据都显示"免费领取"
- 点"新增券模板"：表单有"分发模式"下拉
- 选"免费领取" → "售价"字段不显示
- 选"单张售卖" → "售价"字段出现且必填
- 选"仅组包" → "售价"字段不显示

- [ ] **Step 4：负向校验**

- 建新券，分发模式选"单张售卖"，售价留空 → 应报"单张售卖券必须填写大于 0 的售价"
- 售价输入 `-5` → 同样报错
- 切换到"免费领取"再切回"单张售卖"时售价应被清空（不保留上次输入）

- [ ] **Step 5：小程序端过滤验证**

创建三张启用的券：
- A：DistributionMode=FreeClaim
- B：DistributionMode=PaidStandalone + SalePrice=9.9
- C：DistributionMode=PackOnly

打开小程序首页 → **只应看到 A 张券**在"免费领取"区。打开一个商品详情 → "相关券"/"推荐券"区域也只应出现 A（如果适用范围匹配）。

- [ ] **Step 6：回归**

- 用户已领的券（如果数据库里有测试数据）依然能在"我的券"页看到，不受过滤影响
- 已有券包里带 B 或 C 券的场景，买券包后用户还是能拿到 B 或 C 券（券包详情过滤没加）

测完把通过/失败情况反馈给用户。

---

## Task 8：合并到 main

- [ ] **Step 1：确认分支干净**

```bash
cd /g/发卷小程序 && git status --short
git log --oneline main..HEAD
```

应看到 6 个 commit（Task 1-6 每个 1 个 commit）。

- [ ] **Step 2：切 main + fast-forward 合并 + push**

```bash
git checkout main
git merge --ff-only feature/coupon-distribution-mode
git push origin main
```

- [ ] **Step 3：删本地 feature 分支**

```bash
git branch -d feature/coupon-distribution-mode
```

---

## Self-Review

**1. Spec 覆盖：**
- spec §4.1 新枚举 → Task 1 Step 1 ✓
- spec §4.2 实体字段 → Task 1 Step 2 ✓
- spec §4.3 迁移 → Task 2 ✓
- spec §4.4 应用级校验 → Task 4 ✓
- spec §5.1 Controller List/Create/Update + 校验 → Task 3 + 4 + 5 ✓
- spec §5.2 小程序 3 处过滤（券详情不过滤）→ Task 5 ✓
- spec §6 前端表格 + 表单 + 条件显示 + 本地清空 → Task 6 ✓
- spec §7 迁移默认值 → Task 2 的 `defaultValue: 0` ✓
- spec §8 测试策略 → Task 4 后端测试 + Task 7 手测（spec 里提到的 miniapp 测试在 plan 顶部明确改为手测）✓
- spec §10 阶段 2 预告 → 本 plan **不**实施，正确范围对齐 ✓

**2. Placeholder 扫描：** 无 "TBD" / "TODO" / "Similar to"。所有代码块都是完整可粘贴的。`tag-mode-X` / `price-badge` 等 class 是"可沿用项目现有风格"指引，不是占位符。

**3. 类型一致性：**
- `CouponDistributionMode` 枚举在 Task 1 定义 → Task 3 DTO 引用 → Task 4 测试引用 → Task 5 miniapp 过滤引用，同名
- `SalePrice` `decimal?` 字段贯穿实体 / DTO / 列定义 / 测试 / 前端 `number?`，一致
- 前端 `distributionMode: number` 对应后端枚举 int，一致
- Controller 的 `DistributionMode = request.DistributionMode` 在 Task 4 Step 4 定义了，Task 5 的 List Select 在 Task 5 Step 1 引用同名属性，一致

**4. 分支命名：** Task 8 使用 `feature/coupon-distribution-mode`。Task 1-6 的 commit 前需要先切分支（在 Task 1 之前人工执行 `git checkout -b feature/coupon-distribution-mode`）——这一步在 subagent-driven 模式下由 controller 在分发第一个 task 前处理，和上次支付工作的做法一致。
