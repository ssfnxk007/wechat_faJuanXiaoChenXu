# 微信支付参数入库与后台配置页 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把微信支付参数（AppId/商户号/证书序列号/私钥PEM/APIv3密钥/回调地址/模拟支付开关）从 `appsettings.json` 搬到 SQL Server，并在后台"小程序设置"页新增「支付参数」tab 让有权限管理员在线编辑。

**Architecture:** 新增单行 EF 实体 `WeChatPaySetting` + EF 迁移；新增 `WeChatPaySettingsProvider`（Scoped 服务 + 静态 30s 内存缓存）替换 `IOptions<WeChatPayOptions>`；`WeChatPayService` 把 `IsConfigured()` / `GetStatus()` / 签名读密钥全部改为从 provider 异步取值；新建 `AdminMiniAppPayController`（GET 掩码回显，PUT 留空保留）；前端把 `MiniAppSettingView.vue` 重构为双 tab；最后清理 `appsettings.json` 的 `WeChatPay` 节 + `WeChatPayOptions.cs` + `Program.cs` 的 Configure 调用。

**Tech Stack:** .NET 8 / EF Core 8 / SQL Server / xUnit + `Microsoft.EntityFrameworkCore.InMemory` / Vue 3 + TypeScript / Vite

**设计文档:** `Docs/specs/2026-04-20-微信支付参数入库设计.md`

**开放问题决定（源自 spec §12）:**
- **Q1 异步化边界** → 决定：全链路异步化。所有同步调用点（`PaymentsController:62`、`MiniAppController:923`、`PaymentsController:24 GetStatus`）都在已有 `async` 的 action method 里，改造成本低。**不引入** block-on-cache 同步 fallback
- **Q2 前端显示 `UpdatedAt`** → 决定：显示，"最后修改于 YYYY-MM-DD HH:mm"
- **Q3 默认授权角色** → 决定：仅 `super-admin`，与 `miniapp.theme.manage` 一致

---

## File Structure

### 新建

| 文件 | 职责 |
|---|---|
| `backend/src/FaJuan.Api/Domain/Entities/WeChatPaySetting.cs` | EF 实体，8 个字段 |
| `backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/<timestamp>_AddWeChatPaySetting.cs` | EF 迁移（含种子行） |
| `backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPaySettingsProvider.cs` | DB 读写 + 缓存 + 留空保留逻辑 |
| `backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPaySettingsModels.cs` | `WeChatPaySettingsSnapshot` / `WeChatPaySettingsUpdate` |
| `backend/src/FaJuan.Api/Controllers/AdminMiniAppPayController.cs` | GET / PUT |
| `backend/src/FaJuan.Api/Contracts/AdminWeChatPaySettingsDto.cs` | 回显 DTO |
| `backend/src/FaJuan.Api/Contracts/SaveAdminWeChatPaySettingsRequest.cs` | 提交 DTO |
| `backend/tests/FaJuan.Api.Tests/WeChatPaySettingsProviderTests.cs` | provider 单元测试 |
| `backend/tests/FaJuan.Api.Tests/AdminMiniAppPayControllerTests.cs` | controller 单元测试 |
| `frontend/src/api/miniapp-pay-setting.ts` | 前端 HTTP 客户端 |
| `frontend/src/types/miniapp-pay-setting.ts` | 前端类型 |
| `Docs/sql/2026-04-20-支付参数配置-权限初始化.sql` | 新增 `miniapp.pay.manage` 权限 + super-admin 绑定 |

### 修改

| 文件 | 改动 |
|---|---|
| `backend/src/FaJuan.Api/Infrastructure/Persistence/AppDbContext.cs` | 新增 `DbSet<WeChatPaySetting>` + `OnModelCreating` 配置 |
| `backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPayService.cs` | 构造参数改为 `WeChatPaySettingsProvider`；`IsConfigured`/`GetStatus` 异步化；`SignContent` 从 `ImportFromPem(snapshot.PrivateKeyPem)` 取私钥 |
| `backend/src/FaJuan.Api/Controllers/PaymentsController.cs` | 去掉 `IOptions<WeChatPayOptions>`；调用点改异步 |
| `backend/src/FaJuan.Api/Controllers/MiniAppController.cs` | 同上 |
| `backend/src/FaJuan.Api/Program.cs` | 删 `Configure<WeChatPayOptions>`；加 `AddScoped<WeChatPaySettingsProvider>` |
| `backend/src/FaJuan.Api/appsettings.json` | 删 `WeChatPay` 节（第 9–17 行） |
| `frontend/src/views/miniapp-setting/MiniAppSettingView.vue` | 顶部插入 tab 切换；支付 tab 内表单 |

### 删除

- `backend/src/FaJuan.Api/Contracts/WeChatPayOptions.cs`

---

## Task 1：新增 `WeChatPaySetting` 实体与 EF 配置

**Files:**
- Create: `backend/src/FaJuan.Api/Domain/Entities/WeChatPaySetting.cs`
- Modify: `backend/src/FaJuan.Api/Infrastructure/Persistence/AppDbContext.cs`

- [ ] **Step 1：写实体类**

`backend/src/FaJuan.Api/Domain/Entities/WeChatPaySetting.cs`：

```csharp
namespace FaJuan.Api.Domain.Entities;

public class WeChatPaySetting
{
    public int Id { get; set; }
    public string AppId { get; set; } = string.Empty;
    public string MerchantId { get; set; } = string.Empty;
    public string MerchantSerialNo { get; set; } = string.Empty;
    public string PrivateKeyPem { get; set; } = string.Empty;
    public string ApiV3Key { get; set; } = string.Empty;
    public string NotifyUrl { get; set; } = string.Empty;
    public bool EnableMockFallback { get; set; } = true;
    public DateTime UpdatedAt { get; set; }
}
```

- [ ] **Step 2：在 AppDbContext 注册 DbSet + 模型配置**

在 `backend/src/FaJuan.Api/Infrastructure/Persistence/AppDbContext.cs` 第 31 行 `DbSet<AdminRolePermission>` 之后加：

```csharp
    public DbSet<WeChatPaySetting> WeChatPaySettings => Set<WeChatPaySetting>();
```

在 `OnModelCreating` 末尾（第 305 行 `})` 的后面、方法闭合 `}` 前）加：

```csharp
        modelBuilder.Entity<WeChatPaySetting>(entity =>
        {
            entity.ToTable("WeChatPaySetting");
            entity.HasKey(x => x.Id);
            entity.Property(x => x.Id).ValueGeneratedNever();
            entity.Property(x => x.AppId).HasMaxLength(64).IsRequired();
            entity.Property(x => x.MerchantId).HasMaxLength(32).IsRequired();
            entity.Property(x => x.MerchantSerialNo).HasMaxLength(128).IsRequired();
            entity.Property(x => x.PrivateKeyPem).HasColumnType("nvarchar(max)").IsRequired();
            entity.Property(x => x.ApiV3Key).HasMaxLength(128).IsRequired();
            entity.Property(x => x.NotifyUrl).HasMaxLength(512).IsRequired();
            entity.Property(x => x.EnableMockFallback).IsRequired();
            entity.Property(x => x.UpdatedAt).HasColumnType("datetime");

            entity.HasData(new WeChatPaySetting
            {
                Id = 1,
                AppId = string.Empty,
                MerchantId = string.Empty,
                MerchantSerialNo = string.Empty,
                PrivateKeyPem = string.Empty,
                ApiV3Key = string.Empty,
                NotifyUrl = string.Empty,
                EnableMockFallback = true,
                UpdatedAt = new DateTime(2026, 4, 20, 0, 0, 0, DateTimeKind.Utc),
            });
        });
```

- [ ] **Step 3：编译确认无语法错误**

```bash
cd backend && dotnet build src/FaJuan.Api/FaJuan.Api.csproj --nologo -v minimal
```

Expected：`Build succeeded. 0 Warning(s). 0 Error(s).`

- [ ] **Step 4：生成迁移**

```bash
cd backend && dotnet ef migrations add AddWeChatPaySetting --project src/FaJuan.Api/FaJuan.Api.csproj
```

Expected：在 `backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/` 生成 `<timestamp>_AddWeChatPaySetting.cs`、`.Designer.cs` 以及 `AppDbContextModelSnapshot.cs` 更新。

打开生成的迁移文件，确认：
1. `Up()` 里有 `CreateTable("WeChatPaySetting", ...)`
2. `Up()` 里有 `InsertData("WeChatPaySetting", ...)`，`UpdatedAt` 是 `new DateTime(2026, 4, 20, ...)` 常量而非 `DateTime.Now`
3. `Down()` 里有 `DropTable("WeChatPaySetting")`

- [ ] **Step 5：Commit**

```bash
git add backend/src/FaJuan.Api/Domain/Entities/WeChatPaySetting.cs \
        backend/src/FaJuan.Api/Infrastructure/Persistence/AppDbContext.cs \
        backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/
git commit -m "feat(pay): add WeChatPaySetting entity and migration"
```

---

## Task 2：新增 `WeChatPaySettingsProvider`（TDD）

**Files:**
- Create: `backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPaySettingsModels.cs`
- Create: `backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPaySettingsProvider.cs`
- Test: `backend/tests/FaJuan.Api.Tests/WeChatPaySettingsProviderTests.cs`

- [ ] **Step 1：写 snapshot/update 模型**

`backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPaySettingsModels.cs`：

```csharp
namespace FaJuan.Api.Infrastructure.WeChatPay;

public class WeChatPaySettingsSnapshot
{
    public string AppId { get; init; } = string.Empty;
    public string MerchantId { get; init; } = string.Empty;
    public string MerchantSerialNo { get; init; } = string.Empty;
    public string PrivateKeyPem { get; init; } = string.Empty;
    public string ApiV3Key { get; init; } = string.Empty;
    public string NotifyUrl { get; init; } = string.Empty;
    public bool EnableMockFallback { get; init; }
    public DateTime UpdatedAt { get; init; }

    public bool IsConfigured =>
        !string.IsNullOrWhiteSpace(AppId)
        && !string.IsNullOrWhiteSpace(MerchantId)
        && !string.IsNullOrWhiteSpace(MerchantSerialNo)
        && !string.IsNullOrWhiteSpace(PrivateKeyPem)
        && !string.IsNullOrWhiteSpace(ApiV3Key)
        && !string.IsNullOrWhiteSpace(NotifyUrl);
}

public class WeChatPaySettingsUpdate
{
    public string? AppId { get; set; }
    public string? MerchantId { get; set; }
    public string? MerchantSerialNo { get; set; }
    public string? PrivateKeyPem { get; set; }
    public string? ApiV3Key { get; set; }
    public string? NotifyUrl { get; set; }
    public bool? EnableMockFallback { get; set; }
}
```

- [ ] **Step 2：写测试文件（先全部失败）**

`backend/tests/FaJuan.Api.Tests/WeChatPaySettingsProviderTests.cs`：

```csharp
using FaJuan.Api.Domain.Entities;
using FaJuan.Api.Infrastructure.Persistence;
using FaJuan.Api.Infrastructure.WeChatPay;
using Microsoft.EntityFrameworkCore;

namespace FaJuan.Api.Tests;

public class WeChatPaySettingsProviderTests
{
    [Fact]
    public async Task GetAsync_Should_Return_Seed_Row_On_First_Read()
    {
        await using var dbContext = CreateDbContext();
        SeedDefaultRow(dbContext);
        WeChatPaySettingsProvider.InvalidateCacheForTests();
        var provider = new WeChatPaySettingsProvider(dbContext);

        var snapshot = await provider.GetAsync();

        Assert.Equal(string.Empty, snapshot.AppId);
        Assert.True(snapshot.EnableMockFallback);
        Assert.False(snapshot.IsConfigured);
    }

    [Fact]
    public async Task SaveAsync_Should_Overwrite_NonNull_Fields_And_Keep_Null_Fields()
    {
        await using var dbContext = CreateDbContext();
        SeedDefaultRow(dbContext);
        dbContext.WeChatPaySettings.Single().ApiV3Key = "ORIG_V3";
        dbContext.WeChatPaySettings.Single().PrivateKeyPem = "ORIG_PEM";
        await dbContext.SaveChangesAsync();
        WeChatPaySettingsProvider.InvalidateCacheForTests();
        var provider = new WeChatPaySettingsProvider(dbContext);

        await provider.SaveAsync(new WeChatPaySettingsUpdate
        {
            AppId = "wxNEW",
            ApiV3Key = null,       // 保留原值
            PrivateKeyPem = "",    // 空串视为不修改
        });

        var snapshot = await provider.GetAsync();
        Assert.Equal("wxNEW", snapshot.AppId);
        Assert.Equal("ORIG_V3", snapshot.ApiV3Key);
        Assert.Equal("ORIG_PEM", snapshot.PrivateKeyPem);
    }

    [Fact]
    public async Task SaveAsync_Should_Ignore_Display_Placeholder_For_Sensitive_Fields()
    {
        await using var dbContext = CreateDbContext();
        SeedDefaultRow(dbContext);
        dbContext.WeChatPaySettings.Single().PrivateKeyPem = "ORIG_PEM";
        await dbContext.SaveChangesAsync();
        WeChatPaySettingsProvider.InvalidateCacheForTests();
        var provider = new WeChatPaySettingsProvider(dbContext);

        await provider.SaveAsync(new WeChatPaySettingsUpdate { PrivateKeyPem = "已配置" });

        var snapshot = await provider.GetAsync();
        Assert.Equal("ORIG_PEM", snapshot.PrivateKeyPem);
    }

    [Fact]
    public async Task SaveAsync_Should_Invalidate_Cache_Immediately()
    {
        await using var dbContext = CreateDbContext();
        SeedDefaultRow(dbContext);
        WeChatPaySettingsProvider.InvalidateCacheForTests();
        var provider = new WeChatPaySettingsProvider(dbContext);

        _ = await provider.GetAsync();  // 触发缓存
        await provider.SaveAsync(new WeChatPaySettingsUpdate { AppId = "wxAFTER" });

        var snapshot = await provider.GetAsync();
        Assert.Equal("wxAFTER", snapshot.AppId);
    }

    [Fact]
    public async Task SaveAsync_Should_Update_UpdatedAt_Timestamp()
    {
        await using var dbContext = CreateDbContext();
        SeedDefaultRow(dbContext);
        WeChatPaySettingsProvider.InvalidateCacheForTests();
        var provider = new WeChatPaySettingsProvider(dbContext);
        var before = DateTime.UtcNow.AddSeconds(-1);

        await provider.SaveAsync(new WeChatPaySettingsUpdate { AppId = "wxT" });

        var snapshot = await provider.GetAsync();
        Assert.True(snapshot.UpdatedAt >= before);
    }

    private static AppDbContext CreateDbContext()
    {
        var options = new DbContextOptionsBuilder<AppDbContext>()
            .UseInMemoryDatabase(Guid.NewGuid().ToString("N"))
            .Options;
        return new AppDbContext(options);
    }

    private static void SeedDefaultRow(AppDbContext dbContext)
    {
        dbContext.WeChatPaySettings.Add(new WeChatPaySetting
        {
            Id = 1,
            EnableMockFallback = true,
            UpdatedAt = new DateTime(2026, 4, 20, 0, 0, 0, DateTimeKind.Utc),
        });
        dbContext.SaveChanges();
    }
}
```

- [ ] **Step 3：跑测试确认失败**

```bash
cd backend && dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --filter "FullyQualifiedName~WeChatPaySettingsProviderTests" --nologo
```

Expected：编译失败或测试全部 FAIL（`WeChatPaySettingsProvider` 不存在）。

- [ ] **Step 4：写 Provider 实现**

`backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPaySettingsProvider.cs`：

```csharp
using FaJuan.Api.Domain.Entities;
using FaJuan.Api.Infrastructure.Persistence;
using Microsoft.EntityFrameworkCore;

namespace FaJuan.Api.Infrastructure.WeChatPay;

public class WeChatPaySettingsProvider(AppDbContext dbContext)
{
    private static WeChatPaySettingsSnapshot? _cached;
    private static DateTime _cachedAt;
    private static readonly SemaphoreSlim Gate = new(1, 1);
    private static readonly TimeSpan Ttl = TimeSpan.FromSeconds(30);

    public const string DisplayPlaceholder = "已配置";

    public async Task<WeChatPaySettingsSnapshot> GetAsync(CancellationToken cancellationToken = default)
    {
        var (cached, cachedAt) = (_cached, _cachedAt);
        if (cached is not null && DateTime.UtcNow - cachedAt < Ttl)
        {
            return cached;
        }

        await Gate.WaitAsync(cancellationToken);
        try
        {
            if (_cached is not null && DateTime.UtcNow - _cachedAt < Ttl)
            {
                return _cached;
            }

            var row = await dbContext.WeChatPaySettings.AsNoTracking()
                .SingleOrDefaultAsync(x => x.Id == 1, cancellationToken);

            _cached = row is null ? new WeChatPaySettingsSnapshot() : ToSnapshot(row);
            _cachedAt = DateTime.UtcNow;
            return _cached;
        }
        finally
        {
            Gate.Release();
        }
    }

    public async Task<WeChatPaySettingsSnapshot> SaveAsync(WeChatPaySettingsUpdate update, CancellationToken cancellationToken = default)
    {
        await Gate.WaitAsync(cancellationToken);
        try
        {
            var row = await dbContext.WeChatPaySettings.SingleOrDefaultAsync(x => x.Id == 1, cancellationToken)
                      ?? throw new InvalidOperationException("WeChatPaySetting seed row missing");

            if (update.AppId is not null) row.AppId = update.AppId.Trim();
            if (update.MerchantId is not null) row.MerchantId = update.MerchantId.Trim();
            if (update.MerchantSerialNo is not null) row.MerchantSerialNo = update.MerchantSerialNo.Trim();
            if (update.NotifyUrl is not null) row.NotifyUrl = update.NotifyUrl.Trim();
            if (update.EnableMockFallback is not null) row.EnableMockFallback = update.EnableMockFallback.Value;

            if (ShouldOverwriteSensitive(update.PrivateKeyPem)) row.PrivateKeyPem = update.PrivateKeyPem!.Trim();
            if (ShouldOverwriteSensitive(update.ApiV3Key)) row.ApiV3Key = update.ApiV3Key!.Trim();

            row.UpdatedAt = DateTime.UtcNow;
            await dbContext.SaveChangesAsync(cancellationToken);

            _cached = ToSnapshot(row);
            _cachedAt = DateTime.UtcNow;
            return _cached;
        }
        finally
        {
            Gate.Release();
        }
    }

    internal static void InvalidateCacheForTests()
    {
        _cached = null;
        _cachedAt = DateTime.MinValue;
    }

    private static bool ShouldOverwriteSensitive(string? value)
    {
        if (value is null) return false;
        var trimmed = value.Trim();
        if (trimmed.Length == 0) return false;
        if (trimmed == DisplayPlaceholder) return false;
        return true;
    }

    private static WeChatPaySettingsSnapshot ToSnapshot(WeChatPaySetting row) => new()
    {
        AppId = row.AppId,
        MerchantId = row.MerchantId,
        MerchantSerialNo = row.MerchantSerialNo,
        PrivateKeyPem = row.PrivateKeyPem,
        ApiV3Key = row.ApiV3Key,
        NotifyUrl = row.NotifyUrl,
        EnableMockFallback = row.EnableMockFallback,
        UpdatedAt = row.UpdatedAt,
    };
}
```

- [ ] **Step 5：跑测试确认全部通过**

```bash
cd backend && dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --filter "FullyQualifiedName~WeChatPaySettingsProviderTests" --nologo
```

Expected：`Passed! - Failed: 0, Passed: 5, Skipped: 0`

- [ ] **Step 6：Commit**

```bash
git add backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPaySettingsModels.cs \
        backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPaySettingsProvider.cs \
        backend/tests/FaJuan.Api.Tests/WeChatPaySettingsProviderTests.cs
git commit -m "feat(pay): add WeChatPaySettingsProvider with cache and keep-empty semantics"
```

---

## Task 3：改造 `WeChatPayService` 用 Provider 替代 `IOptions`

**Files:**
- Modify: `backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPayService.cs`

- [ ] **Step 1：把构造参数和成员改掉**

打开 `WeChatPayService.cs`，把：

```csharp
using FaJuan.Api.Contracts;
using Microsoft.Extensions.Options;
...
public class WeChatPayService(HttpClient httpClient, IOptions<WeChatPayOptions> options)
{
    private readonly WeChatPayOptions _options = options.Value;
```

改为：

```csharp
using FaJuan.Api.Contracts;
...
public class WeChatPayService(HttpClient httpClient, WeChatPaySettingsProvider settingsProvider)
{
```

移除 `using Microsoft.Extensions.Options;`。移除 `_options` 字段。

- [ ] **Step 2：把 `IsConfigured` / `GetStatus` 改为异步**

替换第 15-35 行两个方法：

```csharp
    public async Task<bool> IsConfiguredAsync(CancellationToken cancellationToken = default)
    {
        var opts = await settingsProvider.GetAsync(cancellationToken);
        return opts.IsConfigured;
    }

    public async Task<WeChatPayConfigStatusDto> GetStatusAsync(CancellationToken cancellationToken = default)
    {
        var opts = await settingsProvider.GetAsync(cancellationToken);
        return new WeChatPayConfigStatusDto
        {
            IsConfigured = opts.IsConfigured,
            EnableMockFallback = opts.EnableMockFallback,
            AppIdPreview = Preview(opts.AppId),
            MerchantIdPreview = Preview(opts.MerchantId),
            NotifyUrl = opts.NotifyUrl,
            Message = opts.IsConfigured ? "微信支付配置已就绪" : "请补齐 WeChatPay 配置",
        };
    }
```

- [ ] **Step 3：改造 `CreateJsapiOrderAsync`（第 37 行起）**

方法内第一行 `if (!IsConfigured())` 改为：

```csharp
        var opts = await settingsProvider.GetAsync(cancellationToken);
        if (!opts.IsConfigured)
        {
            return (false, "微信支付配置未完成", null);
        }
```

后续所有 `_options.AppId / _options.MerchantId / _options.NotifyUrl` 等改为 `opts.AppId / opts.MerchantId / opts.NotifyUrl`。

把方法底部 `var paySign = SignMiniProgramPay(_options.AppId, ...)` 改为 `SignMiniProgramPay(opts.AppId, opts.PrivateKeyPem, ...)`（见 Step 6 签名方法重构）。

- [ ] **Step 4：改造 `TryDecryptCallback`**

因为要访问 ApiV3Key，把方法签名改为接受一个 snapshot 入参或改为 async：

```csharp
    public async Task<WeChatTransactionResource?> TryDecryptCallbackAsync(string requestBody, CancellationToken cancellationToken = default)
    {
        var opts = await settingsProvider.GetAsync(cancellationToken);
        if (string.IsNullOrWhiteSpace(opts.ApiV3Key))
        {
            return null;
        }
        // 原有逻辑中 _options.ApiV3Key 改为 opts.ApiV3Key
        ...
    }
```

调用点同步改（在 PaymentsController 的回调里）。

- [ ] **Step 5：改造 `VerifyCallbackSignatureAsync`、`RefundAsync`、`LoadPlatformCertificateAsync`、`DecryptCertificate`、`BuildAuthorization`**

这些方法都需要 `opts` 信息。`VerifyCallbackSignatureAsync` 和 `RefundAsync` 已是 async，只需在方法开头拿 `var opts = await settingsProvider.GetAsync(ct);` 并把 `_options.XXX` 全部替换为 `opts.XXX`。

`LoadPlatformCertificateAsync` 和 `DecryptCertificate` 改为接受 `WeChatPaySettingsSnapshot` 入参（由调用方传递），避免重复查 provider：

```csharp
    private async Task<RSA?> LoadPlatformCertificateAsync(string serial, WeChatPaySettingsSnapshot opts, CancellationToken cancellationToken)
    {
        if (string.IsNullOrWhiteSpace(opts.ApiV3Key)) return null;
        ...
        // 内部调用 BuildAuthorization 时传入 opts
        var authorization = BuildAuthorization("GET", "/v3/certificates", timestamp, nonce, string.Empty, opts);
        ...
        // 内部调用 DecryptCertificate 时传入 opts
        var plainCert = DecryptCertificate(item.EncryptCertificate, opts);
        ...
    }

    private string? DecryptCertificate(WeChatPayEncryptCertificate encryptCertificate, WeChatPaySettingsSnapshot opts) { ... }

    private string BuildAuthorization(string method, string canonicalUrl, string timestamp, string nonce, string body, WeChatPaySettingsSnapshot opts)
    {
        var message = $"{method}\n{canonicalUrl}\n{timestamp}\n{nonce}\n{body}\n";
        var signature = SignContent(message, opts.PrivateKeyPem);
        return $"WECHATPAY2-SHA256-RSA2048 mchid=\"{opts.MerchantId}\",nonce_str=\"{nonce}\",signature=\"{signature}\",timestamp=\"{timestamp}\",serial_no=\"{opts.MerchantSerialNo}\"";
    }
```

所有 `BuildAuthorization` 的调用方都要传 `opts`。

- [ ] **Step 6：改造 `SignContent` 和 `SignMiniProgramPay`（PEM 从文件→字符串）**

```csharp
    private string SignMiniProgramPay(string appId, string privateKeyPem, string timestamp, string nonce, string packageValue)
    {
        var message = $"{appId}\n{timestamp}\n{nonce}\n{packageValue}\n";
        return SignContent(message, privateKeyPem);
    }

    private string SignContent(string content, string privateKeyPem)
    {
        using var rsa = RSA.Create();
        rsa.ImportFromPem(privateKeyPem);
        var signed = rsa.SignData(Encoding.UTF8.GetBytes(content), HashAlgorithmName.SHA256, RSASignaturePadding.Pkcs1);
        return Convert.ToBase64String(signed);
    }
```

在 `CreateJsapiOrderAsync` 调用处：`var paySign = SignMiniProgramPay(opts.AppId, opts.PrivateKeyPem, payTimeStamp, payNonce, packageValue);`

- [ ] **Step 7：`_platformCertificates` 字段保留**

不动——仍然是 per-service 实例缓存。

- [ ] **Step 8：编译确认**

```bash
cd backend && dotnet build src/FaJuan.Api/FaJuan.Api.csproj --nologo -v minimal
```

Expected：`Build succeeded. 0 Warning(s). 0 Error(s).`（注意此时 `PaymentsController` 和 `MiniAppController` 的调用点还没改，会编译失败——这一步暂时先看 `WeChatPayService.cs` 本身没语法错。Controllers 会在 Task 4 修。可以临时把 controller 里的调用注释掉再编，或者直接跳到 Task 4。）

**建议：** 跳到 Task 4 一并完成，在 Task 3 结束前不单独编译。

- [ ] **Step 9：Commit（与 Task 4 合并提交）**

不单独 commit，与 Task 4 一起。

---

## Task 4：修 `PaymentsController` 和 `MiniAppController` 调用点

**Files:**
- Modify: `backend/src/FaJuan.Api/Controllers/PaymentsController.cs`
- Modify: `backend/src/FaJuan.Api/Controllers/MiniAppController.cs`

- [ ] **Step 1：修 `PaymentsController`**

打开 `backend/src/FaJuan.Api/Controllers/PaymentsController.cs`：

1. 删除构造参数 `IOptions<WeChatPayOptions> weChatPayOptions`（第 21 行）和相关 `using Microsoft.Extensions.Options;`、`using FaJuan.Api.Contracts;`（若只为 WeChatPayOptions 用，酌情保留）
2. `GetWeChatPayStatus`（第 24 行）改为 async：

   ```csharp
   [HttpGet("wechat-status")]
   public async Task<ActionResult<ApiResponse<WeChatPayConfigStatusDto>>> GetWeChatPayStatus(CancellationToken cancellationToken)
       => Ok(Success(await weChatPayService.GetStatusAsync(cancellationToken)));
   ```

3. 第 62 行 `if (!weChatPayService.IsConfigured())` 改为：

   ```csharp
   var payStatus = await weChatPayService.GetStatusAsync(cancellationToken);
   if (!payStatus.IsConfigured)
   {
       if (!payStatus.EnableMockFallback)
       {
           return BadRequest(Failure<CreatePaymentResultDto>("微信支付未配置完成，且已关闭模拟支付回退"));
       }
       // 原走 mock 的逻辑
       ...
   }
   ```

4. 回调里用 `TryDecryptCallbackAsync`：`var resource = await weChatPayService.TryDecryptCallbackAsync(body, cancellationToken);`

- [ ] **Step 2：修 `MiniAppController`**

打开 `backend/src/FaJuan.Api/Controllers/MiniAppController.cs`：

1. 删除构造参数 `IOptions<WeChatPayOptions> weChatPayOptions`（第 27 行）
2. 删除 `using Microsoft.Extensions.Options;`（第 16 行，如只此处使用）
3. 第 923 行：

   ```csharp
   var payStatus = await weChatPayService.GetStatusAsync(cancellationToken);
   if (!payStatus.IsConfigured)
   {
       if (!payStatus.EnableMockFallback)
       {
           return BadRequest(Failure<MiniAppCreateOrderPaymentResultDto>("微信支付未配置完成，且已关闭模拟支付回退"));
       }
       ...
   }
   ```

- [ ] **Step 3：编译**

```bash
cd backend && dotnet build src/FaJuan.Api/FaJuan.Api.csproj --nologo -v minimal
```

Expected：`Build succeeded. 0 Warning(s). 0 Error(s).`（注意 `WeChatPayOptions` 类还存在，Program.cs 还 Configure 着，编译仍能过。如果已经引用了不存在的 `WeChatPaySettingsProvider`，其 DI 会在 Task 6 注册。）

- [ ] **Step 4：跑现有测试确保没回归**

```bash
cd backend && dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --nologo
```

Expected：现有 `UserCouponGrantServiceTests / ShareTrackingControllerTests / ApiResponseTests / ImageCompressorTests` 全过，加上 `WeChatPaySettingsProviderTests` 也过。

- [ ] **Step 5：Commit**

```bash
git add backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPayService.cs \
        backend/src/FaJuan.Api/Controllers/PaymentsController.cs \
        backend/src/FaJuan.Api/Controllers/MiniAppController.cs
git commit -m "refactor(pay): route WeChatPayService and controllers through WeChatPaySettingsProvider"
```

---

## Task 5：新建 `AdminMiniAppPayController`（TDD）

**Files:**
- Create: `backend/src/FaJuan.Api/Contracts/AdminWeChatPaySettingsDto.cs`
- Create: `backend/src/FaJuan.Api/Contracts/SaveAdminWeChatPaySettingsRequest.cs`
- Create: `backend/src/FaJuan.Api/Controllers/AdminMiniAppPayController.cs`
- Test: `backend/tests/FaJuan.Api.Tests/AdminMiniAppPayControllerTests.cs`

- [ ] **Step 1：写 DTO**

`backend/src/FaJuan.Api/Contracts/AdminWeChatPaySettingsDto.cs`：

```csharp
namespace FaJuan.Api.Contracts;

public class AdminWeChatPaySettingsDto
{
    public string AppId { get; set; } = string.Empty;
    public string MerchantId { get; set; } = string.Empty;
    public string MerchantSerialNo { get; set; } = string.Empty;
    public string PrivateKeyDisplay { get; set; } = string.Empty;
    public string ApiV3KeyDisplay { get; set; } = string.Empty;
    public string NotifyUrl { get; set; } = string.Empty;
    public bool EnableMockFallback { get; set; }
    public bool IsConfigured { get; set; }
    public DateTime? UpdatedAt { get; set; }
}
```

`backend/src/FaJuan.Api/Contracts/SaveAdminWeChatPaySettingsRequest.cs`：

```csharp
namespace FaJuan.Api.Contracts;

public class SaveAdminWeChatPaySettingsRequest
{
    public string? AppId { get; set; }
    public string? MerchantId { get; set; }
    public string? MerchantSerialNo { get; set; }
    public string? PrivateKeyPem { get; set; }
    public string? ApiV3Key { get; set; }
    public string? NotifyUrl { get; set; }
    public bool? EnableMockFallback { get; set; }
}
```

- [ ] **Step 2：写 Controller 测试文件**

`backend/tests/FaJuan.Api.Tests/AdminMiniAppPayControllerTests.cs`：

```csharp
using FaJuan.Api.Contracts;
using FaJuan.Api.Controllers;
using FaJuan.Api.Domain.Entities;
using FaJuan.Api.Infrastructure.Persistence;
using FaJuan.Api.Infrastructure.WeChatPay;
using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;

namespace FaJuan.Api.Tests;

public class AdminMiniAppPayControllerTests
{
    [Fact]
    public async Task Get_Should_Mask_Sensitive_Fields_When_Configured()
    {
        await using var dbContext = CreateDbContext();
        SeedRow(dbContext, privateKey: "PEM_SECRET", apiV3: "V3_SECRET");
        WeChatPaySettingsProvider.InvalidateCacheForTests();
        var controller = new AdminMiniAppPayController(new WeChatPaySettingsProvider(dbContext));

        var actionResult = await controller.Get(default);
        var response = ExtractOk(actionResult);

        Assert.Equal("已配置", response.PrivateKeyDisplay);
        Assert.Equal("已配置", response.ApiV3KeyDisplay);
        Assert.Equal("wxDEMO", response.AppId);
    }

    [Fact]
    public async Task Get_Should_Return_Empty_Display_When_Not_Configured()
    {
        await using var dbContext = CreateDbContext();
        SeedRow(dbContext, privateKey: "", apiV3: "");
        WeChatPaySettingsProvider.InvalidateCacheForTests();
        var controller = new AdminMiniAppPayController(new WeChatPaySettingsProvider(dbContext));

        var response = ExtractOk(await controller.Get(default));

        Assert.Equal(string.Empty, response.PrivateKeyDisplay);
        Assert.False(response.IsConfigured);
    }

    [Fact]
    public async Task Update_Should_Keep_Sensitive_Fields_When_Request_Sends_Empty()
    {
        await using var dbContext = CreateDbContext();
        SeedRow(dbContext, privateKey: "ORIG_PEM", apiV3: "ORIG_V3");
        WeChatPaySettingsProvider.InvalidateCacheForTests();
        var controller = new AdminMiniAppPayController(new WeChatPaySettingsProvider(dbContext));

        await controller.Update(new SaveAdminWeChatPaySettingsRequest
        {
            AppId = "wxNEW",
            PrivateKeyPem = "",
            ApiV3Key = null,
        }, default);

        var row = dbContext.WeChatPaySettings.AsNoTracking().Single();
        Assert.Equal("wxNEW", row.AppId);
        Assert.Equal("ORIG_PEM", row.PrivateKeyPem);
        Assert.Equal("ORIG_V3", row.ApiV3Key);
    }

    [Fact]
    public async Task Update_Should_Reject_Non_Https_Notify_Url()
    {
        await using var dbContext = CreateDbContext();
        SeedRow(dbContext);
        WeChatPaySettingsProvider.InvalidateCacheForTests();
        var controller = new AdminMiniAppPayController(new WeChatPaySettingsProvider(dbContext));

        var actionResult = await controller.Update(new SaveAdminWeChatPaySettingsRequest
        {
            NotifyUrl = "http://insecure.example.com/callback",
        }, default);

        var bad = Assert.IsType<BadRequestObjectResult>(actionResult.Result);
        var body = Assert.IsType<ApiResponse<AdminWeChatPaySettingsDto>>(bad.Value);
        Assert.False(body.Success);
        Assert.Contains("https", body.Message);
    }

    [Fact]
    public async Task Update_Should_Reject_Malformed_Private_Key_Pem()
    {
        await using var dbContext = CreateDbContext();
        SeedRow(dbContext);
        WeChatPaySettingsProvider.InvalidateCacheForTests();
        var controller = new AdminMiniAppPayController(new WeChatPaySettingsProvider(dbContext));

        var actionResult = await controller.Update(new SaveAdminWeChatPaySettingsRequest
        {
            PrivateKeyPem = "not-a-pem-content",
        }, default);

        var bad = Assert.IsType<BadRequestObjectResult>(actionResult.Result);
        var body = Assert.IsType<ApiResponse<AdminWeChatPaySettingsDto>>(bad.Value);
        Assert.False(body.Success);
        Assert.Contains("PEM", body.Message, StringComparison.OrdinalIgnoreCase);
    }

    private static AppDbContext CreateDbContext()
    {
        var options = new DbContextOptionsBuilder<AppDbContext>()
            .UseInMemoryDatabase(Guid.NewGuid().ToString("N"))
            .Options;
        return new AppDbContext(options);
    }

    private static void SeedRow(AppDbContext dbContext, string privateKey = "", string apiV3 = "")
    {
        dbContext.WeChatPaySettings.Add(new WeChatPaySetting
        {
            Id = 1,
            AppId = "wxDEMO",
            MerchantId = "1600000000",
            MerchantSerialNo = "ABC123",
            PrivateKeyPem = privateKey,
            ApiV3Key = apiV3,
            NotifyUrl = "https://example.com/callback",
            EnableMockFallback = true,
            UpdatedAt = new DateTime(2026, 4, 20, 0, 0, 0, DateTimeKind.Utc),
        });
        dbContext.SaveChanges();
    }

    private static AdminWeChatPaySettingsDto ExtractOk(ActionResult<ApiResponse<AdminWeChatPaySettingsDto>> actionResult)
    {
        var ok = Assert.IsType<OkObjectResult>(actionResult.Result);
        var body = Assert.IsType<ApiResponse<AdminWeChatPaySettingsDto>>(ok.Value);
        Assert.True(body.Success);
        return body.Data!;
    }
}
```

- [ ] **Step 3：跑测试确认失败**

```bash
cd backend && dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --filter "FullyQualifiedName~AdminMiniAppPayControllerTests" --nologo
```

Expected：编译失败（`AdminMiniAppPayController` 不存在）。

- [ ] **Step 4：写 Controller 实现**

`backend/src/FaJuan.Api/Controllers/AdminMiniAppPayController.cs`：

```csharp
using System.Text.RegularExpressions;
using FaJuan.Api.Contracts;
using FaJuan.Api.Infrastructure.Auth;
using FaJuan.Api.Infrastructure.WeChatPay;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;

namespace FaJuan.Api.Controllers;

[Authorize]
[AdminMenuAuthorize("/miniapp-settings")]
public class AdminMiniAppPayController(WeChatPaySettingsProvider provider) : ApiControllerBase
{
    private static readonly Regex DigitsOnly = new("^[0-9]{1,32}$");

    [HttpGet]
    public async Task<ActionResult<ApiResponse<AdminWeChatPaySettingsDto>>> Get(CancellationToken cancellationToken)
    {
        var snapshot = await provider.GetAsync(cancellationToken);
        return Ok(Success(ToDto(snapshot)));
    }

    [AdminPermissionAuthorize("miniapp.pay.manage")]
    [HttpPut]
    public async Task<ActionResult<ApiResponse<AdminWeChatPaySettingsDto>>> Update(
        [FromBody] SaveAdminWeChatPaySettingsRequest request, CancellationToken cancellationToken)
    {
        if (request is null)
        {
            return BadRequest(Failure<AdminWeChatPaySettingsDto>("请求体不能为空"));
        }

        if (!string.IsNullOrWhiteSpace(request.MerchantId) && !DigitsOnly.IsMatch(request.MerchantId.Trim()))
        {
            return BadRequest(Failure<AdminWeChatPaySettingsDto>("商户号必须为数字，长度 1-32"));
        }

        if (!string.IsNullOrWhiteSpace(request.NotifyUrl)
            && !request.NotifyUrl.Trim().StartsWith("https://", StringComparison.OrdinalIgnoreCase))
        {
            return BadRequest(Failure<AdminWeChatPaySettingsDto>("支付回调地址必须以 https:// 开头"));
        }

        if (!string.IsNullOrWhiteSpace(request.PrivateKeyPem)
            && request.PrivateKeyPem.Trim() != WeChatPaySettingsProvider.DisplayPlaceholder
            && (!request.PrivateKeyPem.Contains("BEGIN PRIVATE KEY") || !request.PrivateKeyPem.Contains("END PRIVATE KEY")))
        {
            return BadRequest(Failure<AdminWeChatPaySettingsDto>("商户私钥 PEM 格式错误，必须包含 BEGIN/END PRIVATE KEY 标记"));
        }

        var snapshot = await provider.SaveAsync(new WeChatPaySettingsUpdate
        {
            AppId = request.AppId,
            MerchantId = request.MerchantId,
            MerchantSerialNo = request.MerchantSerialNo,
            PrivateKeyPem = request.PrivateKeyPem,
            ApiV3Key = request.ApiV3Key,
            NotifyUrl = request.NotifyUrl,
            EnableMockFallback = request.EnableMockFallback,
        }, cancellationToken);

        return Ok(Success(ToDto(snapshot), "保存成功"));
    }

    private static AdminWeChatPaySettingsDto ToDto(WeChatPaySettingsSnapshot snapshot) => new()
    {
        AppId = snapshot.AppId,
        MerchantId = snapshot.MerchantId,
        MerchantSerialNo = snapshot.MerchantSerialNo,
        PrivateKeyDisplay = string.IsNullOrEmpty(snapshot.PrivateKeyPem) ? "" : WeChatPaySettingsProvider.DisplayPlaceholder,
        ApiV3KeyDisplay = string.IsNullOrEmpty(snapshot.ApiV3Key) ? "" : WeChatPaySettingsProvider.DisplayPlaceholder,
        NotifyUrl = snapshot.NotifyUrl,
        EnableMockFallback = snapshot.EnableMockFallback,
        IsConfigured = snapshot.IsConfigured,
        UpdatedAt = snapshot.UpdatedAt == default ? null : snapshot.UpdatedAt,
    };
}
```

- [ ] **Step 5：跑测试确认通过**

```bash
cd backend && dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --filter "FullyQualifiedName~AdminMiniAppPayControllerTests" --nologo
```

Expected：`Passed! - Failed: 0, Passed: 5, Skipped: 0`

- [ ] **Step 6：Commit**

```bash
git add backend/src/FaJuan.Api/Contracts/AdminWeChatPaySettingsDto.cs \
        backend/src/FaJuan.Api/Contracts/SaveAdminWeChatPaySettingsRequest.cs \
        backend/src/FaJuan.Api/Controllers/AdminMiniAppPayController.cs \
        backend/tests/FaJuan.Api.Tests/AdminMiniAppPayControllerTests.cs
git commit -m "feat(pay): add AdminMiniAppPayController with masked read and keep-empty write"
```

---

## Task 6：DI 注册 + 清理 appsettings 和 `WeChatPayOptions`

**Files:**
- Modify: `backend/src/FaJuan.Api/Program.cs`
- Modify: `backend/src/FaJuan.Api/appsettings.json`
- Modify: `backend/src/FaJuan.Api/appsettings.Development.json`（如存在 WeChatPay 节）
- Delete: `backend/src/FaJuan.Api/Contracts/WeChatPayOptions.cs`

- [ ] **Step 1：改 `Program.cs`**

打开 `backend/src/FaJuan.Api/Program.cs`：

1. 删除第 44 行：`builder.Services.Configure<WeChatPayOptions>(builder.Configuration.GetSection("WeChatPay"));`
2. 在 `builder.Services.AddHttpClient<WeChatPayService>();`（第 105 行）**之前**加：
   ```csharp
   builder.Services.AddScoped<WeChatPaySettingsProvider>();
   ```
3. 如 `using FaJuan.Api.Contracts;` 只用于 `WeChatPayOptions`，评估是否可删（大概率还有别的 DTO 在用，保留）

- [ ] **Step 2：删 `appsettings.json` 的 `WeChatPay` 节**

打开 `backend/src/FaJuan.Api/appsettings.json`，删除第 9-17 行整段：

```json
  "WeChatPay": {
    "AppId": "",
    ...
    "EnableMockFallback": true
  },
```

保留其余配置。注意去掉前一行逗号。

- [ ] **Step 3：检查 `appsettings.Development.json`**

```bash
grep -A 10 '"WeChatPay"' backend/src/FaJuan.Api/appsettings.Development.json || echo "no WeChatPay section"
```

若有，一并删除。

- [ ] **Step 4：删除 `WeChatPayOptions.cs`**

```bash
rm backend/src/FaJuan.Api/Contracts/WeChatPayOptions.cs
```

- [ ] **Step 5：全量编译 + 跑测试**

```bash
cd backend && dotnet build FaJuan.sln --nologo -v minimal && dotnet test tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj --nologo
```

Expected：`Build succeeded` + 所有测试通过。若 `WeChatPayOptions` 还有残留引用，把 `using FaJuan.Api.Contracts;` 对应文件里的引用清掉。

- [ ] **Step 6：Commit**

```bash
git add backend/src/FaJuan.Api/Program.cs \
        backend/src/FaJuan.Api/appsettings.json \
        backend/src/FaJuan.Api/appsettings.Development.json
git rm backend/src/FaJuan.Api/Contracts/WeChatPayOptions.cs
git commit -m "chore(pay): remove WeChatPay appsettings and WeChatPayOptions; register provider"
```

---

## Task 7：前端类型 + API 客户端

**Files:**
- Create: `frontend/src/types/miniapp-pay-setting.ts`
- Create: `frontend/src/api/miniapp-pay-setting.ts`

- [ ] **Step 1：写类型定义**

`frontend/src/types/miniapp-pay-setting.ts`：

```ts
export interface AdminWeChatPaySettingsDto {
  appId: string
  merchantId: string
  merchantSerialNo: string
  privateKeyDisplay: string
  apiV3KeyDisplay: string
  notifyUrl: string
  enableMockFallback: boolean
  isConfigured: boolean
  updatedAt: string | null
}

export interface SaveAdminWeChatPaySettingsRequest {
  appId?: string
  merchantId?: string
  merchantSerialNo?: string
  privateKeyPem?: string
  apiV3Key?: string
  notifyUrl?: string
  enableMockFallback?: boolean
}
```

- [ ] **Step 2：写 API 客户端**

`frontend/src/api/miniapp-pay-setting.ts`：

```ts
import http from './http'
import type { ApiResponse } from '@/types/api'
import type {
  AdminWeChatPaySettingsDto,
  SaveAdminWeChatPaySettingsRequest,
} from '@/types/miniapp-pay-setting'

export const getMiniAppPaySettings = () =>
  http.get<never, ApiResponse<AdminWeChatPaySettingsDto>>('/AdminMiniAppPay')

export const updateMiniAppPaySettings = (payload: SaveAdminWeChatPaySettingsRequest) =>
  http.put<SaveAdminWeChatPaySettingsRequest, ApiResponse<AdminWeChatPaySettingsDto>>(
    '/AdminMiniAppPay',
    payload,
  )
```

- [ ] **Step 3：Commit**

```bash
git add frontend/src/types/miniapp-pay-setting.ts frontend/src/api/miniapp-pay-setting.ts
git commit -m "feat(pay): frontend API client and types for pay settings"
```

---

## Task 8：前端页面改造为双 tab

**Files:**
- Modify: `frontend/src/views/miniapp-setting/MiniAppSettingView.vue`

- [ ] **Step 1：在 `<script setup>` 区增加支付 tab 状态、表单、加载逻辑**

打开 `frontend/src/views/miniapp-setting/MiniAppSettingView.vue`，在现有 `<script setup>` 第 128 行 `import` 区加：

```ts
import { getMiniAppPaySettings, updateMiniAppPaySettings } from '@/api/miniapp-pay-setting'
import type {
  AdminWeChatPaySettingsDto,
  SaveAdminWeChatPaySettingsRequest,
} from '@/types/miniapp-pay-setting'
```

在 `const activeThemeLabel = computed(...)` 之后（第 148 行下方）加：

```ts
type TabKey = 'theme' | 'pay'
const activeTab = ref<TabKey>('theme')
const canManagePay = authStorage.hasPermission('miniapp.pay.manage')

const payLoading = ref(false)
const paySubmitting = ref(false)
const payLoaded = ref(false)
const payForm = reactive<SaveAdminWeChatPaySettingsRequest>({
  appId: '',
  merchantId: '',
  merchantSerialNo: '',
  privateKeyPem: '',
  apiV3Key: '',
  notifyUrl: '',
  enableMockFallback: true,
})
const payStatus = reactive({
  privateKeyDisplay: '',
  apiV3KeyDisplay: '',
  isConfigured: false,
  updatedAt: '' as string | null,
})

const payUpdatedAtLabel = computed(() =>
  payStatus.updatedAt ? new Date(payStatus.updatedAt).toLocaleString() : '从未保存',
)
const payConfiguredLabel = computed(() => (payStatus.isConfigured ? '已配置' : '未配置完整'))

const loadPayData = async () => {
  payLoading.value = true
  try {
    const response = await getMiniAppPaySettings()
    const dto = response.data
    payForm.appId = dto.appId
    payForm.merchantId = dto.merchantId
    payForm.merchantSerialNo = dto.merchantSerialNo
    payForm.privateKeyPem = ''
    payForm.apiV3Key = ''
    payForm.notifyUrl = dto.notifyUrl
    payForm.enableMockFallback = dto.enableMockFallback
    payStatus.privateKeyDisplay = dto.privateKeyDisplay
    payStatus.apiV3KeyDisplay = dto.apiV3KeyDisplay
    payStatus.isConfigured = dto.isConfigured
    payStatus.updatedAt = dto.updatedAt
    payLoaded.value = true
  } catch (error) {
    notify.error(getErrorMessage(error, '加载支付参数失败'))
  } finally {
    payLoading.value = false
  }
}

const submitPay = async () => {
  if (!canManagePay || paySubmitting.value) return
  paySubmitting.value = true
  try {
    const payload: SaveAdminWeChatPaySettingsRequest = {
      appId: payForm.appId,
      merchantId: payForm.merchantId,
      merchantSerialNo: payForm.merchantSerialNo,
      notifyUrl: payForm.notifyUrl,
      enableMockFallback: payForm.enableMockFallback,
    }
    if (payForm.privateKeyPem && payForm.privateKeyPem.trim().length > 0) {
      payload.privateKeyPem = payForm.privateKeyPem
    }
    if (payForm.apiV3Key && payForm.apiV3Key.trim().length > 0) {
      payload.apiV3Key = payForm.apiV3Key
    }
    await updateMiniAppPaySettings(payload)
    notify.success('支付参数保存成功')
    await loadPayData()
  } catch (error) {
    notify.error(getErrorMessage(error, '保存支付参数失败'))
  } finally {
    paySubmitting.value = false
  }
}

const switchTab = (tab: TabKey) => {
  activeTab.value = tab
  if (tab === 'pay' && !payLoaded.value) void loadPayData()
}
```

- [ ] **Step 2：在 `<template>` 顶部 hero 之后、`stats-grid` 之前插入 tab 栏**

在 `</section>` 关闭 `.hero-panel` 之后（约第 36 行）、`<section class="stats-grid">`（约第 38 行）之前插入：

```html
    <nav class="tab-bar">
      <button type="button" class="tab-btn" :class="{ active: activeTab === 'theme' }" @click="switchTab('theme')">主题设置</button>
      <button type="button" class="tab-btn" :class="{ active: activeTab === 'pay' }" @click="switchTab('pay')">支付参数</button>
    </nav>
```

- [ ] **Step 3：把现有主题相关内容（`stats-grid` 和 `miniapp-theme-content-grid` 两个 section）包进 `v-if="activeTab === 'theme'"` 容器**

在 `<section class="stats-grid stats-grid-v2">` 外包一层 `<div v-if="activeTab === 'theme'">`，在 `</section>` 关闭 `.miniapp-theme-content-grid` 之后关闭该 `</div>`。

- [ ] **Step 4：在主题容器之后插入支付 tab 的内容**

```html
    <div v-if="activeTab === 'pay'" class="pay-tab">
      <article class="card card-v2 data-card">
        <div class="section-head">
          <div class="section-head-main">
            <span class="section-kicker">微信支付</span>
            <h3>支付参数配置</h3>
            <p class="section-tip">保存后即时生效；敏感字段留空表示保留原值。</p>
          </div>
          <div class="section-meta">
            <span class="badge" :class="payStatus.isConfigured ? 'success' : 'warning'">{{ payConfiguredLabel }}</span>
            <span class="muted">最后修改：{{ payUpdatedAtLabel }}</span>
          </div>
        </div>

        <div class="form-grid">
          <label class="form-field">
            <span>小程序 AppId</span>
            <input v-model="payForm.appId" :disabled="!canManagePay || paySubmitting" placeholder="wx开头的 AppId" />
          </label>
          <label class="form-field">
            <span>商户号</span>
            <input v-model="payForm.merchantId" :disabled="!canManagePay || paySubmitting" placeholder="1600000000" />
          </label>
          <label class="form-field">
            <span>商户证书序列号</span>
            <input v-model="payForm.merchantSerialNo" :disabled="!canManagePay || paySubmitting" placeholder="40 位左右的大写十六进制" />
          </label>
          <label class="form-field form-field-wide">
            <span>商户私钥 PEM</span>
            <textarea v-model="payForm.privateKeyPem" :disabled="!canManagePay || paySubmitting" rows="6"
              :placeholder="payStatus.privateKeyDisplay ? `已配置（留空表示保留原值）` : '-----BEGIN PRIVATE KEY-----&#10;...&#10;-----END PRIVATE KEY-----'"></textarea>
          </label>
          <label class="form-field">
            <span>APIv3 密钥</span>
            <input v-model="payForm.apiV3Key" type="password" :disabled="!canManagePay || paySubmitting"
              :placeholder="payStatus.apiV3KeyDisplay ? '已配置（留空表示保留原值）' : '32 位 APIv3 密钥'" />
          </label>
          <label class="form-field form-field-wide">
            <span>支付回调地址</span>
            <input v-model="payForm.notifyUrl" :disabled="!canManagePay || paySubmitting" placeholder="https://your-domain/api/payments/callback" />
          </label>
          <label class="form-field form-field-wide form-checkbox">
            <input type="checkbox" v-model="payForm.enableMockFallback" :disabled="!canManagePay || paySubmitting" />
            <span>未配置时回落模拟支付（开发/测试用）</span>
          </label>
        </div>

        <div class="toolbar-actions pay-actions">
          <button type="button" class="ghost-button" :disabled="payLoading || paySubmitting" @click="loadPayData">刷新配置</button>
          <button v-if="canManagePay" type="button" class="primary-button" :disabled="payLoading || paySubmitting" @click="submitPay">{{ paySubmitting ? '保存中...' : '保存支付参数' }}</button>
        </div>
      </article>
    </div>
```

- [ ] **Step 5：在 `<style scoped>` 底部加样式**

```css
.tab-bar {
  display: flex;
  gap: 8px;
  margin: 12px 0;
}

.tab-btn {
  padding: 10px 20px;
  border: 1px solid var(--line);
  border-radius: 999px;
  background: #fff;
  color: #475467;
  cursor: pointer;
  font-weight: 500;
  transition: all 0.15s ease;
}

.tab-btn.active {
  background: linear-gradient(135deg, #2563eb, #4f46e5);
  border-color: transparent;
  color: #fff;
  box-shadow: 0 10px 20px rgba(37, 99, 235, 0.18);
}

.pay-tab .form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px 18px;
  margin-top: 18px;
}

.pay-tab .form-field {
  display: flex;
  flex-direction: column;
  gap: 6px;
  font-size: 14px;
  color: #334155;
}

.pay-tab .form-field > span {
  font-weight: 500;
}

.pay-tab .form-field input,
.pay-tab .form-field textarea {
  padding: 10px 12px;
  border: 1px solid var(--line);
  border-radius: 10px;
  background: #fff;
  font-family: inherit;
  font-size: 14px;
}

.pay-tab .form-field textarea {
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  font-size: 12px;
  line-height: 1.5;
}

.pay-tab .form-field-wide {
  grid-column: 1 / -1;
}

.pay-tab .form-checkbox {
  flex-direction: row;
  align-items: center;
  gap: 10px;
}

.pay-tab .pay-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 18px;
}

.pay-tab .section-meta {
  display: flex;
  align-items: center;
  gap: 14px;
  color: #64748b;
  font-size: 12px;
}
```

- [ ] **Step 6：本地启动前端验证 Vue 编译**

```bash
cd frontend && npm run build
```

Expected：`build` 成功无 TypeScript 错误。

- [ ] **Step 7：Commit**

```bash
git add frontend/src/views/miniapp-setting/MiniAppSettingView.vue
git commit -m "feat(pay): add pay settings tab to MiniAppSettingView"
```

---

## Task 9：菜单 SQL 投递脚本

**Files:**
- Create: `Docs/sql/2026-04-20-支付参数配置-权限初始化.sql`

- [ ] **Step 1：写 SQL 脚本**

`Docs/sql/2026-04-20-支付参数配置-权限初始化.sql`：

```sql
-- 支付参数配置：权限、角色绑定初始化
-- 兼容 SQL Server 2008 R2
-- 菜单沿用现有 /miniapp-settings，仅新增按钮权限

SET NOCOUNT ON;

DECLARE @Now DATETIME;
SET @Now = GETDATE();

DECLARE @PermissionId BIGINT;

IF NOT EXISTS (SELECT 1 FROM AdminPermission WHERE Code = 'miniapp.pay.manage')
BEGIN
    INSERT INTO AdminPermission ([Name], Code, MenuPath, Sort, IsEnabled, CreatedAt)
    VALUES (N'支付参数配置', 'miniapp.pay.manage', '/miniapp-settings', 81, 1, @Now);
END;

SELECT @PermissionId = Id
FROM AdminPermission
WHERE Code = 'miniapp.pay.manage';

IF @PermissionId IS NOT NULL
BEGIN
    INSERT INTO AdminRolePermission (AdminRoleId, AdminPermissionId, CreatedAt)
    SELECT r.Id, @PermissionId, @Now
    FROM AdminRole r
    WHERE r.IsEnabled = 1
      AND r.Code = 'super-admin'
      AND NOT EXISTS (
          SELECT 1
          FROM AdminRolePermission rp
          WHERE rp.AdminRoleId = r.Id AND rp.AdminPermissionId = @PermissionId
      );
END;
```

- [ ] **Step 2：在本地 SQL Server 上执行一次验证**

```bash
# 以项目现有连接字串连上数据库，手动用 sqlcmd 或 SSMS 执行脚本
# 或等待下一次启动由运维统一执行；本步只要保证脚本语法无误
```

Expected：脚本执行无报错；`SELECT * FROM AdminPermission WHERE Code = 'miniapp.pay.manage'` 返回 1 行。

- [ ] **Step 3：Commit**

```bash
git add "Docs/sql/2026-04-20-支付参数配置-权限初始化.sql"
git commit -m "chore(pay): add SQL seed for miniapp.pay.manage permission"
```

---

## Task 10：端到端手测

- [ ] **Step 1：启动后端**

```bash
cd backend/src/FaJuan.Api && dotnet run
```

确认启动日志显示迁移自动应用，新表 `WeChatPaySetting` 出现在数据库中。

- [ ] **Step 2：启动前端（另一个终端）**

```bash
cd frontend && npm run dev
```

- [ ] **Step 3：登录后台 → 进入「小程序设置」**

用 super-admin 账号登录。确认页面顶部出现两个 tab 按钮：「主题设置」「支付参数」。默认停留在「主题设置」，功能无回归（能切主题、能保存）。

- [ ] **Step 4：切到「支付参数」tab**

- 所有字段空（因为种子行是空串）
- `privateKeyDisplay` 和 `apiV3KeyDisplay` 为空 → 对应 placeholder 显示 `-----BEGIN PRIVATE KEY-----...` 样例
- 状态显示「未配置完整」
- 最后修改显示「从未保存」或 2026-04-20 种子时间

- [ ] **Step 5：填入假数据并保存**

- AppId: `wxTESTDEMO`
- 商户号: `1600000001`
- 证书序列号: `ABCDEF1234567890`
- 私钥 PEM：粘贴一段合法 PEM（可以 `openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out /tmp/test.pem` 生成，或用在线工具）
- APIv3 密钥：`32charslongtestkeyxxxxxxxxxxxxxx`
- NotifyUrl: `https://example.com/api/payments/callback`
- 勾掉"回落模拟支付"

保存 → 应看到"保存成功"提示。

- [ ] **Step 6：刷新页面重新切到 pay tab**

- 非敏感字段显示原值
- `privateKeyDisplay` 和 `apiV3KeyDisplay` 的占位符变成"已配置（留空表示保留原值）"
- 状态显示「已配置」
- 最后修改显示刚才的时间

- [ ] **Step 7：只改 AppId，敏感字段留空**

把 AppId 改为 `wxUPDATED`，不动 PEM / APIv3，保存。再次刷新，AppId 已改，敏感字段状态仍为「已配置」。用 SQL 确认数据库中 `PrivateKeyPem / ApiV3Key` 未被清空：

```sql
SELECT AppId, LEN(PrivateKeyPem) AS PemLen, LEN(ApiV3Key) AS V3Len FROM WeChatPaySetting;
```

- [ ] **Step 8：负向校验**

- 把 NotifyUrl 改为 `http://...`（非 https）→ 保存应报"必须以 https:// 开头"
- 把私钥 PEM 贴成 `"not-a-pem"` → 保存应报"PEM 格式错误"
- 把商户号改为 `abc123` → 保存应报"商户号必须为数字"

- [ ] **Step 9：模拟支付回退**

1. 关闭「回落模拟支付」checkbox，把 AppId 清空保存 → 此时 `IsConfigured=false`
2. 在小程序端发起一笔订单支付 → 应返回"微信支付未配置完成，且已关闭模拟支付回退"
3. 勾上「回落模拟支付」保存，再次下单 → 走模拟支付正常

- [ ] **Step 10：权限验证**

- 用一个**没有** `miniapp.pay.manage` 权限的角色登录
- 切到支付 tab：字段全部只读、无"保存"按钮
- 即使强行 curl PUT `/api/AdminMiniAppPay`，也返回 403

---

## Task 11：整体回归与 PR

- [ ] **Step 1：跑全部后端测试**

```bash
cd backend && dotnet test FaJuan.sln --nologo
```

Expected：全部 Pass。

- [ ] **Step 2：跑前端类型检查 + 构建**

```bash
cd frontend && npm run build
```

Expected：无 error。

- [ ] **Step 3：检查 git 工作区**

```bash
git status
git log --oneline main..HEAD
```

应看到 7 个左右的 commit（每个 Task 一个），对应 feat(pay) / refactor(pay) / chore(pay) 等。

- [ ] **Step 4：根据项目习惯决定 PR / 直合**

若项目走 PR flow：

```bash
gh pr create --title "feat: 微信支付参数入库与后台配置页" --body "$(cat <<'EOF'
## Summary
- 新增 WeChatPaySetting 实体与迁移，支付参数从 appsettings 迁移到数据库
- WeChatPaySettingsProvider 负责读写 + 30s 内存缓存 + 敏感字段留空保留语义
- WeChatPayService 全链路异步化，私钥从 PEM 文本直接 ImportFromPem
- 新增 AdminMiniAppPayController（GET 掩码回显 / PUT 留空保留）
- 前端 MiniAppSettingView 改为双 tab：主题设置 / 支付参数
- 删除 appsettings.json 的 WeChatPay 节、WeChatPayOptions.cs
- 新增权限 miniapp.pay.manage，默认授予 super-admin（SQL 脚本）

## Test plan
- [ ] 后端单元测试全过（Provider / Controller 共 10 个新增）
- [ ] 前端 npm run build 无 TS 错误
- [ ] 手测：切 tab、保存、敏感字段留空不清空、负向校验（NotifyUrl / PEM / MerchantId）
- [ ] 手测：模拟支付回退开关实际影响小程序下单
- [ ] 手测：无权限账号 tab 只读 + PUT 403
EOF
)"
```

或按项目习惯直接 `git push origin HEAD` 合主。

---

## Self-Review

检查项：

**1. Spec 覆盖:**
- §4 数据模型 → Task 1 ✓
- §5 Provider → Task 2 ✓
- §5 WeChatPayService 改造 → Task 3 ✓
- §6 API → Task 5 ✓
- §7 前端 → Task 7 + Task 8 ✓
- §8 清理 → Task 6 ✓
- §9 菜单权限 → Task 9 ✓
- §10 测试 → Task 2/5 单测 + Task 10 手测 ✓
- §11 迁移发布 → Task 10 + 11 ✓
- §12 开放问题 → 顶部已拍板 ✓

**2. Placeholder scan:** 无 TBD / TODO / "Similar to Task N" / "appropriate error handling"。`spec §12 Q1` 这类只在"开放问题决议"块引用，已给出明确决定。

**3. Type consistency:**
- `WeChatPaySetting` 8 个字段 / `WeChatPaySettingsSnapshot` 8 个字段 / `AdminWeChatPaySettingsDto` 9 个字段（多一个 `IsConfigured`）✓
- `DisplayPlaceholder = "已配置"` 常量在 Provider 和 Controller 两处都有使用，通过 `WeChatPaySettingsProvider.DisplayPlaceholder` 共享 ✓
- Provider 方法：`GetAsync(ct)` / `SaveAsync(update, ct)` / `InvalidateCacheForTests()` 在所有引用处名字一致 ✓
- Service 新名：`IsConfiguredAsync` / `GetStatusAsync` / `TryDecryptCallbackAsync`——Controller 修改步骤已用这些名字 ✓
