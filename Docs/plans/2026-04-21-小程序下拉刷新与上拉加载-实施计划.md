# 小程序下拉刷新与上拉加载 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为小程序 4 个 tab 页 + 2 个列表页补齐自定义下拉刷新与上拉加载更多，视觉与主题统一。

**Architecture:** 新增 `CmPullRefresh` 组件（基于 `scroll-view` + refresher slot，内置主题色 logo 旋转动画）和 `use-list-pagination` composable（集中管理 items/pageIndex/noMore 状态）。各页在外层 `cm-page` 内用 `CmPullRefresh` 包裹现有内容，列表页额外接入 composable。

**Tech Stack:** uni-app + Vue 3 `<script setup>`、`@dcloudio/uni-app` 生命周期、现有 `fetchOrderList` 分页契约、`.cm-page` CSS 变量主题系统。本项目小程序端无单测框架，验证依赖微信开发者工具手工验证。

**相关 spec：** [Docs/specs/2026-04-21-小程序下拉刷新与上拉加载设计.md](../specs/2026-04-21-小程序下拉刷新与上拉加载设计.md)

---

## 范围说明与偏差

落地时对 spec 做一处调整，已在本计划中固化，执行人不要再改方向：

- **verify-record 页从"阶段二"降级为"阶段一"**：读源码发现 `pages/verify-record/index.vue` 当前全是硬编码 mock，后端未暴露分页接口。本计划只给它接入下拉刷新（和 tab 页同策略），不做上拉加载。当未来新增核销记录分页接口后，再补一个独立任务把它升级为 composable 模式。

---

## 文件结构

**新增**
- `miniapp/coupon-miniapp/components/CmPullRefresh.vue` — 通用容器组件
- `miniapp/coupon-miniapp/composables/use-list-pagination.js` — 列表分页状态机

**改动**
- `miniapp/coupon-miniapp/pages/index/index.vue` — 首页接入下拉刷新
- `miniapp/coupon-miniapp/pages/mall/index.vue` — 商城接入下拉刷新
- `miniapp/coupon-miniapp/pages/coupon/index.vue` — 卡包接入下拉刷新
- `miniapp/coupon-miniapp/pages/profile/index.vue` — 我的接入下拉刷新
- `miniapp/coupon-miniapp/pages/verify-record/index.vue` — 核销记录接入下拉刷新（仅模拟重载 mock）
- `miniapp/coupon-miniapp/pages/order/list.vue` — 订单列表接入下拉刷新 + 上拉加载

---

## Task 1: 新建 `CmPullRefresh` 组件

**Files:**
- Create: `miniapp/coupon-miniapp/components/CmPullRefresh.vue`

- [ ] **Step 1：写组件代码**

```vue
<template>
  <scroll-view
    class="cm-pull-refresh"
    scroll-y
    :refresher-enabled="true"
    refresher-default-style="none"
    :refresher-triggered="refreshing"
    :refresher-threshold="80"
    lower-threshold="80"
    :scroll-top="scrollTop"
    @refresherrefresh="handleRefresh"
    @refresherpulling="handlePulling"
    @refresherrestore="handleRestore"
    @scrolltolower="handleScrollToLower"
    @scroll="handleScroll"
  >
    <view slot="refresher" class="cm-pull-refresher">
      <image
        src="/static/logo.png"
        :class="['cm-pull-logo', refreshing ? 'is-spinning' : '']"
        mode="aspectFit"
      />
      <text class="cm-pull-text">{{ pullText }}</text>
    </view>

    <slot />

    <view v-if="showLoadMore" class="cm-pull-footer">
      <template v-if="loadingMore">
        <view class="cm-pull-dot"></view>
        <view class="cm-pull-dot"></view>
        <view class="cm-pull-dot"></view>
        <text class="cm-pull-footer-text">加载中…</text>
      </template>
      <text v-else-if="noMore" class="cm-pull-footer-text cm-pull-footer-text--muted">已到底啦</text>
    </view>
  </scroll-view>
</template>

<script setup>
import { ref } from 'vue'

const props = defineProps({
  refreshing: { type: Boolean, default: false },
  loadingMore: { type: Boolean, default: false },
  noMore: { type: Boolean, default: false },
  showLoadMore: { type: Boolean, default: false },
  scrollTop: { type: Number, default: 0 }
})

const emit = defineEmits(['refresh', 'loadmore', 'scroll'])

const pullDistance = ref(0)

const pullText = ref('下拉刷新')

function handlePulling(e) {
  const dy = e?.detail?.dy ?? 0
  pullDistance.value = dy
  if (props.refreshing) {
    pullText.value = '正在刷新…'
  } else if (dy >= 80) {
    pullText.value = '松手刷新'
  } else {
    pullText.value = '下拉刷新'
  }
}

function handleRefresh() {
  pullText.value = '正在刷新…'
  emit('refresh')
}

function handleRestore() {
  pullText.value = '下拉刷新'
  pullDistance.value = 0
}

function handleScrollToLower() {
  if (props.loadingMore || props.noMore || !props.showLoadMore) return
  emit('loadmore')
}

function handleScroll(e) {
  emit('scroll', e)
}
</script>

<style lang="scss" scoped>
.cm-pull-refresh {
  width: 100%;
  height: 100vh;
  box-sizing: border-box;
}

.cm-pull-refresher {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 120rpx;
  gap: 8rpx;
}

.cm-pull-logo {
  width: 48rpx;
  height: 48rpx;
  transition: transform 0.2s;
}

.cm-pull-logo.is-spinning {
  animation: cm-pull-spin 1s linear infinite;
}

.cm-pull-text {
  font-size: 22rpx;
  color: var(--cm-theme-primary-strong, #23493b);
}

.cm-pull-footer {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  height: 80rpx;
  padding: 0 24rpx;
}

.cm-pull-footer-text {
  font-size: 24rpx;
  color: var(--cm-theme-primary-strong, #23493b);
}

.cm-pull-footer-text--muted {
  color: var(--cm-theme-primary-soft, #6f8a6d);
}

.cm-pull-dot {
  width: 8rpx;
  height: 8rpx;
  border-radius: 50%;
  background: var(--cm-theme-primary-strong, #23493b);
  animation: cm-pull-dot-blink 1.2s infinite;
}

.cm-pull-dot:nth-child(2) { animation-delay: 0.2s; }
.cm-pull-dot:nth-child(3) { animation-delay: 0.4s; }

@keyframes cm-pull-spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

@keyframes cm-pull-dot-blink {
  0%, 80%, 100% { opacity: 0.3; }
  40% { opacity: 1; }
}
</style>
```

- [ ] **Step 2：提交**

```bash
git add miniapp/coupon-miniapp/components/CmPullRefresh.vue
git commit -m "feat(miniapp): add CmPullRefresh component for unified pull-refresh/load-more"
```

---

## Task 2: 新建 `use-list-pagination` composable

**Files:**
- Create: `miniapp/coupon-miniapp/composables/use-list-pagination.js`

- [ ] **Step 1：写 composable 代码**

```javascript
import { ref } from 'vue'

/**
 * 列表分页状态机。
 * @param {Function} fetcher - async ({ pageIndex, pageSize, ...extraQuery }) => { items, pageIndex, totalPages, totalCount }
 * @param {Object}   options
 * @param {number}   options.pageSize - 每页条数，默认 10
 * @param {Function} options.buildQuery - 可选，返回额外查询参数（如状态筛选），每次请求前调用
 */
export function useListPagination(fetcher, options = {}) {
  const pageSize = options.pageSize ?? 10
  const buildQuery = options.buildQuery ?? (() => ({}))

  const items = ref([])
  const refreshing = ref(false)
  const loadingMore = ref(false)
  const noMore = ref(false)
  const pageIndex = ref(1)
  const totalCount = ref(0)

  async function load(targetPage, mode) {
    const query = { ...buildQuery(), pageIndex: targetPage, pageSize }
    const result = await fetcher(query)
    const resultItems = Array.isArray(result?.items) ? result.items : []
    const totalPages = Number(result?.totalPages) || 1

    if (mode === 'refresh') {
      items.value = resultItems
    } else {
      items.value = items.value.concat(resultItems)
    }
    pageIndex.value = Number(result?.pageIndex) || targetPage
    totalCount.value = Number(result?.totalCount) || items.value.length
    noMore.value = pageIndex.value >= totalPages || resultItems.length === 0
  }

  async function onRefresh() {
    if (refreshing.value) return
    refreshing.value = true
    try {
      await load(1, 'refresh')
    } catch (error) {
      console.warn('[pagination] refresh failed', error)
      uni.showToast({ title: error?.message || '加载失败', icon: 'none' })
    } finally {
      refreshing.value = false
    }
  }

  async function onLoadMore() {
    if (loadingMore.value || noMore.value || refreshing.value) return
    loadingMore.value = true
    const nextPage = pageIndex.value + 1
    try {
      await load(nextPage, 'append')
    } catch (error) {
      console.warn('[pagination] load more failed', error)
      uni.showToast({ title: error?.message || '加载失败', icon: 'none' })
    } finally {
      loadingMore.value = false
    }
  }

  return {
    items,
    refreshing,
    loadingMore,
    noMore,
    pageIndex,
    pageSize,
    totalCount,
    onRefresh,
    onLoadMore,
    reload: onRefresh
  }
}
```

- [ ] **Step 2：提交**

```bash
git add miniapp/coupon-miniapp/composables/use-list-pagination.js
git commit -m "feat(miniapp): add use-list-pagination composable for paged lists"
```

---

## Task 3: 首页接入下拉刷新

**Files:**
- Modify: `miniapp/coupon-miniapp/pages/index/index.vue`

- [ ] **Step 1：在 `<template>` 最外层替换结构**

把原本的：
```
<view :class="['cm-page', themeClass]">
  <!-- 所有现有内容 -->
</view>
```

改成：
```
<view :class="['cm-page', themeClass]">
  <CmPullRefresh :refreshing="refreshing" @refresh="handleRefresh">
    <!-- 所有现有内容 -->
  </CmPullRefresh>
</view>
```

- [ ] **Step 2：在 `<script setup>` 顶部追加 import 与状态**

找到 import 区块末尾，追加：
```javascript
import CmPullRefresh from '@/components/CmPullRefresh.vue'
```

找到现有的响应式状态声明处（靠近 `const { themeClass } = useTheme()`），追加（该页实际函数名 `loadHomeData`，位于第 175 行）：
```javascript
const refreshing = ref(false)

async function handleRefresh() {
  if (refreshing.value) return
  refreshing.value = true
  try {
    await loadHomeData()
  } finally {
    refreshing.value = false
  }
}
```

- [ ] **Step 3：微信开发者工具手动验证**

运行 `npm run dev:mp-weixin`（或项目 README 指定命令），打开首页：
1. 下拉松手 → logo 开始旋转，数据重新加载，加载完成后 logo 停止，refresher 回收
2. 断网（DevTools → Network → Offline）再下拉 → 旋转停止、toast 提示"加载失败"、页面不卡死

- [ ] **Step 4：提交**

```bash
git add miniapp/coupon-miniapp/pages/index/index.vue
git commit -m "feat(miniapp): wire pull-refresh on home tab"
```

---

## Task 4: 商城页接入下拉刷新

**Files:**
- Modify: `miniapp/coupon-miniapp/pages/mall/index.vue`

- [ ] **Step 1：改模板**

与 Task 3 Step 1 结构完全一致——外层 `cm-page` 内用 `<CmPullRefresh :refreshing="refreshing" @refresh="handleRefresh">` 包住原内容。

- [ ] **Step 2：改脚本**

import：
```javascript
import CmPullRefresh from '@/components/CmPullRefresh.vue'
```

state + handler（该页实际函数名 `loadMallData`）：
```javascript
const refreshing = ref(false)

async function handleRefresh() {
  if (refreshing.value) return
  refreshing.value = true
  try {
    await loadMallData()
  } finally {
    refreshing.value = false
  }
}
```

- [ ] **Step 3：手动验证**

开发者工具切到商城 tab：
1. 下拉 → 商品/券包列表重新拉取
2. 断网下拉 → toast"加载失败"、状态回收

- [ ] **Step 4：提交**

```bash
git add miniapp/coupon-miniapp/pages/mall/index.vue
git commit -m "feat(miniapp): wire pull-refresh on mall tab"
```

---

## Task 5: 卡包页接入下拉刷新

**Files:**
- Modify: `miniapp/coupon-miniapp/pages/coupon/index.vue`

- [ ] **Step 1：改模板**

与 Task 3 Step 1 结构一致。

- [ ] **Step 2：改脚本**

import：
```javascript
import CmPullRefresh from '@/components/CmPullRefresh.vue'
```

state + handler（该页实际函数名 `loadCoupons`，位于第 117 行）：
```javascript
const refreshing = ref(false)

async function handleRefresh() {
  if (refreshing.value) return
  refreshing.value = true
  try {
    await loadCoupons()
  } finally {
    refreshing.value = false
  }
}
```

- [ ] **Step 3：手动验证**

开发者工具切到卡包 tab：
1. 下拉 → 我的券包 + 单张券 重新加载
2. 断网下拉 → toast 报错、不卡

- [ ] **Step 4：提交**

```bash
git add miniapp/coupon-miniapp/pages/coupon/index.vue
git commit -m "feat(miniapp): wire pull-refresh on coupon tab"
```

---

## Task 6: 我的页接入下拉刷新（scaffold only）

> ⚠️ `pages/profile/index.vue` 当前只从 `session` store 读取数据，**没有任何 API 调用**。本任务只铺设基础设施，下拉刷新执行 700ms 占位延时 + toast 提示，等未来接入"拉取用户统计"接口时替换。

**Files:**
- Modify: `miniapp/coupon-miniapp/pages/profile/index.vue`

- [ ] **Step 1：改模板**

与 Task 3 Step 1 结构一致。

- [ ] **Step 2：改脚本**

import：
```javascript
import CmPullRefresh from '@/components/CmPullRefresh.vue'
```

state + handler（无 API，占位实现）：
```javascript
const refreshing = ref(false)

async function handleRefresh() {
  if (refreshing.value) return
  refreshing.value = true
  await new Promise((resolve) => setTimeout(resolve, 700))
  uni.showToast({ title: '已为您刷新', icon: 'none' })
  refreshing.value = false
}
```

- [ ] **Step 3：手动验证**

开发者工具切到我的 tab：
1. 下拉 → logo 旋转 ≈0.7s → toast"已为您刷新" → refresher 收起
2. 页面滚动顺畅

- [ ] **Step 4：提交**

```bash
git add miniapp/coupon-miniapp/pages/profile/index.vue
git commit -m "feat(miniapp): wire pull-refresh on profile tab"
```

---

## Task 7: 核销记录页接入下拉刷新（仅重载 mock）

> 当前 `pages/verify-record/index.vue` 使用硬编码的 `records` 常量，无 API。本任务只把刷新基础设施铺上，刷新时重置滚动/触发一次"伪刷新"提示，等后端分页接口到位后再改造。

**Files:**
- Modify: `miniapp/coupon-miniapp/pages/verify-record/index.vue`

- [ ] **Step 1：改模板**

外层改为：
```
<view :class="['cm-page', themeClass]">
  <CmPullRefresh :refreshing="refreshing" @refresh="handleRefresh">
    <view class="cm-container">
      <view class="cm-nav-spacer"></view>
      <!-- 原有 hero、stats、filter、timeline 所有内容 -->
    </view>
  </CmPullRefresh>
</view>
```

> ⚠️ 原页面 `cm-page` 同时带 `cm-container` 类。迁移时把 `cm-container` 下放到内部 wrapper，避免 `scroll-view` 被挤压。`cm-nav-spacer` 必须放在滚动内容里，否则顶部被导航胶囊遮挡。

- [ ] **Step 2：改脚本**

找到 `<script setup>`，在 import 区末尾追加：
```javascript
import CmPullRefresh from '@/components/CmPullRefresh.vue'
```

state + handler（无 API，仅模拟）：
```javascript
const refreshing = ref(false)

async function handleRefresh() {
  if (refreshing.value) return
  refreshing.value = true
  await new Promise((resolve) => setTimeout(resolve, 600))
  uni.showToast({ title: '已为您刷新', icon: 'none' })
  refreshing.value = false
}
```

- [ ] **Step 3：手动验证**

1. 下拉 → logo 旋转 ≈0.6s → toast"已为您刷新" → refresher 收起
2. 页面滚动顺畅，不会被 scroll-view 高度限制卡住
3. 顶部"VERIFY RECORD"标题完整可见，未被胶囊遮挡

- [ ] **Step 4：提交**

```bash
git add miniapp/coupon-miniapp/pages/verify-record/index.vue
git commit -m "feat(miniapp): wire pull-refresh scaffold on verify-record page"
```

---

## Task 8: 订单列表接入下拉刷新 + 上拉加载

> 这是唯一接入完整分页的页面。现有 `loadOrders` 硬写 `pageSize: 50` 且无上拉加载。本任务把它整体迁移到 `use-list-pagination`。

**Files:**
- Modify: `miniapp/coupon-miniapp/pages/order/list.vue`

- [ ] **Step 1：改模板外层**

把原模板最外层的 `<view :class="['cm-page', themeClass]"> … </view>` 内容整体用 `<CmPullRefresh>` 包住：

```
<view :class="['cm-page', themeClass]">
  <CmPullRefresh
    :refreshing="refreshing"
    :loading-more="loadingMore"
    :no-more="noMore"
    :show-load-more="true"
    @refresh="onRefresh"
    @loadmore="onLoadMore"
  >
    <!-- 原有 order-hero + order-body 全部内容 -->
  </CmPullRefresh>
</view>
```

- [ ] **Step 2：改脚本 import 与状态**

替换 `<script setup>` 中原本的 `const orders = ref([])` 与 `loadOrders` 相关代码。

新 import（在现有 import 末尾追加）：
```javascript
import CmPullRefresh from '@/components/CmPullRefresh.vue'
import { useListPagination } from '@/composables/use-list-pagination'
```

删除：
```javascript
const orders = ref([])
const loadOrders = async () => { ... }
```

替换为：
```javascript
async function ordersFetcher(query) {
  await ensureMiniProgramLogin()
  const ready = await ensurePhoneReady({
    force: true,
    redirect: '/pages/order/list'
  })
  if (!ready) {
    return { items: [], pageIndex: 1, totalPages: 1, totalCount: 0 }
  }
  return fetchOrderList(query)
}

const {
  items: orders,
  refreshing,
  loadingMore,
  noMore,
  onRefresh,
  onLoadMore,
  reload
} = useListPagination(ordersFetcher, { pageSize: 10 })
```

- [ ] **Step 3：改脚本 - 生命周期**

原来的：
```javascript
onShow(() => {
  loadOrders()
})
```

改成：
```javascript
onShow(() => {
  reload()
})
```

- [ ] **Step 4：确认下游 computed 兼容**

`filteredOrders` / `statusTabs` / `overviewStats` 等 computed 之前都依赖 `orders.value`。由于 composable 解构时已将 `items` 重命名为 `orders`，**这些 computed 无需任何修改**。打开脚本肉眼核对一遍所有引用 `orders` 的位置，确认引用没有断。

- [ ] **Step 5：手动验证 - 下拉刷新**

微信开发者工具打开订单列表页：
1. 下拉 → 回到第一页，`orders.length` ≈ 10
2. 断网下拉 → toast"加载失败"、refreshing 状态回收

- [ ] **Step 6：手动验证 - 上拉加载**

(前提：后端有 > 10 条订单。若 mock 环境只有 3 条，需先往数据库塞多于 10 条订单，或暂时把 `pageSize` 改小到 2 再测完恢复。)

1. 滚到底部 → "加载中…" 出现约 0.5s → 列表增加第二页
2. 继续到底 → 最后一页 → "已到底啦"
3. 再下拉 → 回到第一页，`noMore=false`，再次可上拉

- [ ] **Step 7：手动验证 - 分状态切换**

点击状态 tab（全部 / 待付款 / 已支付 / 已完成）：
- 当前实现是前端过滤（`filteredOrders`），切换 tab 不会触发接口，这是预期行为
- 切 tab 不影响已加载的 `orders`

- [ ] **Step 8：提交**

```bash
git add miniapp/coupon-miniapp/pages/order/list.vue
git commit -m "feat(miniapp): wire pull-refresh + infinite load on order list"
```

---

## Task 9: 全量回归手动验证

**Files:** 无（纯手工）

- [ ] **Step 1：清缓存重新编译**

```bash
cd miniapp/coupon-miniapp
rm -rf unpackage/dist/dev/mp-weixin
npm run dev:mp-weixin
```

开发者工具点"清除全部缓存"后重新编译。

- [ ] **Step 2：6 个页面轮询**

依次访问 index / mall / coupon / profile / verify-record / order-list：
- 每页都能下拉刷新，logo 旋转顺畅
- 只有 order-list 有"加载中 / 已到底啦"的上拉加载
- 其余 5 页没有底部加载条

- [ ] **Step 3：主题切换验证**

进入设置切换主题（light / candy / orange / red / default），每个主题下：
- 下拉文案颜色使用主题色（不是固定的墨绿 `#23493b`）
- 上拉"加载中…"的圆点也跟随主题色

- [ ] **Step 4：真机验证（iOS + Android 各一）**

- 真机下拉阻尼感正常，无卡顿
- 回到顶部 / 滚到底部触发一致
- 顶部胶囊与 `cm-nav-spacer` 没有遮挡

- [ ] **Step 5：若全部通过，补提交一次工作日志**

在 `Docs/2026-04-21-工作.md` 追加一段：
```markdown
## 小程序下拉刷新 + 上拉加载
- 新增 CmPullRefresh 组件、use-list-pagination composable
- 接入：index / mall / coupon / profile / verify-record（仅刷新）、order/list（刷新+分页）
- verify-record 上拉加载待后端分页接口后补齐
```

```bash
git add Docs/2026-04-21-工作.md
git commit -m "docs: log miniapp pull-refresh rollout"
```

---

## 后续（不在本次范围）

- 核销记录后端分页接口：新增 `GET /api/miniapp/users/write-off-records?pageIndex=&pageSize=`，返回标准分页结构，然后把 `verify-record/index.vue` 升级为 `use-list-pagination` 模式
- 刷新失败重试 UI（现只 toast）
- tab 切换时保留 `scrollTop`（接入 pinia session store）
- 骨架屏 / 首次加载占位
