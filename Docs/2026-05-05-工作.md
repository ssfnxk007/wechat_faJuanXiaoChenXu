# 2026-05-05 工作

- 变更范围 / 目的：为服务器上线准备前后端生产发布包，统一后台管理端生产 API 指向正式接口域名 `https://xcxapi.bookso.cn/api`，避免依赖同域反向代理才能访问后台接口。
- 关键文件：`frontend/.env.production`
- 验证情况：已执行 `dotnet publish backend/src/FaJuan.Api/FaJuan.Api.csproj -c Release -o release/2026-05-05/backend-api` 与 `frontend: npm run build`；发布产物已整理至 `release/2026-05-05/backend-api`、`release/2026-05-05/frontend-admin`，并额外生成 `backend-api.zip`、`frontend-admin.zip`。
- 后续事项 / 风险：正式上线前仍需在服务器环境填写生产数据库、JWT、ERP API Key、小程序 `AppSecret`、微信支付 `APIv3 Key` 等敏感配置；后台支付配置需关闭模拟支付回退后再做真实支付验收。
