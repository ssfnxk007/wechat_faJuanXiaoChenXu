# 2026-04-09 工作

- 变更范围 / 目的：初始化 `backend` 与 `frontend` 基础脚手架，先行推进后台管理端与 API 开发。
- 关键文件：`backend/`、`frontend/`、`README.md`、`database/README.md`
- 验证情况：已完成后端测试通过、后端解决方案构建通过、前端生产构建通过。
- 后续事项 / 风险：小程序 UI 与页面实现暂缓；多 Agent 子任务调度遇到瞬时 502，后续可在基础骨架上继续并行细化业务模块。
- 变更范围 / 目的：补充第一批业务模块骨架，新增门店、商品、券模板的后端实体与管理接口，并为后台接入对应管理页面。
- 关键文件：`backend/src/FaJuan.Api/Infrastructure/Persistence/AppDbContext.cs`、`backend/src/FaJuan.Api/Controllers/StoresController.cs`、`backend/src/FaJuan.Api/Controllers/ProductsController.cs`、`backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs`、`frontend/src/views/store/StoreView.vue`、`frontend/src/views/product/ProductView.vue`、`frontend/src/views/coupon-template/CouponTemplateView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`dotnet test backend/tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj -c Release --no-build`、`npm run build`。
- 后续事项 / 风险：当前为首版业务骨架，尚未接入鉴权、数据库迁移、编辑/删除、查询条件和字典枚举展示优化；下一步可继续做用户登录/手机号绑定与券包模块。
- 变更范围 / 目的：继续补充登录与券包主线，新增小程序模拟登录/手机号绑定、用户列表、券包管理与订单基础下单接口，并在后台接入对应页面。
- 关键文件：`backend/src/FaJuan.Api/Controllers/AuthController.cs`、`backend/src/FaJuan.Api/Controllers/UsersController.cs`、`backend/src/FaJuan.Api/Controllers/CouponPacksController.cs`、`backend/src/FaJuan.Api/Controllers/CouponOrdersController.cs`、`frontend/src/views/user/UserView.vue`、`frontend/src/views/coupon-pack/CouponPackView.vue`、`frontend/src/views/order/CouponOrderView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`dotnet test backend/tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj -c Release --no-build`、`npm run build`。
- 后续事项 / 风险：当前登录为开发期模拟逻辑，后续需接真实微信 `code2Session`；券包仅完成基础资料和订单创建，尚未接明细配置、支付回调与发券幂等。
- 变更范围 / 目的：补充支付与发券主链路骨架，新增支付流水、用户券实例、支付创建/回调接口，以及订单支付成功后的自动发券服务；后台订单页支持模拟支付完成联调。
- 关键文件：`backend/src/FaJuan.Api/Controllers/PaymentsController.cs`、`backend/src/FaJuan.Api/Application/Orders/OrderPaymentService.cs`、`backend/src/FaJuan.Api/Domain/Entities/UserCoupon.cs`、`backend/src/FaJuan.Api/Domain/Entities/PaymentTransaction.cs`、`frontend/src/views/order/CouponOrderView.vue`、`frontend/src/api/payment.ts`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`dotnet test backend/tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj -c Release --no-build`、`npm run build`。
- 后续事项 / 风险：当前支付仍为开发期模拟支付链路；券包若未配置明细则不会实际发券，后续需补券包明细管理、真实微信支付统一下单与回调签名校验。
- 变更范围 / 目的：补齐券包明细、用户券与核销链路，新增券包明细配置接口、用户券列表接口、核销接口与核销记录模型；后台新增券包明细、用户券、核销模拟页面。
- 关键文件：`backend/src/FaJuan.Api/Controllers/CouponPackItemsController.cs`、`backend/src/FaJuan.Api/Controllers/UserCouponsController.cs`、`backend/src/FaJuan.Api/Controllers/WriteOffController.cs`、`backend/src/FaJuan.Api/Domain/Entities/CouponWriteOffRecord.cs`、`frontend/src/views/coupon-pack/CouponPackItemView.vue`、`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`dotnet test backend/tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj -c Release --no-build`、`npm run build`。
- 后续事项 / 风险：当前核销未校验券模板适用门店与指定商品范围；券包明细仍需更友好的下拉选择与删除能力；下一步建议接 EF 迁移、真实微信登录/支付与菜单权限。
- 变更范围 / 目的：接入后台 JWT 鉴权与登录流程，新增后台登录接口、JWT Token 服务、前端 token 存储/路由守卫/请求拦截，并补充 EF Core 迁移与 SQL 导出命令说明。
- 关键文件：`backend/src/FaJuan.Api/Controllers/AdminAuthController.cs`、`backend/src/FaJuan.Api/Infrastructure/Auth/JwtTokenService.cs`、`backend/src/FaJuan.Api/Program.cs`、`frontend/src/api/admin-auth.ts`、`frontend/src/api/http.ts`、`frontend/src/views/login/LoginView.vue`、`frontend/src/router/index.ts`、`backend/README.md`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`dotnet test backend/tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj -c Release --no-build`、`npm run build`。
- 后续事项 / 风险：后台账号当前仍为配置文件静态账号，后续可切换为数据库管理员与角色权限；EF 迁移命令已给出，但尚未正式生成首个迁移文件与 SQL 脚本。
- 变更范围 / 目的：生成 EF Core 首个迁移与初版建表 SQL，正式落地当前 Code First 数据模型。
- 关键文件：`backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/20260408175041_InitialCreate.cs`、`backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs`、`database/migrations/001-initial.sql`、`database/migrations/README.md`
- 验证情况：已成功执行 `dotnet dotnet-ef migrations add InitialCreate`、`dotnet dotnet-ef migrations script`，并检查 SQL 输出。
- 后续事项 / 风险：当前迁移仍基于配置文件静态后台账号；后续若引入管理员表、菜单权限、适用门店关系表，需要继续补迁移与 SQL。
- 变更范围 / 目的：在本机默认 SQL Server 实例上实际创建 `FaJuanDb` 数据库，并执行首版初始化 SQL，完成基础表结构落库。
- 关键文件：`database/migrations/001-initial.sql`
- 验证情况：已通过 `sqlcmd` 创建数据库，并核验 `AppUser`、`Store`、`Product`、`CouponTemplate`、`CouponPack`、`CouponPackItem`、`CouponOrder`、`UserCoupon`、`PaymentTransaction`、`CouponWriteOffRecord`、`__EFMigrationsHistory` 共 11 张表存在。
- 后续事项 / 风险：当前数据库仅有基础业务表，尚未插入后台菜单、权限、管理员表或字典初始化数据；后续真实微信配置仍需补齐。
- 变更范围 / 目的：接入真实微信小程序登录骨架，新增 `code2Session` 服务封装、微信配置状态检查接口、真实 `mini-login` 实现与联调文档。
- 关键文件：`backend/src/FaJuan.Api/Infrastructure/WeChat/WeChatMiniProgramService.cs`、`backend/src/FaJuan.Api/Controllers/AuthController.cs`、`backend/src/FaJuan.Api/appsettings.json`、`backend/src/FaJuan.Api/appsettings.Development.json`、`Docs/api/2026-04-09-微信小程序登录联调说明.md`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`dotnet test backend/tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj -c Release --no-build`、`npm run build`。
- 后续事项 / 风险：要完成真实联调，仍需你提供并填写真实微信小程序 `AppId` 与 `AppSecret`；小程序前端 `wx.login()` 页面逻辑将在后续 `miniapp/` 开发时接入。
- 变更范围 / 目的：接入真实微信支付骨架，新增微信支付配置、JSAPI 下单签名服务、支付配置状态检查接口，并将支付创建逻辑升级为“真实微信支付优先、未配置时可回退模拟支付”。
- 关键文件：`backend/src/FaJuan.Api/Infrastructure/WeChatPay/WeChatPayService.cs`、`backend/src/FaJuan.Api/Controllers/PaymentsController.cs`、`backend/src/FaJuan.Api/appsettings.json`、`backend/src/FaJuan.Api/appsettings.Development.json`、`Docs/api/2026-04-09-微信支付接入说明.md`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`dotnet test backend/tests/FaJuan.Api.Tests/FaJuan.Api.Tests.csproj -c Release --no-build`、`npm run build`。
- 后续事项 / 风险：当前回调已具备报文解密入口，但尚未接入微信支付平台证书验签；要完成真实支付联调，还需你提供商户号、商户证书序列号、私钥 PEM 路径、APIv3 Key 和公网回调地址。
- 变更范围 / 目的：新增后台权限表并切换后台登录为数据库优先认证，完成管理员、角色、菜单、用户角色、角色菜单表的迁移、落库与初始化数据。
- 关键文件：`backend/src/FaJuan.Api/Domain/Entities/AdminUser.cs`、`backend/src/FaJuan.Api/Domain/Entities/AdminRole.cs`、`backend/src/FaJuan.Api/Domain/Entities/AdminMenu.cs`、`backend/src/FaJuan.Api/Controllers/AdminAuthController.cs`、`backend/src/FaJuan.Api/Infrastructure/Persistence/Migrations`、`database/migrations/002-admin-auth.sql`、`database/migrations/002-admin-auth-seed.sql`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`dotnet dotnet-ef migrations add AddAdminAuthTables`、`dotnet dotnet-ef migrations script ... -o database/migrations/002-admin-auth.sql`、`sqlcmd` 执行迁移及初始化数据，并确认默认管理员/角色/菜单存在。
- 后续事项 / 风险：当前前端菜单仍为静态路由展示，尚未从数据库动态读取；后续如需细粒度按钮权限，可继续扩展权限点表或菜单权限编码。

- 变更范围 / 目的：补充后台管理端静态 UI 原型，输出首版 HTML 预览稿，便于先确认后台导航结构、信息密度与业务区块布局风格。
- 关键文件：`Docs/ui/admin-preview-v1.html`
- 验证情况：已检查 HTML 文件生成成功，可直接本地打开预览。
- 后续事项 / 风险：当前仅为静态视觉原型，尚未迁移到 `frontend/` 的 Vue 页面；待你确认风格后再按模块拆成真实后台页面。

- 变更范围 / 目的：将后台静态 HTML 原型正式迁移到 `frontend/`，统一后台登录页、整体布局、仪表盘和核心业务页的视觉风格。
- 关键文件：`frontend/src/style.css`、`frontend/src/layouts/AdminLayout.vue`、`frontend/src/router/index.ts`、`frontend/src/views/login/LoginView.vue`、`frontend/src/views/dashboard/DashboardView.vue`、`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/coupon-pack/CouponPackView.vue`、`frontend/src/views/order/CouponOrderView.vue`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：当前主要统一了后台骨架与核心页面，其他业务页仍可继续补状态标签、统计区块与更细的交互体验；暂未引入新的组件体系，后续可视反馈后再做第二轮细化。

- 变更范围 / 目的：继续完成后台 UI 第二轮统一，补齐用户、门店、商品、券包明细、用户券、核销页的统一业务后台样式。
- 关键文件：`frontend/src/views/user/UserView.vue`、`frontend/src/views/store/StoreView.vue`、`frontend/src/views/product/ProductView.vue`、`frontend/src/views/coupon-pack/CouponPackItemView.vue`、`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`、`frontend/src/style.css`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：当前已形成较完整的后台视觉统一稿，但仍属于轻量样式层改造；如需更强的筛选区、分页、弹窗编辑、统计图和按钮权限态，可继续做第三轮细化。

- 变更范围 / 目的：完成后台 UI 第三轮细化，为核心管理页补充本地筛选区、工具栏摘要、结果计数与更正式的列表操作体验。
- 关键文件：`frontend/src/views/user/UserView.vue`、`frontend/src/views/store/StoreView.vue`、`frontend/src/views/product/ProductView.vue`、`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/coupon-pack/CouponPackView.vue`、`frontend/src/views/order/CouponOrderView.vue`、`frontend/src/style.css`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：当前筛选为前端本地筛选，后续若数据量上升，建议补后端查询参数与服务端分页；弹窗编辑、删除、分页组件、图表总览仍可继续做第四轮完善。

- 变更范围 / 目的：完成后台 UI 第四轮，将门店、商品、券模板、券包四个核心 CRUD 页面接成正式管理交互，支持弹窗新增/编辑、删除确认和服务端分页查询。
- 关键文件：`frontend/src/views/store/StoreView.vue`、`frontend/src/views/product/ProductView.vue`、`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/coupon-pack/CouponPackView.vue`、`frontend/src/api/store.ts`、`frontend/src/api/product.ts`、`frontend/src/api/coupon-template.ts`、`frontend/src/api/coupon-pack.ts`、`frontend/src/style.css`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`。
- 后续事项 / 风险：订单、用户券、券包明细、核销页仍以查询/操作为主，尚未扩展为完整编辑删除流；若继续增强，建议下一轮做统一消息提示、删除失败提示、日期选择优化和动态权限显隐。

- 变更范围 / 目的：继续完善交易链路后台页面，升级订单、用户券、核销、券包明细页面为正式后台操作页，并补齐用户券服务端分页查询。
- 关键文件：`frontend/src/views/order/CouponOrderView.vue`、`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`、`frontend/src/views/coupon-pack/CouponPackItemView.vue`、`frontend/src/api/user-coupon.ts`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：订单当前仍为创建+模拟支付链路，未扩展编辑删除；用户券与核销以查询和执行为主，后续可继续补失败提示、成功提示、复制券码、导出与二维码展示。

- 变更范围 / 目的：补充后台统一消息提示能力，并为用户券页增加券码二维码预览与复制能力，提升联调与核销展示体验。
- 关键文件：`frontend/src/utils/notify.ts`、`frontend/src/utils/http-error.ts`、`frontend/src/components/AppToast.vue`、`frontend/src/App.vue`、`frontend/src/views/user-coupon/UserCouponView.vue`、`frontend/src/views/writeoff/WriteOffView.vue`、`frontend/src/views/order/CouponOrderView.vue`、`frontend/package.json`
- 验证情况：已完成 `npm install qrcode`、`npm install -D @types/qrcode`、`npm run build`。
- 后续事项 / 风险：当前统一提示已接入高频交易页与核销页，其他 CRUD 页面仍可继续逐步接入；二维码当前展示的是券码本身，后续如要和小程序用户端一致，可继续统一二维码载荷格式与详情弹窗信息。

- 变更范围 / 目的：按用户确认继续补充用户券“券详情弹窗”，在列表中可直接查看状态、时间范围与券码信息，并联动二维码查看。
- 关键文件：`frontend/src/views/user-coupon/UserCouponView.vue`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：当前详情弹窗展示的是用户券实例字段，若后续要展示更多模板业务信息（如券类型、优惠金额、适用门店），需要补用户券详情聚合接口或联查模板数据。

- 变更范围 / 目的：继续完善用户券详情弹窗，新增后端用户券聚合详情接口，补充模板名称、券类型、有效期规则、优惠金额、门槛金额、适用门店、是否新人券等业务信息。
- 关键文件：`backend/src/FaJuan.Api/Contracts/UserCouponDtos.cs`、`backend/src/FaJuan.Api/Controllers/UserCouponsController.cs`、`frontend/src/types/user-coupon.ts`、`frontend/src/api/user-coupon.ts`、`frontend/src/views/user-coupon/UserCouponView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`。
- 后续事项 / 风险：当前详情仍为单券查看，如后续要支持门店明细、指定商品明细或核销记录轨迹，需要继续补详情聚合内容或单独的用户券历史接口。

- 变更范围 / 目的：继续补齐用户券详情的核销轨迹能力，新增按用户券查询核销记录接口，并在详情弹窗中展示核销时间、门店、操作人、设备号与券码。
- 关键文件：`backend/src/FaJuan.Api/Contracts/UserCouponDtos.cs`、`backend/src/FaJuan.Api/Controllers/UserCouponsController.cs`、`frontend/src/types/user-coupon.ts`、`frontend/src/api/user-coupon.ts`、`frontend/src/views/user-coupon/UserCouponView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`。
- 后续事项 / 风险：当前核销轨迹为用户券维度的记录明细，后续如需更完整审计，可继续补核销来源渠道、关联订单号、门店编码及操作结果快照字段展示。

- 变更范围 / 目的：完成订单详情弹窗，新增后端订单聚合详情接口，并在后台展示订单基础信息、支付流水与发券结果，串起订单到支付再到发券的闭环。
- 关键文件：`backend/src/FaJuan.Api/Contracts/CouponPackDtos.cs`、`backend/src/FaJuan.Api/Controllers/CouponOrdersController.cs`、`frontend/src/types/coupon-pack.ts`、`frontend/src/api/coupon-pack.ts`、`frontend/src/views/order/CouponOrderView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`。
- 后续事项 / 风险：当前订单详情已展示支付流水与发券结果，但尚未汇总支付回调原文、券模板名称、失败支付重试记录；后续如需实施排查，可继续扩展为更完整的交易追踪面板。

- 变更范围 / 目的：按用户要求继续为后台页面补齐统一成功/失败提示，覆盖门店、商品、券模板、券包、券包明细、用户管理等高频操作页。
- 关键文件：`frontend/src/views/store/StoreView.vue`、`frontend/src/views/product/ProductView.vue`、`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/coupon-pack/CouponPackView.vue`、`frontend/src/views/coupon-pack/CouponPackItemView.vue`、`frontend/src/views/user/UserView.vue`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：当前提示已覆盖主要后台操作页，后续仍可继续把登录页、更多查询动作、复制动作和复杂批量操作统一接入更细致的提示文案与节流机制。

- 变更范围 / 目的：继续收口后台统一成功/失败提示，补齐登录页与看板健康检查页的统一消息提示，完成后台页面提示覆盖收尾。
- 关键文件：`frontend/src/views/login/LoginView.vue`、`frontend/src/views/dashboard/DashboardView.vue`
- 验证情况：已完成 `npm run build`。
- 后续事项 / 风险：登录页当前仍保留页内错误文案并新增 toast 提示；看板页仅在健康检查失败时提示，避免进入首页即产生过多成功提示噪音。

- 变更范围 / 目的：补充订单详情“发券结果”展示内容，在后台订单详情弹窗中显示每张已发用户券对应的券模板名称，便于联调和运营核对发券结果。
- 关键文件：`backend/src/FaJuan.Api/Contracts/CouponPackDtos.cs`、`backend/src/FaJuan.Api/Controllers/CouponOrdersController.cs`、`frontend/src/types/coupon-pack.ts`、`frontend/src/views/order/CouponOrderView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`。
- 后续事项 / 风险：当前发券结果已能展示模板名称；如后续还要更直观，可继续补券类型、优惠值、适用门店或商品范围到订单详情弹窗。

- 变更范围 / 目的：继续增强订单详情“发券结果”展示，补充券类型、优惠信息、适用门店摘要等业务字段，便于运营和联调直接核对每张已发券的业务规则。
- 关键文件：`backend/src/FaJuan.Api/Contracts/CouponPackDtos.cs`、`backend/src/FaJuan.Api/Controllers/CouponOrdersController.cs`、`frontend/src/types/coupon-pack.ts`、`frontend/src/views/order/CouponOrderView.vue`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`。
- 后续事项 / 风险：当前适用范围摘要仅展示“全部门店/指定门店”，因现阶段尚无模板-门店/商品明细关联表；如后续补充关联配置，可继续展示具体门店名和商品名。

- 变更范围 / 目的：正式落地后台 RBAC 权限模块，补充管理员、角色、菜单三组管理接口与后台页面，并修复后台权限初始化 SQL 中默认管理员密码哈希与新页面菜单初始化问题。
- 关键文件：`backend/src/FaJuan.Api/Contracts/AdminPermissionDtos.cs`、`backend/src/FaJuan.Api/Controllers/AdminUsersController.cs`、`backend/src/FaJuan.Api/Controllers/AdminRolesController.cs`、`backend/src/FaJuan.Api/Controllers/AdminMenusController.cs`、`backend/src/FaJuan.Api/Controllers/AdminAuthController.cs`、`frontend/src/api/admin-permission.ts`、`frontend/src/types/admin-permission.ts`、`frontend/src/views/admin-user/AdminUserView.vue`、`frontend/src/views/admin-role/AdminRoleView.vue`、`frontend/src/views/admin-menu/AdminMenuView.vue`、`frontend/src/router/index.ts`、`frontend/src/layouts/AdminLayout.vue`、`database/migrations/002-admin-auth-seed.sql`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`。
- 后续事项 / 风险：当前已完成 RBAC 管理页与关系落库，但业务控制器仍是统一 `[Authorize]`，尚未细化到按菜单/权限点做接口级拦截；下一轮可继续补“按角色动态菜单显示 + 接口权限校验”。

- 变更范围 / 目的：让后台 RBAC 真正生效，新增菜单授权过滤器，核心业务控制器按菜单路径做访问校验；前端登录后拉取管理员资料并按授权菜单动态显示侧边栏，同时路由无权限自动拦截跳转。
- 关键文件：`backend/src/FaJuan.Api/Infrastructure/Auth/AdminMenuAuthorizeAttribute.cs`、`backend/src/FaJuan.Api/Infrastructure/Auth/AdminMenuAuthorizeFilter.cs`、`backend/src/FaJuan.Api/Controllers/UsersController.cs`、`backend/src/FaJuan.Api/Controllers/StoresController.cs`、`backend/src/FaJuan.Api/Controllers/ProductsController.cs`、`backend/src/FaJuan.Api/Controllers/CouponTemplatesController.cs`、`backend/src/FaJuan.Api/Controllers/CouponPacksController.cs`、`backend/src/FaJuan.Api/Controllers/CouponPackItemsController.cs`、`backend/src/FaJuan.Api/Controllers/CouponOrdersController.cs`、`backend/src/FaJuan.Api/Controllers/UserCouponsController.cs`、`backend/src/FaJuan.Api/Controllers/WriteOffController.cs`、`backend/src/FaJuan.Api/Controllers/PaymentsController.cs`、`frontend/src/utils.auth.ts`、`frontend/src/views/login/LoginView.vue`、`frontend/src/layouts/AdminLayout.vue`、`frontend/src/router/index.ts`
- 验证情况：已完成 `dotnet build backend/FaJuan.sln -c Release -m:1`、`npm run build`。
- 后续事项 / 风险：当前权限粒度已达到“按菜单页面控制接口访问”，但还未拆到更细的按钮级权限点；如后续需要更细粒度控制，可继续扩展 `AdminMenu` 或新增 `AdminPermission` 维度。

- 变更范围 / 目的：根据协作反馈补充仓库代理协作规则，明确非简单任务优先使用多 Agent 并行，并要求长任务主动回报进度，避免长时间无反馈造成误判卡死。
- 关键文件：`AGENTS.md`
- 验证情况：文档规则更新，无需构建。
- 后续事项 / 风险：后续将按该规则优先拆分前后端、验证、文档等并行子任务，并在长任务中主动同步阶段进度。
- 变更范围 / 目的：现场排查并收口后台按钮级权限，补齐后台页面按钮显隐、权限点初始化 SQL，并将按钮权限正式落到本机 `FaJuanDb`。
- 关键文件：`frontend/src/views/admin-user/AdminUserView.vue`、`frontend/src/views/admin-menu/AdminMenuView.vue`、`frontend/src/views/store/StoreView.vue`、`frontend/src/views/product/ProductView.vue`、`frontend/src/views/coupon-template/CouponTemplateView.vue`、`frontend/src/views/coupon-pack/CouponPackView.vue`、`database/migrations/003-admin-permission-seed.sql`、`database/migrations/README.md`
- 验证情况：已完成 `npm run build`、`dotnet build backend/FaJuan.sln -c Release -m:1`；已用 `sqlcmd` 连接 `127.0.0.1/FaJuanDb` 执行 `database/migrations/003-admin-permission.sql` 与 `database/migrations/003-admin-permission-seed.sql`，当前 `AdminPermission=28`、`AdminRolePermission=28`。
- 后续事项 / 风险：当前按钮权限已落库并给 `super_admin` 自动授权，但 EF Migration 尚未补对应历史；后续若继续依赖 EF 自动迁移，需要再补正式 migration 以避免模型与迁移历史漂移。
